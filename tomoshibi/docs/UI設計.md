# ともしび UI設計 — 判断と根拠

企画書 v2（Notion「ともしび — ペット供養アプリ 企画（v2 / 検証済み）」2026-09-13）を
画面に落とすにあたって下した判断と、その根拠を残す。

ここに書かれた「採らない」は、開発中に必ず揺らぐ。継続率が伸び悩んだとき、
採らないと決めたものは必ず「手軽な解決策」として候補に挙がる。そのときにこの文書を読む。

---

## 1. 分析した事例

### 失敗事例

**Facebook「Year in Review」（2014年12月）**
その年に亡くなった6歳の娘の写真を、紙吹雪のイラスト付きで「あなたの1年はこんな感じ！」と
ニュースフィード最上部に自動表示した。Web技術者 Eric Meyer 氏がこれを
*Inadvertent Algorithmic Cruelty*（意図せぬアルゴリズムの残酷さ）と書き、Facebook は謝罪した。

- 出典: https://meyerweb.com/eric/thoughts/2014/12/24/inadvertent-algorithmic-cruelty/
- 報道: https://www.forbes.com/sites/amitchowdhry/2014/12/29/facebook-year-in-review-apology/

**構造的な失敗理由**：大多数で正しく動くコードが、少数の喪失体験者に対してだけ最悪に振る舞う。
しかも「予告なし」と「祝祭的な装飾」が重なった。片方だけなら耐えられた可能性がある。

→ **本案への反映**
1. 再会ムービーを自動再生しない。必ず予告画面を挟む（画面09）
2. アプリ全体から紙吹雪・達成バッジ・称賛アニメーションを排除する
3. お参り完了後に出すのは一言だけにする（画面04）

**Replika（2023年2月の仕様変更）と、いわゆるグリーフボット全般**
常時対話できるAI人格は強い愛着を生み、提供側の都合で人格が変わると利用者が深く動揺する。

→ **本案への反映**：常時対話のAIチャットを実装しない。
手紙には常時「AIが書きました」を表示し、隠さない（画面08）。

**Duolingo型ストリーク（この文脈でのみ失敗）**
学習アプリでは継続率を大きく改善した実証済みの手法。だが弔いの文脈では、
1日休んだ人に「途切れました」と告げることは、故人への不義理を演出する行為になる。

→ **本案への反映**：連続お参り記録を実装しない。数字は常に「前にある節目」だけを示す。

### 成功事例

**Google フォト／Apple「メモリー」の除外設定**
両者とも、自動生成される思い出の提示に対して後年、除外の仕組みを追加した。
Google フォトは設定で「ユーザーとペットを非表示」と日付範囲の除外、
Apple は人物に対する「おすすめに表示しない」を持つ。

- 参考: https://www.androidpolice.com/how-to-hide-memories-of-certain-people-pets-and-dates-in-google-photos/
- 参考: https://tomsguide.com/phones/how-to-hide-someone-from-your-iphone-memories

**教訓**：自動提示は、除外できて初めて安全になる。しかも両者とも**後付け**だった。

→ ~~**本案への反映**：アルバムの全カードに「この期間は出さない」を常設し、設定の奥に隠さない。
章ごとに「出さない」を選べる（画面07）。~~

~~**ただし、既定では何も隠さない。**~~ 提案の段階では闘病期・最期の数週間を
既定で非表示にするつもりだったが、採らないことにした（追記2）。
のちに章ごとの除外そのものも全廃した（追記3）。

> **この教訓の当てはめを誤っていた。** Google／Apple で問題だったのは*不意打ち*、
> つまり本人が求めていないのに思い出が出てくることだった。
> ともしびのアルバムは本人が開きに行くもので、不意打ちの構造がない。
> 除外を借りてくる前提が、そもそも成り立っていなかった。詳しくは追記3。

**Stripe（配色とタイポグラフィの禁欲主義）**
信頼が要る製品ほど、色数と装飾を減らすほうが読みやすさと信頼感が上がる。

→ **本案への反映**：彩度のある色は灯りの橙1色のみ。
成功の緑・エラーの赤は通常の導線に登場させない（この製品に「間違い」はないため）。

**儀式効果（Norton & Gino, 2014）**
決まった手順の行為が喪失後の悲嘆を軽減する。宗教的意味がなくても、
本人が効くと信じていなくても効果が出た。効いているのは内容ではなく、
**自分でコントロールできる手順が毎回同じであること**。

→ **本案への反映**：お参りの4動作は順序・動き・音を固定。並べ替えも追加もできない。
**この画面に限り「飽きさせない工夫」は害**（画面03）。

**ピーク・エンドの法則（Kahneman）／予期効用（Loewenstein）**
体験の記憶は最も強い瞬間と終わり方で決まる。また人は良いことを待つ期間からも効用を得る。

→ **本案への反映**：再会ムービーの終端に「また来年、この日に。」を置く。
進捗バーは置かない（残り秒数を数えさせることが別れのカウントダウンになる）。

---

## 2. 画面ごとの判断

| # | 画面 | 中心的な判断 | 根拠 |
|---|---|---|---|
| 01 | おむかえ | 入力は なまえ・写真・旅立った日 の3つのみ | サンクコストを積み上げない。入力作業自体が負担 |
| 02 | お墓（0〜49日） | 押せるものは1つ。数字は「四十九日まであと35日」の1行のみ | 後ろ向きの計測も、途切れうる記録も置かない |
| 03 | お参りの4動作 | 灯明→水→ごはん→花。順序固定・約8秒・スキップ不可 | 儀式効果。変わらないことが効いている |
| 04 | お参りのあと | 一言だけ。達成率・記録・シェアなし | Year in Review の教訓 |
| 05 | お墓（50日〜半年） | 設定を触らせず、通知が自動で月命日に減る | デフォルト効果。深い悲嘆期に頻度を選ばせない |
| 06 | あの日まで | 直近1件だけ強調。宗派の切替とオフを画面内に明記 | 現在バイアス。遠い報酬を手前に引き寄せる |
| 07 | アルバム | 章を開いて一枚ずつ見る。隠す機能は持たない（追記3で全廃） | 開きに行くものに不意打ちはない |
| 08 | てがみ | 節目に一通3行。届く日は予告済み。AI表記を常時表示 | 変動報酬を排す。あの子が書いたと信じさせない |
| 09 | 命日の予告 | 自動再生せず、何が起きるかを先に全部言う。「やめておく」を同格に | 断ること自体を不義理に感じさせない |
| 10 | 再会の6秒 | UIなし全面黒。進捗バーなし。終端は「また来年、この日に。」 | ピーク・エンド。終わりに未来を置く |
| 11 | 設定 | 頻度はラジオボタンで選ばせず、曲線と「もう少し」2つだけ。持ち出しを最上段 | 設定画面は判断力が落ちた人に最も残酷。損失回避の反転 |

---

## 3. 実装しないUI

| 手法 | 継続率への効果 | それでも採らない理由 |
|---|---|---|
| 連続お参り記録（ストリーク） | 大 | 悲嘆に罪悪感を接続する |
| 達成バッジ・紙吹雪・称賛アニメ | 中 | 弔いが実績管理に変わる |
| ランダムに届く思い出 | 大 | 反復視聴を加速させる |
| 退会引き止めダイアログ | 中 | 手放せる自由がある人だけが安心して留まれる |
| 他人との比較・ランキング | 中 | 悲しみ方に優劣を持ち込む |
| 常時対話できるAIチャット | 大 | 最も依存を生み、最も失望される |

---

## 4. 既存アプリとの関係

このリポジトリの他アプリ（cashbook / dividend / yosou / solar / sekaishi / youtube）とは
**トークンの構成（変数名・余白スケール・角丸の考え方・ダークモードの持ち方）を共有し、値だけを差し替える**。

意図的に反転させたのは1点：**既定テーマをダークにした**。
理由は3つ。(1) 灯りは暗闇でしか灯りに見えない。(2) 悲嘆は夜に強まり、お参りは就寝前の習慣になる。
(3) 1画面の主役を灯り1つに絞るには、他をすべて沈める必要がある。

書体の使い分け（名前・日付・手紙は明朝、操作ラベルはゴシック）は、
cashbook が帳簿という主題に明朝を当てた前例と同じ考え方。

---

## 5. 未決事項

1. ~~製品名に「わんず」を残すか。~~ → **「ともしび」単独で決着**（2026-09-14）。
   企画の市場は犬猫1,600万頭。「わんず」は犬に限定して聞こえ、猫の飼い主が自分向けだと思えない。
   種類の選択に「そのほかの動物」を足したあとでは、名前だけが犬に寄っている状態は筋が通らない。
2. **「つらいときの相談先」の実体。** 実在する窓口を確認するまで表に出せない。
   架空の窓口を出すことが、この製品で最悪の事故になる。
3. ~~最期の数週間の写真を既定で非表示にするか。~~ → 隠さないことで決着（末尾の追記2）。
4. **四十九日の「一度だけの頻度選択」をどの画面で出すか。** 本案は設定の曲線に集約したが、
   企画の事前コミットメントの趣旨からは専用の1画面にする案もある。
5. **お参りの4動作は90日の反復に耐えるか。** 企画の検証仮説そのもの。
   ここが崩れたら「変えない」という前提ごと再設計が要る。

---

*事例の一次出典は本文中のURLを参照。市場・競合・価格の数値は企画書 v2 に基づく（被験者1名の一次検証段階）。*

---

# 追記：作り直し（2026-09-14）

## 方向を変えた

夜の弔い → 昼の「いっしょにいる」。

前の案は「別れを受け入れる」側に寄っていた。**Continuing Bonds（継続する絆／
Klass, Silverman & Nickman, 1996）** は、故人との関係を続けることこそ健全だとする
現在の主流モデルで、「手放す」を前提にした古いグリーフ観を置き換えたもの。
利用者の「1年後のお楽しみではなく、もっと身近にここにいる感を」という直感は、
こちらに沿っている。

| | 前 | 今回 |
|---|---|---|
| 地 | 夜 #0e1013 | 昼 #FDFAF2 |
| 書体 | 明朝 | Zen Maru Gothic ＋ Zen Kaku Gothic New |
| 絵 | シルエット1体 | いぬ・ねこの手描き風イラスト（写真が入れば消える） |
| 再会 | 年に一度だけ | 端末の動画をいつでも |
| 数字 | 次の節目まであと◯日 | おまいり通算◯回目 |
| — | — | おまいりの庭（花＝通算） |

**捨てたもの**：年1回にすることで得ていた予期効用（待つ364日に意味を持たせる）と、
「年に一度しか渡さないものがあるからアンインストールできない」という解約防止。
日常的に会えるようにした以上、この2つは効かない。継続の理由は別に要る。

## 通算回数はストリークではない

利用者の「何回目、を入れて」に対して、**累計**で答えた。

| | 連続記録 | 通算回数 |
|---|---|---|
| 1日休むと | 途切れる | 何も起きない |
| 表示 | 「途切れました」 | 減らない |
| 生む感情 | 罪悪感 | 積み重なった実感 |

Duolingo で継続率を大きく上げた仕掛けが、弔いの文脈では凶器になる。
庭の花も同じ性質で、枯れない・減らない・他人と比べない。

## 写真が入らなかった原因（記録として残す）

1. **IndexedDB が使えない環境がある。** 埋め込み（iframe）表示、iOS Safari の
   サードパーティ制限。`indexedDB.open()` が通っても書き込みで落ちる環境もある。
2. **失敗を誰も受け取っていなかった。** `shrink().then().then()` に `.catch` が無く、
   保存が失敗すると「取り込んでいます…」のまま永久に止まった。

対策は、(a) 起動時に**実際に書いて消す**試験で可否を判定する、
(b) 保存関数は決して reject せず `{ok:false, reason}` を返す、
(c) reason を人の言葉にして**必ず画面に出す**、の3点。

**この製品では、黙って失敗することが最悪の挙動である。**
弔いの対象の写真が入らず、理由も出ないという体験は、機能の欠落より重い。


---

# 追記2：最期の数週間の写真を、既定で隠さないと決めた（2026-09-14）

利用者の判断で「非表示にしなくてよい」となった。**この判断のほうが正しいと考える。**

## なぜ隠そうと提案したのか

3つ重ねていた。

1. **Google フォト・Apple メモリーの教訓。** 自動で思い出を提示する機能は、
   除外の仕組みを後付けするまで人を傷つけた。除外できて初めて安全になる。
2. **闘病期の写真の性質。** 最期の数週間は、管につながれた姿や、痩せた姿や、
   病院での写真であることが多い。準備なく目に入りやすい種類の画像。
3. **ピーク・エンドの法則との整合。** 再会ムービーで「終わりに何を置くか」を
   設計した以上、アルバムでも最悪の画像が終端に来るのを避けたかった。

## なぜ取り下げるのが正しいか

1. **本人の写真で、本人の悲しみである。** 既定で隠すのは、自分のペットの
   どの姿を見てよいかをアプリが決める行為になる。差し出がましい。
2. **隠すこと自体が意味を持ってしまう。** 「この写真は見せられないほどひどい」
   という枠づけになる。多くの人にとって最期の数週間は、最後に抱けた時間でもある。
3. **Continuing Bonds と矛盾する。** 関係を続けることを中心に置き直したのに、
   つらい部分だけ編集して外すのは、その方向と逆を向いている。
4. **そもそも判定が雑。** 撮影日から「最期の数週間」を機械的に切ると、
   最後の旅行のような良い記憶まで一緒に隠れる。

## 残すもの

**章ごとの「この期間は出さない」は残す。** Google/Apple の教訓が言っているのは
「除外できること」であって「既定で隠すこと」ではない。
選べる状態にしておいて、選ぶかどうかは本人に委ねる。

> **追記3で撤回。** この「残すもの」も、のちに全廃した。理由は末尾の追記3。

## 記録の訂正

既定で隠す処理は、**実装には一度も入っていなかった**。提案書（`proposal.html`）に
書いただけで、アプリの `photoHidden` は空で始まり、本人が押したときだけ埋まる。
それにもかかわらず、PR本文や説明で「既定で非表示」と書いていた。
この追記は、提案を正式に取り下げると同時に、記録を実装と一致させるもの。

---

# 追記3：章ごとに隠す機能をやめ、アルバムを開けるようにした（2026-09-14）

利用者から2つ。**「アルバムが登録できるけど、見れない」**、
そして**「この期間は公開しないとあるが、全て公開にして良い」**。

## 見れなかったのは、こちらの手落ち

写真は入るし、章立ても出ていた。だが**章を開く手段がどこにもなかった**。
章の行には「···」のメニューしか押せるものがなく、その中身は
「この期間は出さない」と「名前をつける」だけ。
つまり**隠す操作は作ってあって、見る操作を作っていなかった**。

入れた写真が見られないアルバムは、アルバムではない。これは論点ですらなく、
ただの欠落である。章の行そのものを押せるようにし、写真の一覧と、
一枚ずつ大きく見る画面を足した。

## 隠す機能の全廃

追記2で「除外できることと既定で隠すことは別」と書き、章ごとの除外は残した。
今回それも外す。

理由は、**除外という機能が支払わせている代償のほうが大きい**と判断したため。

1. **隠す操作だけがあって、見る操作がなかった。** どちらを作りたかったのかが
   そのまま表れている。設計の重心が「見せない」側に寄っていた。
2. **除外の必要は、Google/Apple の文脈から借りてきたもの。** あちらは
   *不意打ち*が問題だった。突然「3年前の今日」として出てくるから傷つく。
   ともしびは利用者が**自分でアルバムを開きに行く**。不意打ちの構造がない。
3. **自分で入れた写真である。** カメラロールから1枚ずつ選んで入れたものを、
   あとで隠す前提で設計するのは筋が通らない。見たくなければ入れなければよく、
   入れたあとで要らなくなれば**消せばよい**（一枚ずつ消せるようにした）。

除外を外したぶん、**消す**を写真ごとに用意した。
隠す（見えないが残る）より、消す（無くなる）のほうが意思がはっきりする。

## 記録の訂正

追記2の「残すもの」節は、この追記で撤回された。
節そのものは消さずに残し、撤回された旨だけを付けてある。

---

# 追記4：享年・命日と、戒名を入れた（2026-09-14）

利用者から「トップに享年何歳・何月何日没を入れて」「戒名を入れて、登録するペット固有のものに」。
戒名の中身は任せる、とのことだったので、ここに設計の根拠を残す。

## 戒名を、アプリが作ってよいのか

作ってよいとは考えていない。**戒名はお寺が授けるもので、アプリが授戒できるはずがない。**
実際、動物に戒名を出すこと自体が宗派によって扱いが分かれる（戒＝戒律を保つ主体という前提があるため）。
一方で、ペット霊園や動物供養を行う寺院が、牧会的な配慮として戒名や法名を出す例は現に多い。

そこで**授けるふりをしない**ことを設計の起点にした。

1. 画面にもドキュメントにも「**このアプリがこの子のために選んだ名です。お寺が授けたものではありません**」と明記する
2. **書きかえられる**。お寺からいただいた戒名があれば、そちらが常に優先される
3. **出さないこともできる**。戒名になじみのない人に、宗教的な様式を既定で押しつけない
4. **どの字がどこから来たかを、すべて開いて見せる**。由来の分からない名を渡すのは贈りものではなく押しつけになる

4 が肝だと考えている。自動生成された文字列を「あなたの子の戒名です」と出すだけなら、
ありがたみを装った乱数でしかない。**根拠を全部見せて初めて、選んだ名だと言える。**

## 組み立て

人の戒名にならい、**道号2字＋法号2字＋位号2字**。

| 位置 | 何から決まるか | 例（モコ・犬・2013.4.10生・2025.11.3没） |
|---|---|---|
| 道号 | 旅立った季節（没月を2か月ずつ6つに） | **冬晴** 冬の晴れたころに旅立った |
| 法号・上 | この子の名の頭の一字 | **望** 「モコ」の頭の一字 |
| 法号・下 | 生まれた季節（なければ種類から） | **和** 春に生まれた |
| 位号 | ペット供養で広く使われる結び | **霊位** |

→ **冬晴望和霊位**

カタカナ・ひらがなの名は、頭の音を一字に置きかえる（モ→望、ハ→晴）。
これは**万葉仮名と同じ、音を借りるやり方**で、人の戒名でも名の一字を採るのが通例なのに倣っている。
漢字の名ならその一字をそのまま使う。

意図したのは、**生まれた季節と旅立った季節が名の両端に来る**こと。
名前がその子の一生をそのまま包む形になり、6字がこの子だけのものになる。
同じ名・同じ種類でも、生没の季節が違えば別の名になる。

## 表示

**縦書きの毛筆で、遺影の右に立てる。** 位牌と同じ構えにした（当初は写真の下に横書きで置いていたが、追記5で改めた）。
縦書きの日本語は右から左へ読むので、**右に置くのが順序として正しい**。

書体はこの一か所だけ毛筆にする。アプリの他の文字はすべて丸ゴシックと角ゴシックなので、
**書体と向きが変わること自体が「ここは格が違う」という合図**になる。

Stripe の禁欲主義（色数と装飾を減らすほど信頼感が上がる）と同じ考えで、
**色は増やさず、書体と向きの対比だけで格を作った**。

トップの並びは 戒名（右に縦）→ 名前 → 享年・命日 → 生年月日・いっしょにいた日数。
一番大きいのは**名前**のままにしてある。戒名は儀礼の名で、飼い主がこの子を呼ぶのは名前のほうだから。

### 縦に積むのに writing-mode を使わない

`writing-mode: vertical-rl` で組んだところ、**6字が同じ場所に重なった**。
毛筆体が届かず別の書体に落ちたとき、その書体に縦書きの字送り（vmtx）が入っていないと、
字送りが 0 になる。実測で 6 字の高さが 15px しかなかった（字間の指定ぶんだけ）。

**1字ずつ箱にして縦に並べる**ことで解決した。どの書体でも必ず縦に立つ。
毛筆が読み込めない・オフラインのときは明朝に落ちるが、**縦書きと墨色は残るので格は保たれる**。
書体の有無に見た目の根幹を預けない、というのがここでの判断。

字数に応じて大きさを自動で決め、遺影の絵（高さ212px）からはみ出さないようにしている。
院号つきの長い戒名を手で入れても収まる。

## 享年

満年齢で数える。1年に満たない子は「享年 6か月」と月で出す。
**0歳と出すより、6か月と出すほうがその子の時間に近い。**

---

# 追記5：戒名を縦書きの毛筆にし、遺影の右へ置いた（2026-09-14）

利用者から「戒名は縦に書道の字体で。配置は写真の右側あたり」。

## なぜ右なのか

**縦書きの日本語は右から左へ読む。** だから縦に立てた戒名は右に置くのが順序として正しい。
仏壇で遺影と位牌が並ぶ配置とも合う。左に置くと、読み順の上では名前より後ろになってしまう。

遺影の円は、戒名を出しているときだけ 17px 左へ寄せて釣り合いをとる。
中央のままだと右だけ字で詰まって見える。

## 書体

毛筆体は端末に入っていないので **Yuji Syuku**（Google Fonts）を読み込む。
Google Fonts の日本語は**使う字の含まれる断片だけ**を配るので、増えるのは数KB。
すでに Zen Maru Gothic と Zen Kaku Gothic New を同じ仕組みで読んでいるので、構成は変えていない。

## 踏んだ失敗：writing-mode で字が重なった

`writing-mode: vertical-rl` で組んだら、**6字が 15px の高さに重なった**。
毛筆体が届かず別の書体に落ちたとき、その書体に縦書きの字送り（vmtx）が無いと字送りが 0 になる。
残ったのは字間の指定ぶんだけだった。

**1字ずつ箱にして縦に積む**方式に変えた。これならどの書体でも必ず縦に立つ。
オフラインで毛筆が読み込めなくても、明朝で縦に並ぶ。

教訓は、**見た目の根幹を「読み込めるかどうか分からないもの」に預けない**こと。
書体は格を上げる飾りであって、縦書きという構造そのものは書体に依存させてはいけない。

## ついでに直した：夜の地面

戒名を絵の上に重ねてはじめて気づいたが、**夜のテーマで地面だけ明るい緑のまま**だった。
絵の色が SVG の塗り属性に直に書いてあり、CSS のテーマ切り替えが届いていなかった。
薄墨の最後の字がその上に来て読めなくなるため、夜の色を当てた（地面・雲・草）。
戒名とは別の、前からあった不具合。

---

# 追記6：字の大きさを9段に整理し、右上のあいさつを外した（2026-09-14）

利用者から「右上のこんにちは、は不要」「文字が全体的に小さくスマホで見にくい。全体のバランスを整えて」。

## 何が起きていたか

字の大きさを数えたら、**10px〜38px が20種類**ばらばらに使われていた。
しかも分布が下に偏り、**文章の多くが 11〜13px 台に沈んでいた**。

小さくて読みにくいうえに、**階層も作れていない**。
20種類あっても、隣り合う大きさの差が 0.5px では差として読めないからである。
「なんとなくこれくらい」で書き足していった結果だった。

## 9段に決めた

| 段 | 大きさ | 役目 |
|---|---|---|
| --fs-1 | 12.5px | 注記。**これより小さくしない** |
| --fs-2 | 14px | 補助の説明 |
| --fs-3 | 15.5px | 小さめの本文 |
| --fs-4 | **17px** | **本文の基準** |
| --fs-5 | 19px | 少し強い本文・ボタン |
| --fs-6 | 22px | 小見出し |
| --fs-7 | 26px | 画面の見出し |
| --fs-8 | 31px | 名前 |
| --fs-9 | 42px | 大きな数字 |

日本語の本文は16px以上が読みやすさの下限とされる。基準を 15px → **17px** に上げた。
最小も 10px → 12.5px。以後、**この9段の外を使わない**。

Apple HIG の教訓（色・余白・字をトークン化して使い回すと、個々の画面の完成度以上に
製品全体の信頼感が上がる）をそのまま当てた。同じ理由で余白は既に --s1〜--s8 で揃えてある。

## 字を大きくして出てきたもの

大きくしたことで、**それまで偶然おさまっていた崩れが表に出た**。

1. **てがみカードの見出しと説明が同じ行に流れ込んだ。**
   `span` のままで `display:block` が無く、`margin` も効いていなかった。前からの不具合。
2. **「おまいりする」が画面の外へ落ちた。** カードが伸びたため。
   この操作はこのアプリの中心なので、**絵の高さを画面に応じて縮める**ことにした
   （`clamp(176px, 26vh, 224px)`）。遺影の円も絵に連れて縮める。固定のままだとはみ出した。
3. **季節のおそなえの説明が2行になり、最後に1字だけ残った。** 読点を1つ削り、行の長さをそろえた。

Digg v4 の教訓（見た目を刷新しても、依存している核の導線を壊さない）が2にあたる。
字を大きくすること自体は正しくても、**中心の操作を画面外へ追い出してよい理由にはならない**。

## あいさつを外した

右上の「おはよう／こんにちは」を削除。
時刻で変わるだけで何も伝えておらず、日付と並んで上の帯を混ませていた。
消したことで、上に残るのは日付ひとつになった。

## 残っている制約

画面の高さ 568px（iPhone SE 初代）では、「おまいりする」が最初の画面に収まりきらない。
**これは今回の変更で生じたものではなく、変更前からそうだった**（変更前 -39px、変更後 -31px）。
絵を縮めたぶん、むしろ少し良くなっている。

---

# 追記7：「じぶん」のタブを足した（2026-09-14）

利用者から「1番下のアイコンに自分の状態のアイコンを入れて、すぐに自分の状況を見れるようにして」。

## なぜ独立した画面にするか

いまの気分の記録は、**おまいりの庭の下の方に埋まっていた**。
花と数字を見て、さらにスクロールしないと自分の波が出てこない。
毎日のように開く場所が、いちばん奥にあった。

画面を分けたのは、置き場所の問題だけではない。

**にわ＝この子の画面、じぶん＝飼い主の画面。**
死別への対処は、失った相手に向き合う時間と、自分の生活を建て直す時間を
**行き来しながら**進む（Dual Process Model／Stroebe & Schut, 1999）。
2つは別の営みなので、画面も分けたほうが形に合う。
タブを切り替えるという操作そのものが、その行き来をなぞる。

## 画面の中身

1. **きょうの気分**（1〜5）。おまいりをしなくても、ここだけで記録できる
2. **この30日の波**（グラフ）
3. **記録した日数**と**はじめた日**
4. おもい日が続いたときだけ出る**相談先**

3で数えるのは日数と日付だけにした。
平均も、良い日の割合も出さない。**数えたとたんに良し悪しになる**ため。
おもい日が続いていることは、4の知らせで伝えれば足りる。

## タブが5つになった

5つ並ぶと、狭い端末（320px）では1列60pxしか取れない。
「おまいりの庭」は約70pxあって収まらないため、**タブでは「にわ」と短く呼ぶ**。
画面の見出しは「おまいりの庭」のままにしてある。iOS のタブと同じ扱いかた
（短いタブ名＋完全な画面名）。

「じぶん」を**まん中**に置いた。両隣がこの子に関する画面（にわ・アルバム）で、
中心に自分が立つ配置になる。アイコンは人のかたち。

---

# 追記8：じぶんの画面で「何を見るのか」を言い切った（2026-09-14）

利用者から4つ。「ここで何を見るのかをはっきりさせたい」「ペットの死を乗り越えて自分らしく
生きていくための何かだと記載して」「『記録しました』は今日は記録したという型に。変更もできると」
「『答えなくてもよい』はやめて。**この機能がこのアプリでは最重要**だから」。

## 最重要のものを、控えめに扱っていた

「答えなくても大丈夫です」は、押しつけを避けるつもりで書いた。
だが**この記録はこのアプリの要**である。要になるものに「やらなくていい」と添えるのは、
配慮ではなく**自信のなさ**だった。外した。

代わりに置いたのは「良し悪しはありません。いまの感じに近いものを。」
**やらなくていいとは言わず、間違いはないとだけ言う。** 押しつけずに勧めるのは、これで足りる。

## 何を見るのか

見出しの下に置いた：**「あの子を見送ったあとの毎日を、あなたらしく生きていくための場所です」**

利用者の言葉は「乗り越えて」だったが、そこは変えた。
悲嘆は乗り越えて終わるものではなく、**かたちを変えて続いていく**というのが
Continuing Bonds（Klass, Silverman & Nickman, 1996）の立場で、このアプリの前提でもある。
「乗り越える」と書くと、忘れることが目標のように読めてしまう。
**「見送ったあと」なら起きた事実だけを言う**ので、前提と矛盾しない。

グラフの下には、見かたそのものを置いた。

> ここで見るのは、良くなったかどうかではありません。**波のかたち**です。
> おもい日のあとに、すこし戻る日が来る。また下がる。その行き来そのものが、前へ進んでいる形です。

これは Dual Process Model をそのまま画面の言葉にしたもの。
**グラフが右肩上がりでないことは失敗ではない**と、見る前に言っておく。

## 記録の状態を、日のこととして言う

「記録しました」→**「きょうは 記録しました」**。
1日に1つの記録なので、いつのことか分かる形にした。

補助の行も状態で変える。
- まだ：良し悪しはありません。いまの感じに近いものを。
- 記録済：**押しなおせば、あとから変えられます。**

保存は `selfLog[日付] = 値` で、同じ日を押しなおせば**後から押したほうで上書きされる**。
できるのに言っていなかったので、書いた。

実測で確認：2 → 5 と押しなおすと、記録した日数は8のまま値だけが5に変わり、
開き直しても5が残り、おまいりのあとの画面にも同じ状態が出る。

---

# 追記9：導線を全部たどり直した（2026-09-14）

利用者から「もう一度全体の導線を精査して、おかしなところは直して。人間工学も勉強して」。
14画面すべての操作要素を実測し、出口を1つずつたどった。

## 1. 押した言葉と、起きることが違っていた

**「モコへ てがみを書く」を押すと、手紙の一覧が開いていた。**

```js
if ((st.letters || []).length) show('mails'); else show('write');
```

1通でも書いたことがあると、以後このカードは書く画面に行かない。
**書きたい人が、書く画面にたどり着けない。**
カードの説明文も「これまで 1通 ・ いま伝えたいことを」と、
過去を見ることと新しく書くことを1行に混ぜていた。

原因は、1つの入口に2つの用事を持たせたこと。
直しかたは、**用事の数だけ入口を作る**。

- カード（大きい）＝**書く**。いつ押しても書く画面へ
- その下の控えめなリンク＝**これまでのてがみ（N通）**。1通もなければ出ない
- 書く画面の下にも同じリンクを置いた（書きに来てから読みたくなることがある）

## 2. 逃げ道が、いちばん押しにくかった

「やめる」「とじる」「もどる」は、上の帯に置いた **23px** のボタンだった。
指の最小寸法は44px（Apple HIG / Material とも同等）。**半分しかない。**

しかも上の帯は、**片手持ちで親指がいちばん届きにくい場所**である
（Steven Hoober の観察研究では、片手持ちでの操作が約半数を占め、
画面上部は親指の可動域の外に出る）。

**小さい × 遠い** が重なっていた。逃げ道は、押し間違えると困る操作ではない。
むしろ迷ったとき・つらくなったときに押すものなので、**いちばん押しやすくてよい**。
44px に広げ、押せる範囲を余白のぶんまで拡げた。

44px未満だったものは他にもあり、すべて揃えた。

| 場所 | 前 | 後 |
|---|---|---|
| やめる／とじる／もどる（上の帯） | 23px | **44px** |
| 再生・写真の「とじる」「···」 | 42px | **46px** |
| 手紙の書き出し候補 | 40px | **46px** |

## 3. 一覧の出口が上にしかなかった

これまでのてがみは、**下まで読んでから上に戻らないと閉じられない**。
読み終えた位置＝画面の下に、もう1つ「とじる」を置いた。

同じ問題が起きていないか他の画面も見たが、章のなかみにはタブがあり、
おまいり・あとの画面は出口が下にあるので、直す必要があったのはここだけだった。

## 確かめかた

画面を1つずつ表示して、**押せる要素を全部数えて寸法を測る**やり方をとった。
目で見て回ると必ず見落とす。23pxの3つも、実測ではじめて分かった。

結果：14画面・全操作要素が44px以上。出口のない画面なし。

---

# 追記10：気持ちの欄を、誘う言葉にした（2026-09-14）

利用者から「『きょうは 記録しました』は、今の気持ちを登録して というニュアンスでやさしく」、
続けて「**今日の気持ちを伝えましょう**にして」。

## 「記録しました」が持っていた向き

「記録しました」は、**こちらが処理を終えた報告**である。主語がアプリになっている。
ここで扱っているのはその人の気持ちなので、アプリの都合ではなく気持ちの言葉で言う。

| | 前 | 後 |
|---|---|---|
| まだのとき | いまの気分は、どうですか | **きょうの気持ちを 伝えましょう** |
| 記録したあと | きょうは 記録しました | **きょうの気持ちを 伝えました** |

**誘いと、その返事。** 同じ言葉づかいで対にした。
「どうですか」は問診に近く、答える側に説明を求める。「伝えましょう」は一緒にやろうと誘う。
この欄はこのアプリの要なので、控えるのでも問い詰めるのでもなく、**誘う**のがいい。

「伝える」は、手紙を「とどける」と同じ向きの言葉でもある。画面をまたいで語彙が揃う。

なお「きょう」は仮名のままにした。「きょうで1回目」など、このアプリは一貫して仮名で書いている。

---

# 追記11：ローマ字の名前と、戒名の注記（2026-09-14）

利用者から2つ。「戒名は他の名前でもちゃんと名前にあった形ででるの？」に答えて確かめたところ、
**ローマ字の名前だと戒名が空になる**不具合を見つけたので、確認とあわせて直した。
続けて「戒名のところに、ちゃんと戒名という表記もして。いきなりだとなんだかわからないから」。

## ローマ字の名前で戒名が出なかった

`headChar()` は、漢字ならそのまま・平仮名とカタカナは音の表から拾う、という2つの道しか
持っていなかった。**Coco・Momo のようなローマ字の名前は、どちらにも当てはまらず空文字を返す。**
戒名の6字のうち1字が決まらないので、`kaimyoParts()` が `null` を返し、**戒名そのものが出なくなる**。
エラーにはならず、静かに非表示になるだけなので、報告がなければ気づけなかった。

### 直しかた

ローマ字の綴りから頭の音を拾う対応表（`ROMA`）を足し、平仮名の道が外れたときだけ
最後の手段として通すようにした。長い綴りから先に照合する（"cho" が "c" に化けて
取りこぼさないように）。日本語の音韻にない区別は寄せる：
`l` は `r` に、語頭の `c`（`ch` を除く）は `k` の音として扱う（Coco→Koko）。

厳密な発音より、「先頭がどの行の音か」さえ合えばこの用途には十分と判断した。

| 名前 | 戒名 | 頭の字の由来 |
|---|---|---|
| Coco | 秋水**幸**和霊位 | Coco→Koko→こ→幸 |
| Momo | 薫風**望**清霊位 | Momo→も→望 |
| Charlie | 冬晴**千**和霊位 | Charlie→cha→千 |
| Cookie | 寒月**幸**実霊位 | Cookie→koo→こ→幸 |

既存の仮名・漢字の名前（モコ・太郎など）は出力が一字も変わらないことを確認した
（`headChar` の最初の2つの道は手を加えていない）。

## 「戒名」の注記

縦書きの毛筆だけを遺影の横に出しても、はじめて見た人には何の文字か伝わらない。
**「戒名」の2字を、毛筆の右肩に小さく添えた。**

注記は本文ではないので、書体をあえて分けた。毛筆ではなく地の角ゴシック、
色も墨より薄い `--muted`。本文の字を大きくしても注記までつられて大きくならないよう、
相対単位ではなく固定の10pxにしている。

設定画面には既に見出し「戒名」があるので、そちらには重ねて出さない
（`.kaimyo-box .kaimyo-label{display:none}`）。

注記の高さぶん、遺影の絵からはみ出さないための字の大きさ計算（`fitBrush`）にも
15px を追加で差し引いた。390×844・320×568・戒名6字〜12字・昼夜の組み合わせで
はみ出しがないことを実測した。

---

# 追記12：「てがみ／やめる」が見にくかった不具合を直す（2026-09-14）

利用者から実機の画面を見せてもらった。「てがみ」が「てが」「み」に割れて2行になり、
「やめる」との間が不自然に空いていた。続けて「やめるボタンにして」。

## 原因：CSSの詳細度の事故

前回の監査（追記9）でタップ領域を44pxに広げるため `.bar-btn` を足した。
このとき `.bar-btn{width:auto; min-height:44px; ...}` を `.btn` より**前**に書いてしまった。

`.bar-btn` も `.btn` も単一クラスのセレクタで詳細度は同じ（0,1,0）。
同じ詳細度がぶつかったとき、**あとに書かれたルールが勝つ**。`.btn` が後に来るので、
`.btn{width:100%; min-height:60px}` が `.bar-btn` の指定を丸ごと踏みつぶしていた。

実測すると、「やめる」ボタンは幅238〜307px（本来80px程度でよい）に膨らみ、
flexの縮小計算でその分だけ「てがみ」側が44〜45pxまで圧縮され、3文字が入りきらず
2行に割れていた。`.sub-link`（これまでのてがみ／あたらしく書く の下の入口）も同じ理由で
ずっと壊れたままだった。

見た目のレビューだけでは気づけなかった。原因はブラウザで実測して、
はじめてはっきりした（`.btn` の width:100% が勝っている、という事実は目視では分からない）。

## 直しかた

`.bar-btn` と `.sub-link` を `.btn.bar-btn` / `.btn.sub-link` という**2クラスの複合セレクタ**にした。
詳細度が (0,2,0) になり、`.btn` の (0,1,0) に**ファイル内の順番と関係なく**必ず勝つ。
これで今後また前後関係が変わっても同じ事故が起きない。

## 「やめる」をボタンらしい見た目に

あわせて、`.btn-ghost`（枠なし・地の色と同化）から `.btn-line`（2px の枠つき）に替えた。
文字だけが浮いているとタップできると気づきにくい。枠を足すことで、
「これは押せるものだ」という手がかりを最初から与える。

対象は「もどる」（章のなかみ）・「やめる」（てがみを書く）・「とじる」（これまでのてがみ）の3つ。
一覧の下にある2つ目の出口（「とじる」「あたらしく書く」の下）は元々 `.sub-link` の別デザイン
（全幅・アンバー色・枠なし）のままにしてある。上の帯とは役目が違うため。

390〜430px幅・3画面で1行に収まること、タップ領域が44px以上を保っていること、
アルバムと手紙の一連の流れに退行がないことを確認した。

---

# 追記13：はじめての起動で「つぎへ」に届かなかった（2026-09-14）

利用者から「初めからやる場合、名前入れて、写真取り込んだらそこから先へ進まない。次へのボタンがない」。

## 原因：スクロールできる画面だけに付けるクラスが、最初の画面だけ抜けていた

このアプリの各画面は `<div class="body ...">` の中身を縦に並べ、はみ出す画面には
`scroll` というクラスを付けて `overflow-y:auto` にしている。付けていない画面は、
はみ出したぶんが**見えなくなるだけで、スクロールでは届かない**。

最初の登録画面（`view-onbo`）だけ、このクラスが**最初から付いていなかった**。
名前・犬猫・写真の3画面を進むと内容が積み上がり、実測で高さ 899〜921px になる。
「つぎへ」ボタンはその一番下、上から721px の位置にある。

- **iPhone 14（390×844）** なら 844px あるので、ぎりぎり画面内に収まっていた
- **iPhone SE（320×568・375×667）** では、画面の高さがそもそも721pxに届かない。
  ボタンは常に画面の外にあり、**スクロールする手段もないので絶対に押せない**

大きい端末で確認していたときは気づけず、小さい端末ではじめから触ると
写真を選んだところで詰む。利用者の報告のとおりだった。

## 直しかた

`class="body foot"` を、他の全画面と同じ `class="body scroll foot"` にした。
1行だけの変更。

## 確かめたこと

320×568・375×667・390×844 の3サイズで、名前 → 犬猫 → 写真 → 日づけ → 好きだったもの
の5ステップを実際にスクロールしながら最後まで通し、`おうち` 画面へ着地して
名前が保存されることを確認した。全11画面のはみ出し・切れにも退行なし。

---

# 追記14：iPhone・Android 横断チェック（2026-09-14）

利用者から「iPhone、Android両方で全て使いやすく動くように確認して」。
実機7サイズ（iPhone SE(1st)〜14 Pro Max／Galaxy S8系・Pixel 7・Galaxy Fold折りたたみ内）
×全11画面を自動測定し、見つかった実害のある不具合を直した。

## 見つけて直したもの

**1. 「ホーム画面に追加」の案内が iOS 前提のままだった。**
「共有ボタンから」は iOS Safari の場所で、Android のブラウザに共有ボタンの列はなく
右上の「⋮」メニューにある。存在しないボタンを探させていた。
`navigator.userAgent` で iOS／Android を見分け、機種ごとの文言を出し分ける
`homeHint()` を作り、3箇所（初回登録・おうち・設定）の案内文をこれで埋めるようにした。
iPadOS はデスクトップ名を名乗るため、`platform==='MacIntel' && maxTouchPoints>1` で
タッチ対応の Mac として個別に検出している。

**2. `color-scheme` を宣言していなかった。**
Android Chrome の「ウェブサイトを自動的に暗くする」は、`color-scheme` の宣言がない
ページをヒューリスティックに判定して勝手に反転させることがある。このアプリは
自前で昼／夜を切り替える設計なので、それとかち合うと意図しない配色になりうる。
iOS にはこの挙動がなく、この端末だけでは気づけない不具合だった。
`applyTheme()` で `document.documentElement.style.colorScheme` を
昼夜の切り替えと一緒に明示するようにした。

**3. 気分の5択ボタンが 320px 幅で 44px を割っていた。**
`.scale{gap:8px}` が幅を食い、iPhone SE(1st) で実測 40〜42px。
gap を 6px に詰めて 44.00px ちょうどまで確保した（`追記` 内既出、今回全端末で再確認）。

あわせて、「じぶん」画面と違い「おまいりのあと」画面（`.after`）だけ左右の余白が
26px（`.body` は20px）と6px広く、同じ `.selfask` カードを置くと5分割ボタンだけ
さらに2.4px/個縮んで iPhone SE(1st) で 42px に落ちていた（他画面は44px超で通っていたため
これまで見落としていた）。`.after .selfask` にだけ左右 `-6px` のマイナスマージンを足し、
外側の余白ぶんを打ち消して幅をそろえた。

**4. 設定のバックアップ行が 320px で2行に割れていた。**
「バックアップを書き出す」「バックアップから戻す」。すぐ上に見出し「バックアップ」が
あるので言葉の繰り返しは不要と判断し、「書き出す」「読み込む」に短縮した。

**5.（ついでに）タップの手ごたえが機種で揃っていなかった。**
Android は既定でボタンの形を無視した灰色の四角が光り、iOS の WebKit は既定でほとんど
何も光らない。どちらも半端で、しかも機種によって見え方が違う。
`-webkit-tap-highlight-color:transparent` で既定の光りかたを消し、
まだ専用の `:active` を持っていなかった要素（設定の行・戻る系ボタン・タブ・
お供え・選択肢群・章の一覧・季節の行・レター一覧など）に共通で `opacity:.6` の
押下フィードバックを足した。これは報告された不具合ではなく、横断チェック中に
気づいて直したもの。

## 判断して直さなかったもの

**Galaxy Fold の折りたたみ内側（280px幅）** は、気分の5択ボタンが上の gap 詰め後も
36px までしか確保できず（`280px - .body余白36px×2 - gap24px ÷ 5 = 36.8px`）、
「この端末のデータを消す」の行も2行に割れる。280px は現在のカード余白構造のままでは
5分割ボタンを44px×5に収める数式上の余地がない。これは折りたたみ機の**外側の
カバーディスプレイ**という一時的な確認用の表示面であり、このアプリを主に使う面ではない
と判断し、余白の構造自体を作り替えてまで追いかけるのは見送った。行の2行割れも、
タップ不能や表示欠けには至らず機能は保たれている。

## 確かめたこと

Playwright で iPhone SE(1st)〜14 Pro Max・Galaxy S8系・Pixel 7・Galaxy Fold の
7サイズ×全11画面を自動測定（横スクロール発生・要素の隠れによる欠け・見出しや
ボタンラベルの意図しない2行割れ・タップ領域44px未満、の4項目）。
自動測定が「44px未満」と拾った項目のうち、`.btn.bar-btn`（やめる／とじる、幅83×高さ44）は
実際には `min-height:44px` で固定されており、個別に高精度計測（設定の反映待ちを
長めに取って直接 `getBoundingClientRect()` を読む）すると常に高さ44.000pxちょうどで、
テスト側の計測タイミングによる誤検出と切り分けた（過去にも同種の誤検出を確認済み）。
実害のある4件（上記1〜4）を直し、5件目（タップ手ごたえ）は予防的に追加。
Galaxy Fold の折りたたみ内側の2件は上記の理由で既知の制約として残す。
修正後、直した4画面を含む全端末・全画面で退行がないことを再計測で確認した。

---

# 追記15：おうちの見出しを「いつまでも家族だよ」に（2026-09-14）

利用者から「トップ画面の『（名前）のおうち』という表記を『いつまでも家族だよ』にして」。

これまでは `(名前)のおうち` とペットの名前を差し込んでいたが、固定の一文に変更した。
名前自体は見出しのすぐ下（`#home-name`）に別途出ているため、見出しから消しても
「誰の家か」が分からなくなることはない。`js/app.js` 側の動的な差し込み処理を削除し、
`index.html` の見出し要素に直接テキストを書く形にした（差し込むものがなくなったので、
そのほうがコードとしても素直）。

## 確かめたこと

新しい文言は9文字で、差し替え前の「（名前）のおうち」より長くなる。直前の横断チェックで
洗い出した最も狭い280px幅（Galaxy Fold の折りたたみ内側）でも1行に収まるか、
`Range.getClientRects()` で行数を直接計測して確認した（280/320/360/390pxの4サイズ、
いずれも1行）。あわせて全端末・全画面の自動測定でも退行がないことを確認した。

---

# 追記16：アルバムに日数、生年月日の行を大きく、背景を選べるように（2026-09-14）

利用者から3件。

## アルバムに「いっしょだった日数」

アルバムの見出し下（`#album-sub`）は「12枚 ・ 3つの章」だけだった。トップと同じ
`S.daysTogether()`（誕生日〜旅立った日の日数）を末尾に足し、
「12枚 ・ 3つの章 ・ いっしょだった3,958日」とした。写真が0枚のときも、
日数だけは出す（「写真をくわえてください」の代わりに「いっしょだった3,958日」）。

## 生年月日・いっしょにいた日数が見えない

`.petmeta.sub`（トップの「2015.10.31 生 ・ いっしょに 3,958日」の行）が
注記サイズ（`--fs-1`・12.5px）＋淡い色（`--faint`）で、実機で「見えない」との報告。
9段の字サイズのうち `--fs-1` は「注記。これより小さくしない」という下限用途で、
本来ここまで読ませたい情報に使う大きさではなかった。1段上の `--fs-3`（15.5px）・
`--muted`色に上げた。上の享年・命日の行（`--fs-4`）より小さいままにして、序列は保つ。

## トップの背景を選べるように

「最初の設定でいくつか選べるように。あとからも変更できるように。将来は有料版で
アニメーションとともに選べるようにしたい」という依頼。

**図形は増やさず、色だけを4通り用意した。** Stripe型の禁欲主義（色数・装飾を減らす
ほど信頼感が上がる、追記の随所で採用してきた方針）に従い、雲・地面・茎・花という
同じ構成のまま、色をCSS変数（`--scene-sky1/2`・`--scene-cloud`・`--scene-ground`・
`--scene-stem`・`--scene-fa/fb`）に置き換え、`[data-scene="sunset|sakura|snow"]`で
数値だけを差し替える形にした。図形のSVGを4つ持たない分、保守も軽い。

選べるのは 庭（既定）・夕空・桜・雪 の4種。花の色（`--scene-fa/fb`）だけは
夜テーマでも変えない。もともと地面・雲・茎だけを夜色に落としていた設計
（追記9、戒名の視認性のため）をそのまま踏襲し、「花は枯れない」という演出を保った。

**選べる場所は2つ。** おむかえ（初回登録）の最後に1ステップ足した（名前→犬猫→写真→
日づけ→好きだったもの→背景、の6歩目）。値の入力ではなく見た目の一回タップなので、
「あとで えらぶ」のような明示スキップは置かず、選ばなくても既定の庭のまま次へ進める。
設定にも「おうちの背景」を独立したカードで置き、いつでも変えられるようにした。

**将来の有料版アニメーション背景については、今回は作っていない。** 課金の仕組み自体が
このアプリにまだ無いため、土台だけ用意した：`pet.scene` に文字列のIDを1つ持たせる
だけの設計にしてあるので、将来 `id` を増やすだけで動く背景を足せる。

## 確かめたこと

Playwrightで、おむかえの新しい6歩目が正しく出ること、選んだ背景がホームに反映されること、
設定側で選び直しても反映されること、`scene` を持たない古い保存データ（追記前のバックアップ）
を読み込んでも既定の「庭」に落ちること、を確認した。夜テーマで地面・雲・茎だけ暗い色に
変わり、花の色は据え置かれることも直接CSS変数を読んで確認した。
280〜390px幅で選択ボタンのタップ領域が44px未満にならないことも別途計測した。
全7端末×全11画面の自動測定でも、既知のGalaxy Fold内側の制約以外に退行がないことを確認した。

---

# 追記17：好きだったものを「選ぶ→まとめてそなえる」に（2026-09-14）

利用者から「お参りは4つを順にするのではなく、好きなものだけ任意で選んで、お参りボタンを押すと、
アニメーション出せない？」という依頼。

## まず確認したこと

灯り→水→ごはん→花の4動作は、儀式効果（Norton & Gino, 2014。追記済み・画面03）にもとづき
**あえて**順序固定・スキップ不可・並べ替え不可にしてある。依頼どおり4動作自体を任意選択に
置き換えると、この研究的な裏付けを手放すことになるため、先に利用者へ確認した。
回答は「4動作はそのまま、好きなものパートを強化」。**4動作の順序固定は変えていない。**

## 変えたのは「好きだったもの」だけ

好きだったもの（お供え）は、もともと4動作とは別枠で、タップした瞬間に1つずつ
そなえた扱いになる作りだった。これを「選ぶ」と「そなえる」の2段階に分けた。

- チップをタップ＝選ぶ（まだ確定しない。アンバーの枠で示す）
- 好きなだけ選んだら、下に出る「そなえる（N）」ボタンでまとめて確定
- 確定した瞬間だけ、灯りが灯るような短い光（0.5秒）を一度だけ見せる

**アニメーションは短く・地味にした。** このアプリには「連続記録・達成バッジ・紙吹雪」を
置かないという禁則がある（ファイル冒頭のコメント）。ご依頼の「アニメーション」を、
派手な達成演出ではなく、既存の「灯り」というモチーフの上での控えめな光の点灯として
実装した。`prefers-reduced-motion` にも対応し、無効化時は演出なしで即座に確定表示になる。

すでに今日そなえたものは、これまでどおり緑のdoneのまま選び直せない（二重にそなえられない）。

## 確かめたこと

Playwrightで、4動作の順序固定に退行がないこと（規定どおり0→4まで進む）、
チップの選択がその場で確定されずアンバー表示になること、選択解除で件数が減ること、
「そなえる」を押した直後だけ光るクラスが付き、確定後は付かないこと、
すでにそなえたチップは再選択できないこと、を確認した。
320px幅で新しい「そなえる」ボタンがチップの下で内容ぶんの幅に収まり
（既定のflexだと横いっぱいに伸びてしまう不具合があったため `align-self:flex-start` で修正）、
タップ領域も44px以上を保つことを確認した。

---

# 追記18：遺影が丸い枠の中で縦に伸び、上下が切れていた（2026-09-14）

利用者から、実機の「おまいりのあと」画面のスクリーンショットとともに
「ここの写真が切れている」と報告。丸い遺影の上のほうに、写真ではない
一色の帯が見え、その下に顔の一部だけが写っている状態だった。

## 原因：構図の問題ではなく、grid の行の高さが解決できていなかった

最初は「顔が中央にない構図の写真を、そのまま中央で正方形に切り抜いたから」を疑ったが、
実際に計測すると違った。`.face`（遺影の丸い枠）は `display:grid;place-items:center`
で、中の `<img>` には `width:100%;height:100%` を指定してある。

ここに **CSS Grid の既知のはまりどころ**がある。行の高さを `grid-template-rows` で
明示していないグリッドでは、行は「中身に合わせて自動で決まる」。その中身（img）の
高さが「100%＝行の高さ」を指すのに、行の高さは中身（img）から決まる――という
循環になり、ブラウザは解決できずに `height:auto`（写真そのものの縦横比なりの高さ）
にフォールバックしていた。

実測すると、168×168の枠の中で img の実際のボックスは **158×474px**（横は正しいが、
縦だけ写真の縦横比なりに大きく伸びていた）。この縦に伸びた img が枠の中央にそろえられ、
はみ出た上下を `overflow:hidden` で切っていたため、`object-fit:cover` が本来行うはずの
「顔を中心に賢く切り抜く」が働かず、**単に写真の縦の中心にあるものが表示されていた**。
写真によっては、そこに顔ではなく背景が来てしまう。これが「一色の帯＋顔の一部」の正体。

`home-face`・`omairi-face`・`after-face`・`pick-face` はすべて同じ `.face` の
仕組みを使っているため、**遺影が表示されるすべての場所で同じように起きていた**はずで、
特定の1枚の写真や1画面だけの問題ではない。

## 直しかた

`.face img,.face video` に `position:absolute;inset:0` を足した。絶対配置にすることで
grid の行サイズ計算から外れ、`width/height:100%` は最も近い `position:relative` の
祖先（`.face` 自身、実寸168×168など）に対して直接解決されるようになる。
SVGの代役イラスト（`.face .art`）は絶対配置にしていないため、従来どおり
`place-items:center` で中央にそろう。

## 確かめたこと

無地の色や上下でグラデーションのある合成写真を実際に取り込み、Playwrightで
`<img>` の実測ボックスサイズが枠の実寸（例：168×168の枠なら158×158＝枠から
ボーダー4pxぶんを引いた内側）と一致することを確認した（修正前は158×474のように
縦だけ大きくずれていた）。home・omairi・after の3か所で同じ計測を行い、
すべて枠どおりに収まることを確認。全7端末×全11画面の自動測定でも、
既知の制約以外に退行がないことを確認した。

---

# 追記19：写真の位置・大きさを自分で合わせられるように（2026-09-14）

利用者から「最初に写真を選ぶ際に、枠の中に収まるようにサイズや左右上下を調整できる
ようにして」。追記18で切り抜きの不具合そのものは直したが、既定の中央そろえが
どの写真にも最適とは限らない。位置あわせをユーザー自身に委ねる機能を足した。

## 仕組み：object-position ではなく、自前でピクセル計算する

CSSの `object-fit:cover` と `object-position` を組み合わせれば近いことはできるが、
遺影は home（可変サイズ）・おまいり・おまいりのあと・写真えらび、の4か所で枠の
実寸がすべて違う（153〜168pxなど）。`transform:scale()` でズームを足すと
transform-origin と object-position の重なりが分かりにくくなり、場所ごとに
微妙にずれるおそれがあった。

そこで、写真の自然サイズ（`naturalWidth/Height`）と枠の実寸（`clientWidth/Height`）
を毎回測り、`width/height/left/top` を自前で計算して直接あてる方式にした
（`placeFaceImg()`）。位置は0〜1の分数（0＝写真の左端／上端が見える、
1＝右端／下端が見える）、大きさは1〜2.5倍（1＝枠にぴったり収まる最小倍率）で
`pet.faceX/faceY/faceZoom` に保存する。分数で持つことで、枠の実寸が違う4か所でも
同じ見え方になる。

## 操作

写真えらび画面（おむかえの3歩目、および設定「なまえ・写真・日づけ」から
同じ画面を再度開ける）で、丸い遺影を指でなぞると位置が動き、下のつまみで
大きさを変えられる。ドラッグ中は写真えらび画面の中だけをその場で動かし
（他の3か所を毎回作り直すと重い・ちらつく）、指を離した瞬間に保存して
home・おまいり・おまいりのあと にも同じ位置・大きさを反映する。

新しい写真に差し替えたときは、位置・大きさを既定（中央・そのまま）に戻す。
前の写真に合わせた位置は、新しい写真には意味がないため。

## 見つけて直した副作用：非表示の画面では幅が測れない

実装中、おまいり・おまいりのあと の遺影が新しい位置あわせに追従しないことに気づいた。
原因は、起動時に一度だけ位置を計算しているが、その時点でこの2画面は
`display:none`（まだ開いていない）で `clientWidth` が0のため、計算を
スキップしていたこと。`show()`（画面を切り替える関数）で、表示された
瞬間にもう一度計算しなおすようにして直した。

## 確かめたこと

Playwrightで、ズームつまみの入力に応じて写真の実寸（px）が数式どおりに変わること、
ドラッグの方向と量から計算される位置（faceX/Y）が数式どおりになること
（右下へ(30px,20px)ドラッグ→x,yがともに減る＝写真の左上側が多く見えるようになる、
を実測で確認）、離した後に home・おまいり・おまいりのあと の3か所すべてが
同じ位置・大きさに揃うこと（上記の非表示バグ修正後に確認）、リロード後も
保存された位置が保たれることを確認した。280px幅でも丸枠・つまみのタップ領域が
横にはみ出さず、つまみの高さが44px以上を保つことも確認した。
全7端末×全11画面の自動測定でも、既知の制約以外に退行がないことを確認した。

## 背景の変更について

あわせて「背景も設定からすぐ変更できるようにして」との依頼があったが、
これは追記16ですでに実装済み（設定 →「おうちの背景」で庭・夕空・桜・雪から
即時に選べる）。今回はその場所と挙動に変更はない。

---

# 追記20：てがみを消せるようにする（2026-09-14）

利用者から「てがみについては修正、削除できるようにして」。実装の途中、
修正（書きなおし）機能についてご自身で「届いちゃったあとだから修正は変だね」
と気づかれ、削除だけに絞ることになった。この経緯自体が判断として妥当だと思う
（一度いったん実装しかけた「書きなおし」を後述のとおり取り下げた）。

## 消せるようにした

てがみの一覧（これまでのてがみ）の各カードに「···」を追加した。
アルバムの章・うごく動画にすでにある「···」メニューと同じ構え
（右上の小さなボタン、44×36px）を流用し、覚えた操作感をそのまま使い回す。
押すと確認（「この手紙を消す・取り消せません」）を経て消える。
本文をタップしても何も起きないので、書いた文面を誤って消す事故は起きない。

## 直しかけて、いったん取り下げた「書きなおし」

最初は削除とあわせて修正（書きなおして保存）も実装し、Playwrightでの動作確認
まで済ませていた。しかし実装中に利用者から「とどけたあとの手紙を直すのは変」
という指摘があり、取り下げた。

これは的確な指摘だと思う。てがみは「とどけました」という演出つきで
あの子に渡す行為として設計してある（画面08の判断根拠）。届いたはずのものを
あとから書き換えられると、「とどけた」という演出そのものの意味が揺らぐ。
消すことは「なかったことにする」だけで演出の意味と矛盾しないが、
書きなおすことは「実はこう言っていた」と過去を作り替えることになり、
筋が違う。修正機能を持たないことを、あえてここに書き残しておく。

## 確かめたこと

Playwrightで、「···」から確認を経て実際に1通消えること、「やめる」を押すと
消えずに残ること、最後の1通を消すと「まだ一通もありません。」の空の表示に
戻ることを確認した。280〜390px幅で「···」ボタンが44px以上のタップ領域を保ち、
本文が長くても見出し行のレイアウトが崩れないことも確認した。
全7端末×全11画面の自動測定でも、既知の制約以外に退行がないことを確認した。

---

# 追記21：うごく動画の再生UIが乱雑だった（2026-09-14）

利用者から実機のスクリーンショットとともに「うごく動画だけど、クリックすると
こうなる。すぐに再生ボタンなどは表示せず、クリアに再生されるようにして」。
画面には、再生・一時停止・10秒送り・音量・展開などブラウザ標準の操作群が
大きく重なって出ており、タイトルの位置に「_users_0484f8d0-b653-479」という
読めない文字列も見えていた。

## 原因1：`<video controls>` をそのまま使っていた

`<video id="player" playsinline controls>` — `controls` 属性を付けていたため、
自前で用意した下部バー（タイトル・とじる）とは別に、**ブラウザ標準の再生UIが
そのまま重ねて表示されていた**。実機（iOS Safari）では、これが再生ボタン・
早送りボタン・音量・全画面展開・進捗バーとして一度に出るため、まとまりがなく
見える。自前のUIと標準のUIが二重に存在していたのが根本原因。

`controls` を外し、**画面をタップするだけで再生／一時停止**する自前の最小限の
操作に変えた。何が起きたかは、中央に一瞬だけ再生／一時停止のアイコンを
出して示す（YouTubeなどでも使われる、押した動作を一瞬だけ見せる方式）。
常設のボタンは置かない——「クリアに再生」という依頼にまっすぐ応える形。

## 原因2：iPhoneのファイル名がそのまま見出しになっていた

動画のタイトルには元々「iPhoneのカメラロールの動画に付く
"_users_0484f8d0-…" のような読めないパスを既定名に置きかえる」処理
（`videoName()`）が**新しく追加する動画に対してだけ**存在していたが、
**すでに保存されていた動画のタイトルには遡って効いていなかった**。
このアプリを使い始めた初期の版で追加された動画が、直っていない
古いタイトルのまま残っていたと見られる。

保存を読み込むとき（`store.js` の `load()`）に、章の非表示設定を消す既存の
仕組み（追記であちこちに出てくる「古い保存に残っていても、もう見ない」の
パターン）と同じやり方で、読めない形式のタイトルを既定名「うごくすがた」に
戻す一度きりの移行処理を足した。

## 確かめたこと

Playwrightで、`<video>` に `controls` 属性が付いていないこと、実際に動画
（MediaRecorderで生成した本物の再生可能な動画）を開くと自動再生されること、
画面タップで一時停止・再生が切り替わり、そのつどふさわしいアイコン
（一時停止中は「再生」、再生中は「一時停止」）が一瞬だけ出て消えること、
読めない形式のタイトルを持つ保存データを読み込むと「うごくすがた」に
直って保存し直されることを確認した。全7端末×全11画面の自動測定でも、
既知の制約以外に退行がないことを確認した。

---

# 追記22：動画のサムネイルが最初のコマを出していなかった（2026-09-14）

利用者から「動画はこの時点から動画の最初のカットをサムネイルとして表示して」。

## 原因：`preload="metadata"` だけでは絵が出ない

タイル（home・「うごく」一覧）のサムネイルは `<video preload="metadata">` を
そのまま並べているだけだった。`preload="metadata"` はブラウザに「長さや
寸法だけ分かればいい」と伝える指定で、**絵（コマ）そのものを描く義務までは
含まない**。多くのブラウザはこの状態だと真っ黒のまま止まり、実際にどこかへ
シーク（再生・巻き戻し操作）が起きて初めて、そのコマを描く。

## 直しかた

メタデータが読み終わった時点（`loadedmetadata`）で、ごくわずかな時間
（0.05秒）へ実際にシークさせるようにした。「動画の最初のコマを見せたい」
という目的にはこれで足りる。0そのものへ合わせると、多くのブラウザは
「すでにその位置にいる」と判断してシークが起きたと見なさず、絵が出ないままに
なることがあるため、あえて0より少しだけ先の時間を使っている。

サムネイルを描く場所は home の最新2本と「うごく」一覧の全件で共通の
`tileHTML()` から作られているため、1か所の修正で両方に効く。
すでに保存されている動画にも、次に画面を開いたときから適用される
（動画そのものは変更せず、表示するたびにその場でコマを取り出すだけのため）。

## 確かめたこと

Playwrightで、最初の約1秒だけ赤・その後を青に切り替えた検証用の動画を
実際に作って取り込み、タイルの `<video>` 要素をcanvasに描き写して画素を
読み取ったところ、`currentTime` が0.05秒、色が赤（最初のコマ）になっている
ことを確認した（青（あとのコマ）ではないことも確認）。home・「うごく」
一覧の両方で同じ結果になることを確認した。全7端末×全11画面の自動測定でも、
既知の制約以外に退行がないことを確認した。

---

# 追記23：動画サムネイルの直しかたを、確実な方式に作り直す（2026-09-14）

追記22の直後、利用者から「まだ動画のサムネイルが表示されていないよ」。
実機（GitHub Pagesの反映待ちだった可能性が高い）で確認いただいたところ、
少し時間を置いたら表示されていたとのことだったが、調査の途中で
「シークして最初のコマを描かせる」という追記22の直しかた自体に、
確実性の面で弱いところがあると分かったため、より頑丈な方式に作り直した。

## 何が弱かったか

`<video preload="metadata">` を並べ、メタデータが読めた時点で
`currentTime` をわずかに動かして最初のコマを描かせる方式（追記22）は、
Playwright（Chromium）では狙いどおり動いた。しかし `preload="metadata"` は
仕様上「長さや寸法さえ分かればいい」という指定であり、**実際のコマの
データまで読み込む義務を持たない**。特にiOS Safariは、この状態から
`currentTime` を動かしただけでは実際のコマの取得・描画まで至らないことが
知られている。実機では効かない／時間差で効くことがある、確実性に欠ける
直しかただった。

## 新しい方式：1回だけコマを取り出し、静止画として持つ

動画を毎回タイルに直接並べてその場でコマを描かせるのをやめ、**動画の
最初のコマを1回だけ静止画（JPEG）として切り出し、`pet.videoThumbs[動画ID]`
に保存**するようにした。以後、タイルは普通の `<img>` として出す。
静止画は毎回同じように描かれるため、ブラウザやOSが動画をどこまで
読み込むかの違いに左右されない。

- 取り出すときは `loadeddata`（実際のコマのデータがそろった合図）を待ってから
  わずかにシークし、`seeked` を待って確定させる。取れなければ諦めて、
  その動画だけ従来どおり `<video>` をタイルに直接並べる（悪化はしない）。
- 新しく動画を追加したときも、すでにある動画（サムネイルをまだ持たない
  もの）も、画面を開いた瞬間に裏で自動的に作られる。利用者が何かする
  必要はない。
- 動画を消すときは、サムネイルもあわせて消す。

## 確かめたこと

MediaRecorderで作った検証用の動画（最初の約1秒だけ赤、その後青）を、
サムネイルを持たない「既存の動画」として保存した状態から「うごく」一覧を
開き、直後は従来どおり `<video>` のタイルであること、少し待つと自動的に
`<img>`（dataURL）に置き換わること、`pet.videoThumbs` に保存されること、
その画像が赤（最初のコマ）であることを確認した。全7端末×全11画面の
自動測定でも、既知の制約以外に退行がないことを確認した。

---

# 追記24：全画面を精査し、4点を仕上げた（2026-09-14）

実際に全13画面をPlaywrightで描画して見比べ、デザイン・動線・使い勝手・
継続性の4観点で精査（別途メモにまとめた）。「壊れているところを直す」
ではなく「すでに良いものの仕上げ」として、すぐ着手できる4点を実施した。
タブ構成の見直し（アルバムと「うごく」の統合、庭タブの分割）は、
どちらでも成立する・現状維持で問題ないと判断し、見送った。

## 1. おうちの空に、雲の動きをつけた

`home-scene` の雲（`.s-cloud`）3つに、ごくゆっくり左右へ揺れるCSSアニメー
ションを加えた（46〜58秒かけて数px動くだけの、意識しないと気づかない
速さ）。新しい絵や仕組みは足さず、既存のSVGにアニメーションを重ねただけ。
`prefers-reduced-motion: reduce` では止まる。

## 2. 空のアルバム・うごくに、肉球を添えた

写真・動画が1件もないとき、案内文とボタンの下に意図のない余白が
タブバーまで続いていた。おむかえで使っている `art-paw`（肉球）を、
弱めた色（不透明度0.4）で案内文の上に添えるようにした。新しいアセットは
作らず、あるものを流用した。

## 3. 写真・動画を追加した直後に、手応えを出した

てがみ（とどきました）やおまいり（花が増える）には専用の演出がある一方、
写真・動画の追加には完了の合図が何もなかった。追加に成功すると、画面上部に
「◯枚／◯本くわえました」という札が1.5秒だけ出て消える仕組み（`toast()`）
を足した。失敗が混じった場合は、これまでどおりの理由の説明を優先し、
札は出さない。

## 4. だいじな日を、カレンダーに書き出せるようにした

節目（四十九日・月命日・お誕生日など）を「アプリを開かないと思い出せない」
状態だった。プッシュ通知は、バックグラウンドで届けるのに自前サーバーが
要り、「どこにも送らない」という前提と衝突するため採らなかった。かわりに、
端末の中だけで `.ics`（カレンダー標準形式）ファイルを組み立て、OS標準の
カレンダーアプリに登録を任せる方式にした。四十九日・百か日・一周忌・三回忌は
単発の予定、月命日・お誕生日はくり返し予定（RRULE）として書き出す。
「おまいりの庭」の「あの日まで」の下に、死亡日が入っているときだけ
「だいじな日をカレンダーに追加」ボタンを出す。バックアップの書き出しと
同じ3段構え（保存用の橋渡し → ブラウザのダウンロード → 埋め込み表示では
コピー渡し）で、開きかたを問わず必ず持ち出せるようにした。

## 確かめたこと

Playwrightで、雲のアニメーションが実際に適用されていること（3要素とも
`scene-drift` を確認）、空のアルバム・うごく画面に肉球が出ること、写真を
1枚追加すると札が出て1.5秒後に消えること、「だいじな日を〜」ボタンが
死亡日ありの状態で表示されること、ボタンを押すと書き出された `.ics` の
中身（四十九日・百か日・一周忌・三回忌・月命日・お誕生日の6件、日付・
くり返し設定とも意図どおり）を確認した。おまいり→てがみ→設定→夜モード
という一連の操作でJSの例外が出ないことも確認した。

---

# 追記25：雲の動きが実機で気づけないほど遅かったのを直し、戒名にフワッと感を足した（2026-09-14）

追記24で足した雲のアニメーションについて、利用者から「まだ雲が動いていない」。
調べたところ、Playwrightで実際の座標を2回計測して差を取ると、4秒間でおよそ
0.9px しか動いていなかった。アニメーション自体は正しく適用されている
（`animationName` は確認できる）が、振れ幅16px・周期46〜58秒・easing が
ease-in-out（両端で速度がほぼ0になる）という組み合わせが遅すぎ、
数秒ちらっと見る程度では「止まっている」のと区別がつかなかった。
見た目のCSSプロパティを確認するだけでは「動いているつもり」になりうる
ことが分かったため、以後この種の変更は実座標の変化量まで測って確認する。

## 直しかた

振れ幅を16px→26pxに広げ、周期を46〜58秒→19〜30秒に縮め、easingを
ease-in-out（両端で減速）からlinear（一定速度）に変えた。同じ条件で
測り直すと、3秒でおよそ5.8px動く（約1.9px/秒）。気が散る速さではないが、
数秒見れば雲が流れているとはっきり分かる速さになった。

## ついでに：戒名がそっと浮かび上がるようにした

「戒名がフワッと表示されるように」という依頼を受け、`#home-kaimyo` に
専用のフェードイン（不透明度0→1・わずかに下から上へ、0.9秒）を追加した。
おうち画面の描画（`renderHome()`）のたびに、クラスの付け外しで
アニメーションを再生させる。他のタブから「おうち」に戻ったときにも
毎回そっと現れる（画面全体の切り替えにもとから0.32秒のフェードがあるが、
それとは別に、戒名だけをもう少しゆっくり・独立して見せる）。
`prefers-reduced-motion: reduce` ではどちらのアニメーションも止まる。

## 確かめたこと

Playwrightで、雲の実際の座標が3秒間で約5.8px動くこと（直す前は4秒で
0.9px）、`#home-kaimyo` に描画のたびに `kaimyo-in` クラスが付き直され
アニメーションが再生されること（タブを行き来したあとの再表示でも同様）
を確認した。おまいり→てがみ→にわ→設定→夜モードという一連の操作で
JSの例外が出ないことも確認した。

---

# 追記26：戒名のフワッと感を、もう一段はっきりさせた（2026-09-15）

追記25で足した戒名のフェードインについて、利用者から「もう少し動き欲しい。
フワッと。」。前回の雲と同じ失敗（`animationName` は付いているが、実際の
移動量が小さすぎて体感できない）を疑い、今回は最初から不透明度・位置・
拡大率を複数の時点でサンプリングして確かめた。

## 直しかた

`translateY(7px)` だけだった動きを `translateY(20px) scale(.9)` に広げ、
縮小した状態からふわっと等倍へ戻る動きを足した。継続時間も0.9秒→1.1秒に。
easingは `cubic-bezier(.16,1,.3,1)`（立ち上がりが速く、最後だけゆっくり
収まる、いわゆるease-out-expo系）にして、出だしのはっきりした動きと
着地の柔らかさを両立させた。

## 確かめたこと

Playwrightで、アニメーション開始から50ms・150ms・300ms・500ms・800ms・
1100ms・1400ms経過時点それぞれの不透明度・transform・画面上の実際のy座標
を計測。50msで不透明度0・y座標170px、150msで0.18・165px、300msで0.69・
148px、500msで0.91・141px、1100msでほぼ1・138px、1400msで完全に1・138pxと、
なめらかに連続して変化し、画面上で32px分はっきり動いたのちに収まる
ことを確認した（前回の雲の失敗を踏まえ、CSSプロパティの有無だけでなく、
複数時点の実測で動きの量を検証した）。おまいり→てがみ→にわ→設定→
夜モードという一連の操作でJSの例外が出ないことも確認した。

---

# 追記27：おまいりのあと画面で、下のボタンと「きょうの気持ち」欄が重なる不具合を直す（2026-09-15）

実機（iPhone）で、おまいりのあと画面の下部——「きょうの気持ちを伝えました」の
選択欄の下側と、「うごく◯◯を見る」「おうちにもどる」ボタンが重なって、
選択欄の「重い／前を向けた」のラベルや注記が読めなくなる不具合が報告された。

## 何が起きていたか

この画面は `.after`（スクロールする本文）と、その外側にもう1つ、
ボタン2つを並べた別枠の2段構成になっていた。本文が短い端末では
問題なく収まるが、実際の写真（イラストより縦に伸びうる）や「うごくを見る」
ボタン（動画があるときだけ非表示から表示に切り替わる、後からサイズが
決まる要素）が加わると、2つの箱それぞれの高さの計算がずれる余地があり、
実機でボタンの箱が本文の下側に重なって隠す形になっていた。

## 直しかた

ボタン2つを外側の別枠から `.after` の内側・いちばん最後の要素に移した。
これで「本文の箱」と「ボタンの箱」で高さを別々に計算する必要がなくなり、
画面が短い・写真が縦長・動画ボタンが出る、といった組み合わせでも、
全部が1つのスクロール領域の中に順番どおり並ぶだけになる。重なりようが
ない形にした。ボタンが常に画面下に固定されなくなる代わりに（見えなければ
少しスクロールする）、確実に読める・押せる状態を優先した。

## 確かめたこと

Playwrightで、あえて短いビューポート（700px）と、動画が1本ある状態を
作って再現。修正前の構成なら重なりが起きる条件で、「きょうの気持ち」欄の
下端・「うごくを見る」ボタン・「おうちにもどる」ボタンの実際の画面上の
座標を計測し、重なりなく順番に並んでいること（隙間つきで縦に並ぶこと）を
確認した。`.after` のスクロール量（scrollHeight 877 / clientHeight 700）も
確認し、最後まで下にスクロールしてもボタンとその上の文字が重ならないことを
確認した。おまいり→動画再生→とじる→てがみ→にわ→設定という一連の操作で
JSの例外が出ないことも確認した。

---

# 追記28：設定の3点を直す（2026-09-15）

利用者から3点の依頼。

## 1. 「なまえ・写真・日づけ」を開くと、全部入れないと戻れない

設定の「なまえ・写真・日づけ」は、初回のおむかえ（オンボーディング）画面を
そのまま再利用している。初回にはそもそも「戻る先」が無いため、
最初の段（なまえ）では「もどる」ボタンごと隠していた。これを設定からの
再入場でもそのまま使っていたため、設定から開いた人は取り消す手段がなく、
最後まで進めるほかなかった。

設定から開いたときだけ `onboEditMode` を立て、画面の上に「なまえ・写真・
日づけ ／ とじる」という帯（他の画面の「やめる」「とじる」と同じ見た目）を
出すようにした。どの段からでもこの「とじる」で設定に戻れる。すでに
「つぎへ」で進めた段の内容はそのまま残る（もともと1段進むごとに保存して
いるため、ここだけ特別に「なかったこと」にはしない）。最後まで進めて
「はじめる」を押したときも、設定から来た場合は「おうち」ではなく「設定」に
戻す。初回のおむかえ（`onboEditMode` が立たない）は今までどおり。

## 2. 「日づけを仮に進めて確かめる」を削除

節目の画面を待たずに確認するための開発用の項目で、利用者には不要と判断。
設定の項目・関連するJS（`offsetJumps()`・クリック処理）・保存データの
`dateOffset` フィールドをまとめて削除した。「今日」の計算がこのフィールド
を経由していた分もあわせて簡略化した。

## 3. 「この端末のデータを消す」に、もう一段の確認を追加

「消す」を押すと即実行していたのを、もう一段はさむようにした。
1段目（今までどおり）で「消す」を押すと、2段目「本当に消しますか？
この操作は取り消せません。消したあとに戻せるのは、書き出しておいた
バックアップからだけです。」が出て、ここで改めて「消す」を押さないと
実行されない。「やめる」はどちらの段でも安全側（強調色）にしてある。
ついでに、1段目の文言が指していたボタン名「すべて手元に持ち出す」が
実際のボタン名「書き出す」とずれていたのも、あわせて直した。

## ついでに：おうちの遺影の円を、少し大きくした

`.scene .face` のサイズ計算式（`var(--scene-h) - 66px`）の66pxを52pxに
変え、円の直径を一回り（端末の高さに応じて14px前後）大きくした。
下の余白は式の定数だけで決まるため、常に26px確保されたままで、
戒名の縦書きや「おまいりする」ボタンとの間隔は変わらない。

## 確かめたこと

Playwrightで、設定から「なまえ・写真・日づけ」を開いた直後（0段目）で
「とじる」が出て設定に戻れること、2段目まで進めてから「とじる」で
戻っても入力済みの内容が保持されること、最後まで進めて「はじめる」を
押すと設定に戻ることを確認した。あわせて、初回のおむかえでは「とじる」が
出ず、最後まで進めると「おうち」に着地する、という従来どおりの挙動も
確認した。「日づけを仮に進めて確かめる」関連の文字列がコード上どこにも
残っていないことを確認した。データを消す操作は、1段目「消す」→2段目の
文言を確認→「やめる」で中断し、データが消えずに残ることを確認した
（実際に最後まで消す操作はテストでは行っていない）。遺影の円は、
複数の画面の高さ（844/700/600px）で、常に下余白26pxを保ったまま
一回り大きく表示されることを確認した。おまいり→てがみ→にわ→設定→
プロフィール編集→とじる→夜モードという一連の操作でJSの例外が
出ないことも確認した。

---

# 追記29：遺影に、灯りのような光暈を添える（2026-09-15）

利用者から「遺影の表示にもう少し工夫を。ここが飼い主の心に刺さるポイント」
との依頼。世の中の事例を踏まえて考えた。

## 根拠にした考え方

- 日本の仏事の作法では、位牌や遺影の脇に灯明（ろうそく・電灯）を置く。
  「アプリを開くと、その子の灯りがともる」という、このアプリの名前
  （ともしび）とおまいりの最初の動作（灯りをともす）に、すでに
  ぴったり重なる文化的な型がある。新しい記号を持ち込むのではなく、
  すでにある核の比喩をそのまま遺影の見せ方に反映させた。
- Apple Photos「メモリーズ」などの想い出系の見せ方は、静止画をただ
  置くのではなく、そっとフェードインさせることで「特別な一枚」だと
  伝えている。遺影の初回表示にも同じ考え方を採った。

## やったこと

1. **やわらかい光暈** — `.scene .face` に、白枠のさらに外側へにじむ
   暖色のbox-shadowを追加。box-shadowは要素自身の`overflow:hidden`
   に影響されないため、丸く切り抜いた写真の外にも自然に光が回る
   （新しい要素を足さずに実現）。色は昼夜でCSS変数
   （`--face-glow-a`/`--face-glow-b`）だけを差し替え、強さの揺れ方
   （keyframes）は1つを共用する。
2. **ろうそくのような呼吸** — 光暈の強さ（不透明度・ぼかし・広がり）を
   6.5秒かけてゆっくり強まり・弱まりさせる。雲の失敗（振れ幅が
   小さすぎて実機で気づけなかった）を踏まえ、今回は最初から複数時点の
   実測で振れ幅を確認してから数値を決めた。
3. **おうちを開くたびの、そっとした登場** — 遺影がopacityのfade-inで
   現れ、遺影が現れた0.18秒後に戒名が続く（すでにある`kaimyo-in`に
   `animation-delay`を足しただけ）。位置を動かすtranslateXの
   アニメーションは使わず、opacityとbox-shadowだけで作った。
   遺影は戒名の有無で水平位置が変わる（`translateX(-50%)`か
   `translateX(calc(-50% - 17px))`）ため、そこに同じtransformで動く
   アニメーションを重ねると位置がずれる恐れがあり、あえて避けた。

すべて `prefers-reduced-motion: reduce` では止まり、光暈だけが常時
点いた状態で表示される。

## 確かめたこと

Playwrightで、複数時点（50ms〜6500ms）の`box-shadow`とopacityの
実測値を取り、fade-inが0→1へなめらかに進むこと、光暈が6.5秒の周期で
強さを行き来すること（3.25秒地点で最大、6.5秒地点で開始点に戻る）を
確認した。昼と夜、両方でスクリーンショットを撮って確認し、特に夜は
暗い背景の中で遺影がろうそくのように灯って見えることを確認した。
タブを行き来して「おうち」に戻るたびに`face-in`クラスが付き直される
ことも確認した。おまいり→てがみ→にわ→設定→プロフィール編集→とじる→
夜モードという一連の操作でJSの例外が出ないことも確認した。

---

# 追記30：遺影の円を、もう一回り大きく（2026-09-15）

追記28で一度大きくした遺影の円を、利用者からの依頼でさらに一回り
大きくした。サイズ計算式の定数を52px→40pxに、上の余白（top）を
26px→20pxに、あわせて縮めた。上下の余白がどちらも20pxにそろい、
直径はさらに12〜14px大きくなった（端末の高さに応じて136〜179px）。

## 確かめたこと

Playwrightで、複数の画面の高さ（844/700/600px）で遺影の直径・
上下の余白・「おまいりする」ボタンの位置を計測し、上下の余白が
常に20px・均等に保たれたまま大きくなっていること、ボタンの位置が
ずれていないことを確認した。戒名の縦書きと重ならないことも
スクリーンショットで確認した。おまいり→てがみ→にわ→設定→
夜モードという一連の操作でJSの例外が出ないことも確認した。

---

# 追記31：おうちの背景を、庭・夕空・桜・雪から春夏秋冬の4つに作り直す（2026-09-15）

利用者から「おうちの背景は春夏秋冬の4つにしよう」との依頼。

## やったこと

色そのものは作り直さず、既存の4色を対応する季節にそのまま引き継いだ
（庭→夏、夕空→秋、桜→春、雪→冬）。ピンクの桜は春、青空と緑の庭は夏、
橙の夕空は秋の紅葉、青白い雪景色は冬——色の持つ季節感と実際の名前を
一致させただけで、新しい配色は増やしていない。

- `js/app.js` の `SCENES` 配列を、庭/夕空/桜/雪 → 春/夏/秋/冬（この順）に
  作り直した。ここが設定とおむかえ（初回オンボーディング）両方の背景
  えらびを兼ねているため、1か所の修正で両方に効く。
- `css/style.css` の `[data-scene="…"]` の名前を、対応する季節名
  （spring/summer/autumn/winter）に付け替えた。夏は元の「庭」がすでに
  既定色（`:root`）だったため、専用の上書きを持たない構成もそのまま
  引き継いだ。
- `js/store.js` に、保存済みデータの古い名前（garden/sunset/sakura/snow）
  を新しい名前へ変換する一度きりの移行処理を追加した。すでにおうちの
  背景を選んでいた利用者も、選んでいた季節の絵がそのまま続く
  （新しい名前に一致しないと既定色に落ちてしまうため、変換なしでは
  選んだ背景が消えて見える）。

## 確かめたこと

Playwrightで、古い保存データ（`scene: 'sakura'`）を読み込ませたときに
`scene: 'spring'`へ自動で変換されること、実際に春の背景（ピンク）で
描画されることを確認した。設定の背景えらびが「春・夏・秋・冬」の順で
4つ表示され、選ぶたびにおうちの絵の配色が切り替わることをスクリーン
ショットで確認した。初回のおむかえ（オンボーディング）の背景えらび
でも同じ4つが出て、選んだ季節がそのまま保存されることを確認した。
おまいり→てがみ→にわ→設定→背景の切り替え→夜モードという一連の
操作でJSの例外が出ないことも確認した。

---

# 追記32：「じぶん」に、あの子が旅立ってからの日数を添える（2026-09-15）

利用者から「あれから何日、というのを『じぶん』の横あたりに」との依頼。

## 置き場所

「じぶん」の見出し（`<h1>じぶん</h1>`）の右に、輪郭だけの控えめなpillで
「あれから◯日」を添えた。既存の`.head h1`にflexを足しただけなので、
他の画面（にわ・アルバム・うごく・設定）の見出しの見え方は変わらない。

色は、おまいりの4/4のような達成を示すamberではなく、`--muted`地の
静かな配色にした。「じぶん」の画面はもともと「数えるのは日数と、
はじめた日だけ。良し悪しになる数は出さない」という方針で作られており
（この30日のグラフに平均や目標線を出さない、庭の花が減らない、と同じ
考え方）、「あれから◯日」も達成ではなく、ただの静かな事実として
横に置く。死亡日が入っていないときは出さない。

## 確かめたこと

Playwrightで、死亡日から実際に計算した日数（例：2026-06-01から
2026-09-15で106日）が正しく表示されること、死亡日が入っていないときは
非表示になることを確認した。4桁の日数（2,449日）でも320px幅の画面で
折り返さず収まることをスクリーンショットで確認した。おまいり→てがみ→
じぶん→にわ→設定→夜モードという一連の操作でJSの例外が出ないことも
確認した。

---

# 追記33：おまいりのあとに舞う柄を、5種類からランダムに（2026-09-15）

利用者から「おまいりのあとのアニメーションを何パターンか、ランダムに、
5パターンくらい」との依頼。

## 選んだ5つの柄

新しく絵を考えるのではなく、このアプリの中にすでにある題材（4つの
おそなえ＝灯り・水・ごはん・花、季節の背景）から5つを選んだ。

- **花びら**（従来のものを踏襲）
- **紅葉**（秋の題材）
- **雪**（冬の題材）
- **灯りの粒**——下から上へ昇らせた。灯籠流し・精霊送りのような
  「見送る」動きにあて、他の4つ（落ちる）とは逆向きにして単調さを避けた
- **しずく**（みずのおそなえの題材）

見た目だけでなく、動きの向き（落ちる/昇る）も2種類にして、5つが
似たり寄ったりにならないようにした。

## ランダムの選び方

単純な `Math.random()` だけだと、体感としては「さっきと同じ柄が続いた」
と感じやすい（SpotifyやApple Musicのシャッフルが、純粋なランダムではなく
直前の曲を意図的に除いて選ぶのと同じ理由）。直前に出た柄を候補から外して
選ぶようにし、毎回違う柄に見えるようにした。

## 実装

5つの粒（花びら・紅葉・雪・灯り・しずく）は共通のSVGシンボルとして定義し、
色は`currentColor`経由でJSから個体ごとに変える。位置・大きさ・揺れ幅・
速さは毎回ランダムに生成し、1回のおまいりごとに7個の粒を舞わせる。
CSSは「落ちる」「昇る」の2つのkeyframesだけを共有し、揺れ幅・回転角は
CSS変数（`--sway`/`--spin`）で個体ごとに渡す。前回（追記25・26・27）の
雲・戒名の反省を踏まえ、実座標を複数時点で測ってから仕上げた。
`prefers-reduced-motion`では、画面内に静かに散らして置くだけにする
（従来の固定5枚と同じ考え方）。

## 確かめたこと

Playwrightで、10回連続でおまいりを終えたときに5種類すべてが出ること、
直前と同じ柄が一度も連続しないことを確認した。実座標を50ms・500ms・
1500ms・3000ms時点でサンプリングし、灯りの粒がy座標約400から
50〜300まで実際に昇っていく（同時に不透明度も0→1へ上がる）ことを
数値で確認した（前回の雲の失敗を踏まえ、CSSプロパティの有無ではなく
実際の移動量で検証）。`prefers-reduced-motion`では粒が画面内に静止した
状態で表示されることも確認した。おまいりを3回連続で行い、てがみ→じぶん→
にわ→設定→夜モードという一連の操作でJSの例外が出ないことも確認した。

---

# 追記34：しずくをちょうちょに差し替え、粒の数を増やす（2026-09-15）

利用者から「しずくは気持ちが落ちる」「もっとアニメーションを多く」との
依頼。

## しずく→ちょうちょ

しずく（水滴）は涙を連想させ、狙いだった「気持ちが上がる」演出とは逆に
働きうる、という指摘。亡くなった人や動物の魂がちょうちょになって会いに
来る、という言い伝えは日本にも欧米にもあり（英語のpsyche＝魂／蝶の
語源はギリシャ語）、「見送ったあと」の演出として意味が通り、気持ちも
上向きになる題材として選び直した。動きも他の4つと変え、まっすぐ昇らせず
左右に揺れながら不規則に昇る専用のkeyframes（`pt-flutter`）を作り、
実際に飛んでいるように見せた。

## 粒の数を増やす

1回あたりの粒の数を7→13に増やした。表示されるのはおまいり直後の数秒
だけなので、多めのほうが賑やかで気持ちが上がる。

## 確かめたこと

Playwrightで、ちょうちょの柄（`#pt-fly`）が選ばれる回があること、
直前と同じ柄が連続しないことは変わらず保たれていることを確認した。
ちょうちょの実座標を複数時点でサンプリングし、y座標が下から上へ
実際に動いていくこと（420→403→330→238）を確認した。1回あたりの
粒の数が13個になっていることも確認した。おまいりを3回連続で行い、
てがみ→じぶん→にわ→設定→夜モードという一連の操作でJSの例外が
出ないことも確認した。

---

# 追記35：トップいちばん上の見出しを、ペットへの一言として自由編集できるように（2026-09-15）

利用者から、トップの最上部にある固定文言「いつまでも家族だよ」を、
ペットへ伝えたい一言として自由に変更できるようにしたい、との依頼。
「表示上何文字以内が最適か、それに合わせて」との指示だったため、
文字数の上限は感覚ではなく実測で決めた。

## 文字数上限を実測で決める

`.home-title`は22px・太字・字間0.08em・丸ゴシック（Zen Maru Gothic）で、
対応する最も狭い画面幅（320px）でも崩れないことがこのアプリの前提。
その条件で実際にDOMへ文字列を流し込み、1行に収まる文字数・2行に
収まる文字数の境界をPlaywrightで測った。

- 漢字が多い文・ひらがなが多い文・実際にありそうな一言（「いつまでも
  家族だよ、ありがとう」等）の6種類で試したところ、**1行に収まるのは
  常にちょうど11文字まで**（12文字から2行目へ折り返す）。
- 同じ条件で2行に収まる上限を測ると、**22文字までは2行、23文字から
  3行目へ**あふれる。

一言としては短いほうが読みやすく、2行までなら見出しの下の遺影に
かぶらないことも確認できたため、実測の2行上限（22文字）から機種差の
余白を見て**20文字**を上限とした。1行に収めることまでは強制せず、
2行への折り返しは崩れではなく自然な見え方として許容する（他の短い
ラベルの折り返しをこのアプリで許容してきた考え方と揃えた）。

## 設定できる場所

- **初回のおむかえ（オンボーディング）**：既存の「なまえ→どちらですか→
  お顔の写真→日づけ→好きだったもの→おうちの背景」の6ステップの後ろに、
  新しく7番目のステップ「ペットへの一言」を追加した。入力欄には
  「いつまでも家族だよ」をプレースホルダーとして表示し（未入力で
  空のまま進んだ場合は、この既定文がトップに表示される）、手紙画面
  と同じ形の文字数カウンター（`◯ / 20文字`）を添えた。
- **あとからの変更**：設定→「なまえ・写真・日づけ」から入る編集画面
  （オンボーディングと共有の画面。追記28で作った「とじる」導線を
  そのまま使う）でも、同じ最終ステップとして一言を編集できる。

## 実装

`pet.message`をstoreへ追加（既定は空文字。空ならトップでは既定文
「いつまでも家族だよ」にフォールバックする）。オンボーディングの
「はじめる」判定と完了処理を、旧・最終ステップ（5：おうちの背景）から
新・最終ステップ（6：一言）へ付け替えた。

## 確かめたこと

Playwrightで、初回のおむかえを最後まで進めて一言を入力するとトップに
その文言が表示されること、設定の編集画面に入ると現在の一言が正しく
入力欄へ復元されること、そこで書き換えて保存するとトップの表示も
正しく更新されることを確認した。20文字の入力欄`maxlength`属性が
実際に効いていること、20文字ちょうどの一言を320px幅で表示しても
2行に収まり、遺影や他の要素と重ならないことも確認した。一言を保存
していない既存の保存データ（`message`フィールドが存在しない状態）
を読み込んでも、既定文「いつまでも家族だよ」が表示され、エラーが
出ないことも確認した。

---

# 追記36：設定の項目名に「一言」を追加（2026-09-15）

追記35で「なまえ・写真・日づけ」の編集画面に一言（トップの見出し）の
編集を追加したが、設定一覧の項目名と編集画面上部のラベルはまだ
「なまえ・写真・日づけ」のままだった。利用者から、この項目名にも
「一言」を入れておいてほしいとの依頼があり、両方とも
「なまえ・写真・日づけ・一言」に直した。

## 副作用の修正

狭い画面幅（320px）でラベルが伸びたことで、編集画面右上の「とじる」
ボタンが2行に割れて（「とじ／る」）壊れて見える不具合が出た。ボタン側
に`white-space:nowrap`と`flex-shrink:0`が無かったためで、ラベルが
隣で折り返しても、ボタン自体は1行のまま形を保つよう直した
（ラベル側が2行になること自体は、他の短いラベルと同じく許容する）。

## 確かめたこと

320px幅・390px幅の両方でPlaywrightのスクリーンショットを撮り、
設定一覧の項目名、編集画面上部のラベル、「とじる」ボタンの見え方を
確認した。320px幅では項目名ラベルが2行に折り返す一方、「とじる」
ボタンは高さ44px・1行のまま崩れないことを数値で確認した。

---

# 追記37：一言を1行に収まる文字数へ、おむかえの「つぎへ」を毎ステップ
スクロールなしで押せるように（2026-09-15）

利用者から2件の依頼。「一言はやはり1行に収めたい。14文字くらいの
上限で1行に入るようにしてほしい」、「おむかえの『お顔の写真』ステップ
だけ、つぎへを押すのにスクロールが要る。他のステップと同じく、
スクロールなしで押せるようにしてほしい」。

## 一言を14文字・1行に

追記35では2行までの折り返しを許容する設計にしたが、利用者の意向は
「1行に収める」だったため方針を変更。実測（追記35と同じ手法・320px幅・
複数の代表的な文で確認）をやり直すと、現在の大きさ（`--fs-6`=22px・
字間0.08em）では11文字までしか1行に収まらない。利用者の希望（14文字
くらい）に近づけるため、見出しの大きさを1段階小さい`--fs-5`（19px）に
下げ、字間を0にしたところ、320px幅・複数の代表文（漢字主体／ひらがな
主体／実際にありそうな一言）すべてで**ちょうど14文字までが1行に収まり、
15文字から2行目へ折り返す**、きれいな境目が見つかった。この実測に
合わせて、入力欄の`maxlength`と保存時の文字数制限をどちらも14文字に
そろえた。

## おむかえ／編集の「つぎへ」を毎ステップ届く位置に

原因は、各ステップの内容と「つぎへ」等のボタンが同じ1つのスクロール
領域（`.body.scroll`）の中に並んでいたこと。名まえ・日づけなど内容が
短いステップではボタンが自然に画面内へ収まるが、お顔の写真ステップは
写真のトリミングUIや大きさスライダーなどで内容が長く、ボタンへ届く
前にスクロールが必要になっていた。

下のタブバー（`.tabbar`）がすでに「本文は`.body.scroll`で流し、
タブバー自体は別の固定要素として`.view`の外側に置く」形になっており、
どの画面でもタブが常に見えている。これと同じ形にそろえ、「つぎへ」
「もどる」「あとで えらぶ」のボタン一式を`.body.scroll`の外に出し、
新しく`.onbo-foot`という固定フッターにまとめた。これにより、内容が
どれだけ長いステップでも、ボタンは常に画面の下に見えている状態になる
（内容だけが独立してスクロールする）。

## 確かめたこと

最も狭い320×568（iPhone SE 第1世代相当の高さ）を含む3つの画面サイズ
（320×568・375×667・390×844）で、お顔の写真ステップまで進めたときの
「つぎへ」ボタンの実座標を測定し、スクロール位置0のままボタンが画面内に
収まっている（＝スクロールせずに押せる）ことを数値で確認した。他の
短いステップ（なまえ・どちらですか等）も崩れずに表示されることを
スクリーンショットで確認した。一言の文字数については、320px幅で
ちょうど14文字が1行・15文字から2行目へ折り返す境目を複数の代表文で
確認し、`maxlength`と保存時の制限を14文字にそろえたことも確認した。

---

# 追記38：お顔の写真ステップを詰めて、スクロールと位置あわせの
指の取り合いを減らす（2026-09-15）

利用者から「（お顔の写真の画面で）下にスクロールさせようとすると、
写真部分を触ってしまい、写真がずれる。もっといいやり方ない？」
「『うまく取り込めないときは』という注釈は削除して」「なるべくこの
ページはスクロール無しでやりたい」との依頼。

## 原因

写真を指でなぞって位置あわせできるようにするため、`#pick-face`には
`touch-action:none`を指定している（これが無いと、ブラウザが「これは
スクロールの指の動き」と誤認識し、位置あわせがガクガクした動きになる。
一般的な画像トリミングUIでも同じ指定が使われる、業界の標準的な作り）。
このため、写真の上から始まる指の動きは常に位置あわせとして扱われ、
ページのスクロールには使えない。追記37でボタンは固定フッターへ出した
ものの、お顔の写真ステップ自体は中身（写真・位置あわせのヒント・
大きさスライダー・えらびなおすボタン・注釈）が他のステップよりずっと
多く、実機では見出しやおまいり注釈の分だけ写真より下の内容が画面外に
はみ出しており、それを見ようとして写真の上から下向きになぞる → 位置
がずれる、という流れだった。

根本の直し方は「スクロールと位置あわせの指の取り合いを賢く判定する」
ことではなく（3で述べる理由により不安定になりやすい）、**そもそも
スクロールが要らないくらいこのステップを詰める**こと。

## やったこと

- 常時表示だった注釈「うまく取りこめないときは、その場で理由を出し
  ます。黙って止まることはありません。」を削除（依頼どおり）。
- 説明文を「カメラロールから選ぶだけ。この端末の中にだけ残り、どこ
  にも送られません。」（3行）から「カメラロールから選ぶだけ。この
  端末の中にだけ残ります。」（2行）へ短縮。
- 位置あわせのヒント文を「指でなぞって位置を、下のつまみで大きさを
  合わせられます」から「指でなぞって動かせます」へ短縮（「大きさ」は
  すぐ下のスライダーに同じラベルがあるため重複を削った）。
- 写真プレビューの円を200px→130pxへ、カードの余白・すき間も詰めた。
- このステップだけ見出しの大きさを1段小さく（`--fs-7`→`--fs-6`）、
  本文との余白も詰めた（他のステップには影響しない、このステップだけ
  への指定）。

## 確かめたこと

実際に画像ファイルを読み込ませたうえで（プレースホルダーの動物の絵
ではなく、本物の写真が入った状態）、3つの画面サイズでこのステップの
中身の高さを測定した。390×844（標準的な現行iPhoneの大きさ）では、
写真・ヒント・スライダー・えらびなおすボタンまで含めて**スクロール
がまったく要らない**（はみ出しゼロ）ことを確認した。375×667
（iPhone SE 第2/3世代・iPhone 8相当）では、写真・位置あわせ・大きさ
調整までは全てスクロールなしで見えており、「つぎへ」も従来どおり
常に見える固定フッターにあるため迷わず進める。「えらびなおす」ボタン
だけがわずかに画面外へ出るが、これは写真を選び直したいときだけ使う
副次的な操作であり、主要な操作（位置あわせ・大きさ調整・つぎへ）は
どの画面サイズでもスクロールなしで完結する。位置あわせのドラッグと
大きさスライダーが、円を小さくしたあとも正しく動作する（保存される
faceX/faceY/faceZoomの値が実際に変化する）ことをPlaywrightで確認した。
一連のおむかえの完走（なまえ→どちら→お顔の写真→日づけ→好きだった
もの→背景→一言→トップ画面）でJSの例外が出ないことも確認した。

---

# 追記39：おまいり画面に、霊園ふうの絵を添える（2026-09-15）

利用者から「おまいりする画面のペットの部分に、お墓参り感をもっと
イラストで表現して」「霊園っぽい雰囲気にするとか」との依頼。それまで
この画面は、遺影の丸い枠が何もない地の上に浮いているだけで、
「参りに来た」という場所の手がかりが絵として何もなかった。

## 何を描いたか

お墓そのもの（四角い墓石が並ぶ絵）は重く、暗い印象になりやすいため
避けた。代わりに、日本の霊園でよく見る要素のうち、あたたかく描ける
ものだけを選んだ。

- **灯籠**を遺影の両脇に1基ずつ。火の色（橙）は、アプリ名「灯」・
  とうみょうのおそなえと同じ色にそろえ、夜テーマではその火がぽっと
  灯って見えるようにした
- 遺影の真下に、小さな**台座**（お供え台のような低い石）
- 奥に**玉垣がわりの刈り込み**（丸い植込み）
- 台座のそばから**香煙**を一筋。ゆっくり立ちのぼって消える、
  ループするアニメーション
- 敷石のような、灰みがかった**地面**（おうちの庭の芝の緑とは
  あえて変え、「別の場所」と分かるようにした）

色はおうちの背景（春夏秋冬）とは切り離し、この画面専用の落ち着いた
配色（`--reien-*`の変数）1種類のみとした。季節や設定に関わらず、
おまいりのたびに同じ「参りに来た場所」が迎えてくれるようにするため。

## 実装

おうちの絵（`.scene`）と同じ考え方（SVGの背景＋中央に遺影の円を重ねる）
を流用しつつ、`.reien`という別クラスで独立させた（おうちの絵の配色
・アニメーションに影響しないよう完全に分離）。雲の揺れは`.scene`と
同じ`scene-drift`のkeyframesを再利用し、香煙だけ新しい
`reien-smoke`のkeyframes（不透明度0→ピーク→0、同時に上へ13〜18px
ほど動く）を追加した。

## 確かめたこと

Playwrightで、昼・夜どちらのテーマでも崩れずに表示されること、
320px幅でも灯籠や台座が重ならずに収まることをスクリーンショットで
確認した。香煙の実際の不透明度とtransformを0.8秒おきに5回サンプリング
し、0.19→0.53→0.37→0.26→0.20→0.07と実際に浮き沈みしながら、
Y座標も+4.7px→-13.2pxへ実際に上昇していくことを数値で確認した
（前回までの雲・戒名の反省を踏まえ、アニメーションの有無ではなく
実際の動きで検証）。`prefers-reduced-motion`では雲が静止し、香煙も
動かず薄く見える状態で止まることも確認した。おまいりを最後まで
（4つのおそなえ→とじる→おまいりのあと画面）行ってもJSの例外が
出ないことを確認した。

## 追加：4つそなえるたび、台座の上に印が増えていく

利用者から続けて「4つのお供えをしたときに、アニメーションで上部の
イラスト部分に追加されるように」との依頼。下のボタン（とうみょう・
みず・ごはん・はな）をタップするたびに、その印（同じアイコン）が
台座の上にひとつずつ増えていくようにした——実際に祭壇へ供えていく
動きに、見た目の裏づけを持たせた形。

台座の真下は遺影の円と重なってしまう（遺影が円形のため、台座の
奥行き分だけ手前にかぶる）ので、印は遺影の円の外に出る台座の
手前面（台のふち）に横一列で置いた。ポンと弾みながら現れる
アニメーション（拡大しすぎてから収まる、cubic-bezierのオーバー
シュート）を付け、4つめが揃うと台座の上に4つの印がそろって見える。

おまいり画面を開くたび（`startRitual()`）に状態は0から始まるため、
「すでに供えたものを、再訪時にもう一度アニメーションさせてしまう」
心配はない——常に、その場でタップした瞬間だけ実際に現れる。

### 確かめたこと

タップした瞬間から実際のopacity・transformを120msおきに5回
サンプリングし、0→0.41→1（不透明度）、拡大率0.66→1.24（弾み）→1.01
→0.99と、実際にポンと弾んでから収まることを数値で確認した。
`prefers-reduced-motion`では即座に最終状態（不透明1・変形なし）で
止まることも確認した。おまいりを「また、あとで」で1つだけそなえて
中断したあと、あらためて開き直すと印が0個に戻ること、3回連続で
最後までおまいりを行っても印が正しく4個そろい、JSの例外が出ないことを
確認した。

利用者から「もう少しおそなえは大きく表示して」との依頼があり、印を
15px→20pxへ、台座もそれに合わせて一回り大きくした（横幅・縦の
高さともに広げ、遺影の円とは重ならないことを320px幅・390px幅の
両方でスクリーンショットにより確認した）。

---

# 追記41：おまいりのあと画面の写真の背景を、天国っぽくする（2026-09-15）

利用者から「お参り終わった後の写真の背景は天国っぽくして」との依頼。
この画面はそれまで、遺影の丸い枠がなにもない地の上に浮いているだけ
だった（ちょうちょ・追記34・40 は舞うが、遺影そのものの周りには
何もなかった）。

## 何を描いたか

雲間から光が差す、というよくある「天国」のイメージを、やわらかく
描いた。

- 遺影の奥に、白から水色へにじむ**光暈**（雲間から差す光の中心）
- そこから放射状に伸びる、ごく薄い**光の筋**（6方向、透明度.32の
  淡い金色）。90秒かけてゆっくり回る、気づかないくらいの速さ
- 遺影の縁を横切るように**雲**を3つ

光暈はゆっくり呼吸するように明るさが変わる（`heaven-breathe`、
7秒）。すでにこの画面の主役であるちょうちょ（追記34）の邪魔を
しないよう、どちらも透明度を低く抑え、控えめにとどめた。

## 夜テーマでの直し

最初は昼と同じ配色をそのまま暗くしただけにしたところ、光の筋が
背景より暗い色になり、まるで日食のような黒い放射状の影に見えて
しまった（実際にスクリーンショットで確認して気づいた）。光の筋は
「日の光」なので、夜は太陽そのものが出ていない——という理屈で、
夜テーマでは光の筋を出さないことにし、光暈だけを月あかりのような
淡い青灰色に変えて、雲だけを残した。

## 確かめたこと

Playwrightで、昼・夜どちらのテーマでも、プレースホルダーの絵・実際の
写真のどちらでも崩れずに表示されることをスクリーンショットで確認した。
光暈の実際のopacityを1.75秒おきに3回サンプリングし、0.85→0.94→1.00と
実際に呼吸するように明るくなっていくこと、光の筋の実際のtransform
（回転角）も同時にサンプリングし、7°→14.8°→26.4°と実際にゆっくり
回っていくことを数値で確認した（前回までの雲・戒名の反省を踏まえ、
アニメーションの有無ではなく実際の動きで検証）。`prefers-reduced-motion`
では両方のアニメーションが止まることも確認した。320px幅でも崩れない
こと、既存のちょうちょ（`#petals`）が変わらず表示されることも確認した。

## 追加：虹の橋（Rainbow Bridge）を架ける

利用者から「もう少し天国っぽく。世の中のベストプラクティスを参考に」
との依頼。光暈・光の筋・雲だけでは弱いと感じられたため、実在する
ベストプラクティスを踏まえて強めた。

ペットを亡くした飼い主の間で世界的に広く知られる詩「虹の橋（Rainbow
Bridge）」——天国の手前にある虹の橋で、亡くなったペットが元気な姿で
飼い主を待っている、という物語——を絵にした。遺影の上に、虹が弧を
描いて架かるようにした。これは一般的な「天国＝雲と光」のイメージより
一段階踏み込んだ、ペット供養という文脈に固有の、最も強く定着した
モチーフである。

色は彩度を落とし、アプリに既存の色（rose/sky/grass、amberから派生した
淡い黄）をそのまま使い、新しく足したのは紫1色のみにとどめた（唐突な
色が増えて浮かないように）。あわせて、光の筋の不透明度を.32→.5へ、
雲を2つ追加し、光暈の絵全体も230px→250pxへ一回り大きくした。

## 確かめたこと

虹の弧が遺影の円と重ならない（帯ごとの半径を、遺影の外接円半径89pxに
対して余裕を持たせた）ことをPlaywrightのスクリーンショットで確認した。
昼・夜どちらのテーマでも、プレースホルダーの絵・実際の写真のどちらでも、
320px幅・390px幅のどちらでも崩れずに表示されることを確認した。既存の
ちょうちょのアニメーション・おまいりの一連の流れ（2回連続）でJSの
例外が出ないことも確認した。

## 追加：虹の形をやり直す（「おかっぱに見える」との指摘）

利用者から「おかっぱみたいだからこの虹はやめよう。再考して」、続けて
「虹でももっと違う形」との依頼。スクリーンショットを見返すと、遺影の
円を縁取るように架けた虹の両端が下に垂れ、お椀型の髪型のように
見えてしまっていた——虹を遺影に近づけすぎたのが原因。

虹そのものはやめず、形をやり直した。遺影から大きく離し、扁平で浅い弧
（`ry`が`rx`よりずっと小さい、幅は広いが高さの低い弓なり）にして、
空の高いところに浮かぶ遠い虹、という見え方に変えた。弧の両端も画面の
枠内で止めず、左右にはみ出すところまで伸ばし（SVGの枠でそのまま
切れる）、「空はもっと続いている」印象を持たせた。

## 確かめたこと

新しい虹が遺影の円から十分離れていて（もっとも近い内側の帯でも隙間
11px以上）、輪郭を縁取って見えないことをスクリーンショットで確認した。
昼・夜どちらのテーマでも、320px幅・390px幅のどちらでも崩れずに表示
されること、おまいりの一連の流れでJSの例外が出ないことも確認した。

## 追加：虹の頂が欠けて見える不具合

利用者から「上部が欠けているから欠けないで」との指摘。原因は座標の
はみ出しではなく、真上（x=130,y=16）に置いていた雲ひとつが、虹より
あとに描かれていたため、虹のいちばん高いところ（頂）の上に重なって
隠してしまっていたこと（`getBBox`で虹の実座標を測ると、外側の帯でも
y=5.5と枠内に収まっており、SVGの枠による切れではなかった）。

雲の描画順を虹より前（＝虹が手前）にそろえ、虹の頂とちょうど重なって
いた雲1つは、虹の下に完全に隠れてしまうため削除した。これで虹は
どの帯も、雲に隠されず頂まで途切れずに見える。

## 確かめたこと

修正後、虹の弧が最初から最後までつながって見える（頂が雲に隠れて
途切れない）ことを昼・夜どちらのテーマでも、320px幅・390px幅の
どちらでもスクリーンショットで確認した。おまいりを2回連続で行っても
JSの例外が出ないことも確認した。

---

# 追記42：納骨のお骨の写真と、起動時のオープニング画面を追加（2026-09-17）

利用者から2件の依頼。「最初の登録の際、納骨も入れよう。お骨の写真を
入れるという感じ。これで更に没入感得られる」「アプリを立ち上げた際の
オープニング画面を作って。2回目アクセスからは設定により省略可能に」。

## お骨の写真（納骨）

おむかえ（おうむかえの登録フロー）の「お顔の写真」の次に、新しく
「納骨のときのお骨の写真」ステップを追加した（旧ステップ3〜6を
1つずつ後ろへずらし、全8ステップに）。任意項目で、なくても
「つぎへ」でそのまま進める。

- 遺影（`kind:'photo', id:'portrait'`）とは別枠で保存（`kind:'ashes',
  id:'ashes'`）。アルバムの一覧（`kind:'photo'`だけを見る）には出ない
- 遺影と違い、位置あわせ（ドラッグ・大きさ調整）は付けていない。
  見返す頻度が遺影ほど高くない想定のため、機能を絞ってステップを軽くした
- 設定の「なまえ・写真・日づけ・一言」からも、あとから追加・変更できる
  （項目名も「・納骨」を足した）
- バックアップの書き出し・読み込みは、写真の種類を問わず全メディアを
  対象にしているため、変更なしでお骨の写真も対象に入る

**没入感の置き場所**：おまいり画面（霊園の絵）に、設定していれば台座の
横に小さな写真立てのように表示するようにした。台座の上（灯籠・
おそなえの印が並ぶ面）はすでに埋まっているため、左の灯籠と台座の
あいだの空きに置いた。設定していなければ何も出さず、画面は今までと
変わらない。

## オープニング画面

アプリを開いた最初の一枚として、灯りのマーク（呼吸するように光る）・
「ペットのお墓」という前置き・アプリ名「ともしび」・「あの子といつも
一緒にいるために」という一言・「はじめる」ボタンだけの、静かな迎えの
画面を追加した。押すと、いつも通り（初回ならおむかえ、2回目以降なら
おうち）に進む。

設定に「起動画面」の項目を足し、「表示する／省く」を選べるようにした。
既定は「表示する」（=毎回出る）。1回目は選ぶ間もなく必ず出るが、
2回目以降は設定でいつでも省けるようにする、という依頼どおりの形。

## 確かめたこと

Playwrightで、初回はオープニング画面が必ず出ること、「はじめる」を
押すとおむかえに正しく進むこと、設定で「省く」を選んで保存すると
次回はオープニングを飛ばしてトップへ直接進むことを確認した。おむかえで
お顔の写真を「あとで えらぶ」でスキップしても、次のステップが正しく
お骨の写真（ステップ3）になること、そこも空のまま次へ進めること、
実際に写真を入れると「とりこみました」表示に変わり、おまいり画面の
台座横に写真立てとして表示されることを確認した。設定の項目名・編集
画面上部のラベルに「・納骨」が加わっても、320px幅で「とじる」ボタンが
崩れない（追記36と同じ対策が効いている）ことも確認した。IndexedDBの
実データを直接確認し、お骨の写真が`kind:'ashes'`で保存され、遺影
（`portrait`）や動画とは別に扱われていることを確認した。名まえ入力→
おむかえ完走→おまいりを2回連続→にわ・じぶん・アルバム・うごく・
てがみの各画面を一巡してもJSの例外が出ないことも確認した。

# 追記43：お骨の写真を、おうち画面にもイラストで示す（2026-09-17）

利用者から2件の指摘。「納骨の写真登録したけどトップに出てこないよ。
写真登録するけど、ここはイラストで表示させるほうがよい」。

追記42で足したお骨の写真は、おまいり画面（霊園の絵）の台座横にしか
出しておらず、いちばん最初に開く「おうち」画面（利用者の言う
「トップ」）には何も出していなかった。せっかく登録しても、
おまいりを開くまで反映を確認できない作りだった。

もう1点、おまいり画面の写真立てはこれまで登録した実物の写真を
そのまま表示していたが、これも見直した。遺影とちがい、お骨の写真は
骨壺や祭壇そのものを写した生々しい写真になりがちで、見るたびつらく
なる人もいる。ぬくもりのあるお参りの場にはそぐわないため、
**表示は常に骨壺のイラスト（新しく足した`#of-urn`）にし、
登録した実物の写真そのものは画面には出さない**方針に変えた
（他の`of-*`のお供えアイコンと同じ、塗り＋輪郭線のやさしい絵柄）。
写真登録じたいはこれまで通り残している。「登録した」という事実を
アプリの中で表せて没入感につながる一方、生々しい実写を毎回目にせず
に済む、というバランスを取った（おむかえのステップ内の写真えらび
プレビューだけは、選んだ物を確かめる用途のため、これまで通り実物の
写真を出したままにしている）。

**おうち画面**：戒名の札が絵の右上に立つのと対をなす形で、絵の
左下すみに、骨壺イラストの小さな丸い印（`.ashes-badge`）を追加した。
登録していなければ何も出ない。タップでは何も起きない、事実を伝える
だけの印（`pointer-events:none`）。

**おまいり画面**：台座横の写真立て（`#reien-ashes`）の中身を、
実物の写真の`<img>`から骨壺イラストの`<svg><use href="#of-urn">`に
差し替えた。枠の位置・大きさ（左の灯籠と台座のあいだ）はそのまま。

## 確かめたこと

Playwrightで、お骨の写真を登録すると、おうち画面左下に印が出る
こと（`hidden`が外れる、32×32pxの丸）、おまいり画面の写真立てにも
同じ骨壺イラストが出ること（`<img>`が無くなり`#of-urn`のアイコンに
なっていること）を確認した。登録しなかった場合は、おうち画面の印・
おまいり画面の写真立てのどちらも出ないまま（`hidden`のまま）である
ことも確認した。設定の編集画面から後付けで写真を登録した場合も、
即座に印が出ること、リロード後もIndexedDBから読み直して印が保たれる
ことを確認した。昼・夜どちらの配色でもイラストの輪郭がちゃんと読み
取れることをスクリーンショットで確認した。名まえ入力→おむかえ完走→
おまいり→各画面一巡までJSの例外が出ないことも確認した。

# 追記44：お骨の写真の見せ方を、イラストか実物の写真か選べるように（2026-09-17）

利用者から「骨壷はイラストか写真選べるようにして」との依頼。追記43で
「実物の写真は見るたびつらい人もいる」という理由からイラストに固定した
ばかりだが、それは一律の判断であり、実物の写真のほうがいいと感じる人も
いる。選べるようにするのが筋という判断で、設定に項目を足した。

設定の「戒名」カードのすぐ下に、新しいカード「納骨の写真の見せ方」を
足した。お骨の写真を登録していないうちは意味のない選択なので、
`#seg-opening`（起動画面）と同じ構えの2択セグメント（イラスト／写真）
を、お骨の写真を登録したときだけ出す（登録していなければカードごと
`hidden`）。既定は「イラスト」（追記43からの見た目を変えない）。

選んだ内容は`st.pet.ashesShowPhoto`（既定`false`）に保存し、おうち
画面左下の印・おまいり画面の写真立ての両方に、切り替えた瞬間から
即座に反映する。バックアップの書き出し・読み込みは状態全体を対象に
しているため、この項目も変更なしで対象に入る。

## 確かめたこと

Playwrightで、お骨の写真を登録すると設定に「納骨の写真の見せ方」
カードが現れること（登録前は`hidden`のまま）、既定で「イラスト」が
選ばれていること、「写真」に切り替えるとおうち画面の印・おまいり
画面の写真立てのどちらもすぐに登録した実物の写真（`<img>`）に変わる
こと、リロードしても選んだ「写真」のままであること、「イラスト」に
戻すと両方とも骨壺のイラスト（`#of-urn`）に戻ることを確認した。
写真モードでも昼・夜どちらの配色でJSの例外が出ないことも確認した。

# 追記45：お骨の印は、おまいり画面だけに（2026-09-17）

利用者から「トップ画面は骨壷不要にして。お参り画面だけでよい」との
依頼。追記43でおうち画面の左下にも骨壺の印を足していたが、利用者の
判断で、おうち画面（トップ）には出さず、おまいり画面の写真立てだけ
残すことにした。

`#home-ashes`（おうち画面の丸い印・`.ashes-badge`）をHTML・CSS・
JSから丸ごと削除した。おまいり画面の`#reien-ashes`（写真立て）は
そのまま残り、設定「納骨の写真の見せ方」（追記44）も引き続き効く。
設定カードの説明文も「おうちとおまいりの絵に出す見た目」から
「おまいりの絵に出す見た目」に直した。

## 確かめたこと

Playwrightで、`#home-ashes`要素そのものがDOMに存在しなくなった
こと、おうち画面の見た目に印が残っていないことをスクリーンショットで
確認した。おまいり画面の写真立ては変わらず機能すること（お骨の写真を
登録すると出る、設定で「写真」に切り替えると登録した実物の写真に
変わる）、登録しなければ写真立てが出ないままであることも確認した。
名まえ入力→おむかえ完走→おまいり→設定の編集画面から後付けで登録、
という一連の流れでJSの例外が出ないことも確認した。

# 追記46：飼い主の心情に寄り添う3つの機能（2026-09-17）

「その他どこを変えるとより良くなると思う？飼い主の心情に寄り添う感じで」
という問いに対し、実世界のグリーフケアの考え方（continuing bonds＝
お別れした相手との対話を続けることが支えになる、Dual Process Model＝
悲嘆は行ったり来たりしながら進む）と、このアプリがすでに持つ土台
（大きな節目の自動計算、手紙、庭の記録）を踏まえて3つを提案し、
「全部対応して」との返事を受けて実装した。プッシュ通知はどれも
使わない（自前サーバーが要るため、既存の追記どおり）。アプリを開いた
ときだけ気づける、静かな作りに統一した。

## 1. 手紙のこだま（`#letter-echo`）

大きな節目（四十九日・百か日・一周忌・三回忌・お誕生日。頻度の高い
月命日は含めない）ちょうどその日だけ、おうち画面に、過去に書いた
手紙を1通そっと差し出すカードを出す。日付と手紙数から必ず同じ1通が
決まる（`pickEchoLetter`。乱数を保存しないので、開き直しても同じ
手紙のまま）。タップすると「これまでのてがみ」を開く。右上の×で
その日だけ閉じられる（`S.dismissEcho`）。手紙が1通もない、その日が
節目でない、すでにその日閉じた、のいずれかで出さない。

## 2. ふりかえり（`view-review`）

大きな節目を過ぎたら、「にわ」タブの節目カウントダウンの下に
「〇〇を迎えて／これまでの日々を、そっとふりかえる」という入口が
現れる。節目のその日だけでなく、過ぎたあとはいつでも開けるように
した（その日にアプリを開くとは限らないため）。中身は見送ってからの
日数・おまいりした回数・書いた手紙の数・じぶんを記録した日数の4つ。
回数を人と比べる・良し悪しを付けるためのものではなく、「ゆっくりでも、
ちゃんと続いていた」とだけ気づいてもらうための画面なので、グラフや
ランキングにはしない。

## 3. 節目の日だけの、おまいりのあと画面

四十九日・百か日・一周忌・三回忌・お誕生日、その日にお参りすると、
「ありがとう。またね。」の一言が節目ごとの言葉に変わり
（`MILESTONE_LINE`）、すでにある虹と光の絵（追記41）の不透明度を
ほんの少しだけ上げる（`.heaven.milestone`）。新しい絵は増やさず、
既存の淡さを一段引き上げるだけにとどめ、はしゃぎすぎない特別感に
した。

## 確かめたこと

Playwrightで、命日を48日前に設定（＝今日が四十九日）し、手紙を1通
書いたうえで、おうち画面に手紙のこだまカードが正しい節目名・日付・
本文で出ること、×で閉じると同じ日は出なくなること（タブを行き来
しても戻らない）を確認した。「にわ」タブにふりかえりの入口が現れ、
開くと「四十九日を迎えて」の見出し・見送ってからの日数・3つの数字
（おまいり0・手紙1・じぶん0）が正しく出ることを確認した。命日を
400日前に設定すると、見出しが「一周忌を迎えて」に変わり、数字も
そのときの状態に合わせて更新されることを確認した。四十九日に
実際におまいりを完了させると、おまいりのあと画面の一言が節目の
文言に変わり、`.heaven`要素に`milestone`クラスが付くことを確認した。
命日を設定していない・手紙が1通もない場合は、手紙のこだまもふりかえり
の入口も出ないこと、おまいりのあと画面は既定の「ありがとう。また
ね。」のままであることも確認した。夜配色でも例外が出ないこと、
320px幅でふりかえり画面の3つの数字カードが崩れないことも確認した。

# 追記47：無駄な動き・セキュリティの点検（2026-09-17）

「ここまでの状態で、アプリ最適化して。無駄な動きや、セキュリティ的に
問題ないか。」との依頼を受け、`index.html`・`css/style.css`・
`js/app.js`・`js/store.js`・`sw.js`・`manifest.webmanifest`を通しで
点検した。結論：作り込み済みの防御（後述）のおかげで、直せる深刻な
穴は見つからなかった。見つけた小さな無駄を直し、防御をもう一段固くした。

## すでに安全だったこと（確かめただけで、直す必要はなかった）

- **innerHTMLへの書き込みはすべて、ユーザーの文字は`esc()`（またはそれに
  類する専用のエスケープ）を必ず通してから差し込んでいた**（戒名・
  好きだったもの・章の名前・動画の名前・手紙の本文・ふりかえりの数字、
  すべて確認した）。人の名前や気持ちは`textContent`で入れている箇所が
  大半で、HTMLとして解釈される余地がそもそもない
- **バックアップの読み込み（`restoreAll`）は、決まった項目名の一覧
  （`blank()`が持つキー）としか照合しない作り**だったため、悪意ある
  バックアップファイルに`__proto__`のような項目が混ざっていても
  触られることがない（プロトタイプ汚染が起きない）。中身の文字列も、
  結局は上記の`esc()`を通ってから画面に出るため、細工した手紙の本文
  などが仕込まれていても実行されない
- **写真・動画のBlob URLは、`<img>`・`<video>`のsrcとしてしか使って
  いない**。直接開く・新しいタブで開く・`location.href`に入れる、
  といった使い方はどこにもなく、細工したファイル形式（MIME）を
  仕込まれても実行される経路がない
- `eval`・`new Function`・`document.write`・文字列を渡す`setTimeout`・
  インラインの`onclick=`属性は、コード全体を探しても1つもなかった
- `sw.js`は同一オリジンのGETしかキャッシュしない作りで、外部ドメインの
  リクエストには手を出さない
- ICS（カレンダー）書き出しは、名前に特殊な記号が入っていても
  `icsText()`でエスケープしてから書き出していた

## 直したこと

- **CSP（Content-Security-Policy）を新しく足した**。このアプリは
  どこにも通信しない作りなので、「スクリプトは自分のファイルだけ・
  つながっていい先はGoogle Fontsの2つのドメインだけ」と明記できる。
  スタイルだけ`'unsafe-inline'`が要る（画面のあちこちに
  `style="..."`があるため）。この一手だけで、今後もし何かのはずみで
  HTMLに文字列を差し込む書き方をしてしまっても、外部の`<script>`を
  差し込む形の攻撃はブラウザ自身が止めてくれるようになる
  （多層防御。`index.html`）
- CSPで`script-src`を`'self'`（インライン不可）にするため、
  `index.html`の末尾にあった小さなインラインスクリプト
  （サービスワーカー登録）を`js/register-sw.js`という別ファイルに
  出した。`sw.js`のキャッシュ対象にもこのファイルを追加し、
  キャッシュの版（`CACHE`）も上げて確実に配り直されるようにした
- 使われていなかったCSS（`.tip-grass`。3種類あった「知らせ」の色の
  うち、結局どこからも呼ばれていなかった1つ）を削除した

## 確かめたこと

Playwrightで、CSPを足したあとの状態で、写真・お骨の写真の取り込み
（Blob画像の表示）・戒名・好きだったもの・手紙の送信・手紙のこだま・
ふりかえり・おまいり（節目のあと画面まで）・設定（バックアップの
書き出し・カレンダー書き出し）・タブの行き来を一通り操作し、
`securitypolicyviolation`イベントが1件も発生しないこと、コンソールに
新しいエラーが出ないこと、サービスワーカーが変わらず登録・有効化
されることを確認した。既存のセッション内の確認スクリプト（お骨の
イラスト／写真の切り替え、おまいり画面だけの表示、節目の3機能）も
すべて同じ結果のまま通ることを確認した。

# 追記48：設定の「つらいときの相談先」を、一旦なくす（2026-09-18）

利用者から「相談先の項目はいまは一旦削除して」との依頼。設定の中に
常時出ていた「つらいときの相談先」の行（`#btn-help`）を消した。

「じぶん」画面で、重い気持ちの記録が5日続いたときだけそっと出る
案内（`#self-help`・`#btn-self-help`）は、安全にかかわる仕組みなので
今回は触らず残した。同じ`showHelp()`（相談先の一覧）をそのまま使う。
設定からの入口だけをなくし、必要なときに気づける案内は残す、という
形にした。

## 確かめたこと

Playwrightで、設定画面に「つらいときの相談先」の行が無くなっている
こと（`#btn-help`がDOMに存在しない）、前後の「保存のようす」「この
端末のデータを消す」の並びが詰まって崩れないことをスクリーンショットで
確認した。重い記録が5日続いた状態を作って「じぶん」画面を開くと、
これまでどおり案内が出て、ボタンを押すと相談先の一覧が開くことを
確認した。JSの例外が出ないことも確認した。

# 追記49：「じぶん」の相談先の案内も消す（2026-09-19）

追記48で設定からの入口だけ消したところ、利用者から「重い気持ちが
続いた時のも消していいよ」と、残りも消してよいとの返事があった。

「じぶん」画面で重い記録が5日続いたときにそっと出ていた案内
（`#self-help`・`#btn-self-help`）と、それが呼んでいた相談先の
一覧（`showHelp()`）を削除した。追記48の時点であえて残していた、
この機能自体をなくす依頼だったため、関連コードを一式取り除いた：

- `js/app.js`：`renderSelfChart()`内の案内表示ロジック、`showHelp()`
  関数、それらに触れていた古いコメントを削除
- `js/store.js`：呼び手がいなくなった`S.heavyRun()`（重い記録が
  何日続いているかを数えるためだけの関数）を削除
- `index.html`：`#self-help`の入れ物と、「じぶん」の説明文にあった
  「おもい日が続いたときだけ、相談できるところをお知らせします」の
  一文（もう実際の動きと合わなくなるため）を削除

## 確かめたこと

Playwrightで、重い記録（気持ち1）が5日続く状態を作って「じぶん」を
開いても、案内も相談先ボタンも一切出ないこと（`#self-help`・
`#btn-self-help`ともにDOMに存在しない）、グラフの直後に説明文が
詰まって崩れないことを確認した。にわ・アルバム・うごく・おまいり
（気持ちの記録まで）と一巡してもJSの例外が出ないこと、設定に
「つらいときの相談先」が引き続き出ていないこと（追記48の状態が
保たれていること）も確認した。

# 追記50：波のグラフの説明を、この子の名で締める（2026-09-19）

利用者から「ここで見るのは、良くなったかどうかではありません。
波のかたちです。のところ、ここをもっと寄り添う型でうまく表現して
記載して。」との依頼。例として「あなたがこの悲しみを乗り越える日を
モコ（ペットの名前）も見守っています。」を挙げられた。

「じぶん」画面の説明文（波のグラフの下）の最後に、その子の名を
呼んで見守っている、という一文を足した。名は固定の「モコ」では
なく、登録したこの子の名（無ければ「あの子」）が実際に入るように、
`#jibun-note-warm`という空の`<span>`をHTMLに置き、`renderJibun()`
で`textContent`に差し込む形にした（名にどんな文字が入っても安全に
表示できる）。

言葉は例をそのまま使わず、「乗り越える日を見守る」から「ゆっくり
向き合っていく日々を、そばで見守っている」に直した。理由は、
すぐ上の「良くなったかどうかではありません」「平均も目標線も
出しません」という、このアプリの一貫した立場（Dual Process Model。
悲嘆は"克服してゴール"に向かうものではなく、行きつ戻りつしながら
進むもの）と、「乗り越える日」という終着点を示す言い方が食い違って
しまうため。寄り添う気持ちはそのままに、「見送った子が、いつも
そばにいる」という、このアプリの他の場所（オープニング画面の
「あの子といつも一緒にいるために」など）とも重なる言い方にした。

## 確かめたこと

Playwrightで、ペットの名を登録した状態で「じぶん」画面を開くと、
説明文の最後に「あなたがこの悲しみとゆっくり向き合っていく日々を、
（その名）もそばで見守っています。」が正しく出ることを確認した。
名を登録していない場合は「あの子」に置き換わることも確認した。
説明文の枠が、文が増えても崩れないことを昼・夜配色、320px幅の
スクリーンショットで確認した。JSの例外が出ないことも確認した。

# 追記51：ストア配信（iOS）に向けて、Capacitorでネイティブの土台を作る（2026-09-19）

利用者から「ストア配信をしたい」「iPhoneで進めよう」「では、ネイティブ
っぽくしよう。プッシュ通知機能は何か入れよう」との依頼。Appleの審査
（ガイドライン4.2「単なるWebサイトに見えないこと」）を通りやすくする
ため、既存の静的HTML/CSS/JSをそのまま活かしつつ、Capacitorでネイティブ
の器に包み、ネイティブAPIを実際に使う4点を実装した。

## 方針：ルート直下は今まで通り、www/はビルド成果物

このアプリはGitHub Pages配信のPWAとして、ルート直下の`index.html`・
`css/`・`js/`・`icons/`・`manifest.webmanifest`・`sw.js`がそのまま
配信物になっている。Capacitorはこれとは別に`webDir`（ネイティブが
読み込む場所）を必要とするため、ルート側を一切変えずに`www/`という
ビルド出力先を新設し、そこへコピーするだけの`scripts/build-www.js`を
書いた（`www/`はコピー先なのでgit管理外＝`.gitignore`）。これにより
GitHub PagesのPWAは今までと完全に同じファイルのまま、影響を受けない。

## npmパッケージ（@capacitor/*）をバンドラ無しのapp.jsから使う

このアプリには元々ビルド工程が無く、`<script src>`でそのまま読む
素のJSファイルだけで出来ている。Capacitorのプラグイン（Haptics・
LocalNotifications・Camera・SplashScreen・StatusBar）はnpm/ESM前提の
パッケージなので、直接app.jsからは読めない。

そこで`capacitor-src/bridge.js`（ESMソース、npmパッケージをimportする）
を新設し、esbuildで`js/capacitor-bridge.js`という素のIIFEにまとめる
（`scripts/build-bridge.js`）。これを`window.TomoshibiNative`という
ただのオブジェクトとしてindex.htmlの先頭（store.js・app.jsより前）で
読み込む。`js/capacitor-bridge.js`はビルド成果物だが、ルート直下の
PWAもこのファイルを直接読むため（バンドラが無いので他に方法がない）、
`www/`とは違いgit管理する＝ルート側の「配信物」の一部として扱う。

`Capacitor.isNativePlatform()`が false（ブラウザ・PWA）のときは、
`TomoshibiNative`の全関数が何もせず`null`を返すだけのno-opになる
（`safe()`というラッパーで統一）。app.js側は「ネイティブかどうか」を
気にせずただ呼べばよく、ブラウザで開いたときの挙動は一切変わらない
（Playwrightで確認済み。詳細は下）。

## 実装した4点

1. **触覚フィードバック**：おまいりの4つのお供えが揃った瞬間
   （`tapOffer()`内、`rstep===4`）に`hapticSuccess()`を呼ぶ。
2. **大事な日のお知らせ（ローカル通知）**：設定に「大事な日のお知らせ」
   カード（`#box-notify`、ネイティブでのみ表示）を追加。既定オフ。
   オンにすると`requestNotifyPermission()`で許可を求め、許可されたら
   `syncMilestoneNotifications()`が`S.milestones()`（既存の.icsカレンダー
   書き出し機能と同じ節目計算）から四十九日・百か日・月命日・お誕生日・
   一周忌・三回忌の次回日を組み立て、端末のOSにそのままスケジュールする。
   **サーバーは一切使わない**——このアプリの「100%ローカル・アカウント
   不要」という一貫した約束はここでも変わらない（以前の追記46で
   「プッシュ通知は自前サーバーが要るため使わない」としていたのは
   サーバー配信の話で、`@capacitor/local-notifications`は端末内で完結する
   別の仕組み）。アプリを開くたびに一旦全部キャンセルしてから今日の日付で
   組みなおすため、日付がずれたまま残ることがない。
3. **カメラ**：顔写真・お骨写真の「えらぶ」ボタンは、ネイティブでは
   OS標準のカメラ／フォトライブラリ選択（`@capacitor/camera`、
   `CameraSource.Prompt`で選択肢を出す）を使う。ブラウザ・PWAでは
   従来通り隠しinputのまま。取得した写真はBlobから`File`に包み直し、
   既存の`pickPortrait()`・`pickAshes()`にそのまま渡せるようにした
   （呼び出され側の処理は一切変更していない）。
4. **起動画面・ステータスバー**：`capacitor.config.json`で
   SplashScreenを`launchAutoHide:false`にし、既存の起動シーケンス
   （`S.probe()`→写真読み込み→オープニング表示）が終わったタイミングで
   明示的に`hideSplash()`する（白い一瞬の空白を防ぐ）。背景色はアプリの
   `--bg`（#FDFAF2）に合わせた単色画像に差し替えた（`ios/App/App/
   Assets.xcassets/Splash.imageset/`、Node標準のzlibだけで生成）。
   ステータスバーの文字色は`applyTheme()`の中で昼/夜と同期する
   （`setStatusBarStyle()`）。なお本物のブランドロゴ入り起動画像は
   今回は単色止まりとした——デザインの詰めは実機を持つ人の目で行う方が
   よい判断だと考えたため。

## iOS側の追加設定

- `Info.plist`にカメラ・写真ライブラリの利用目的（`NSCameraUsage
  Description`ほか）を追加。無いと審査どころか起動時にクラッシュする。
- Capacitor 8はデフォルトでCocoaPodsではなくSwift Package Manager
  （`ios/App/CapApp-SPM/`）でプラグインを解決する。そのため
  `npx cap sync ios`はこのLinuxサンドボックスでも最後まで成功した
  （CocoaPods特有の`pod install`が無いため）。ただし実機・シミュレータ
  でのビルド・実行はXcodeが要るため、利用者自身のMacで行う必要がある
  （ここでは確認できていない）。

## 確かめたこと

`node --check`でapp.js・store.jsの構文確認。Playwrightで、ブラウザ
（`TomoshibiNative.isNative===false`）のとき：`#box-notify`が非表示の
まま、おまいりの4お供えが例外なく完了できること（触覚呼び出しが
no-opでも問題ないこと）、設定画面が壊れていないことを確認。さらに
`window.TomoshibiNative`を差し替えてネイティブを模した状態も作り、
「大事な日のお知らせ」カードが正しく表示・トグルでき、状態が保存
されること、昼・夜どちらの配色でも崩れないことをスクリーンショットで
確認した。実機・Xcodeでのビルド、実際の通知・カメラ・触覚の動作は
このサンドボックス環境では確認できていない（利用者のMac側での確認が
必要）。

# 追記52：プライバシーポリシーページを作る（2026-09-19）

App Store・Google Play双方とも、審査提出にプライバシーポリシーの
URLが必須（App Store Connectの「App Privacy」欄、Google Playの
「データセーフティ」欄）。利用者から「作って」との依頼を受け、
`privacy.html`をルート直下に追加した。

## 方針：一般的なひな形をそのまま使わない

多くのアプリが、実態と合わない汎用テンプレート（「マーケティング
パートナーとデータを共有することがあります」等、実際にはしていない
ことまで書かれたもの）をそのまま貼っている。これは信頼を損なうだけで
なく、Appleの審査でも「プライバシー表示（Nutrition Label）と実装の
不一致」（ガイドライン5.1.1）で足を引っ張りうる。SignalやBasecampの
プライバシーポリシーが「平文・要点先出し」で支持されているのと同じ
考え方で、このページも一般論を書かず、実際のコードを確認した事実
（`index.html`のCSP設定、通信先はGoogle Fontsのみ、等）だけを書いた。

- 冒頭に`.tip.tip-amber`のコールアウトで「ひとことで言うと、情報を
  一切集めません」を要点先出し（Stripe/Basecamp型の「まず結論、次に
  詳細」の構成）
- 各セクションは実装済みの機能単位（保存場所・外部通信・ストア版の
  権限・バックアップの書き出し）に対応させ、書いていないことを
  でっち上げない
- 外部通信は実際にCSPで許可している先（Google Fontsのみ）を確認して
  記載。それ以外へは技術的にブロックされていることも明記
- デザインは新しいトーンを持ち込まず、`css/style.css`の既存トークン
  （`.cardbox`は使わず、読み物として`.tip`と素の見出し階層のみ）で
  そのまま作った。ページ自体はアプリ本体（固定高さ・タブ付きシェル）
  とは違う、普通に縦スクロールする文書として`html,body{height:auto;
  overflow:visible}`で上書き
- `js/store.js`をそのまま読み込み、`Store.state.theme`を見て
  夜テーマの人には夜配色で出す（`js/theme-apply.js`、新規追加の
  数行のみ）。暗い画面から急に眩しい白いページへ切り替わらないため
- 設定画面に「プライバシーポリシー」の行を追加（`.setrow`を`<a>`に
  適用、既存のボタン行と同じ見た目）。ストア審査でのアプリ内からの
  到達しやすさにも資する
- `scripts/build-www.js`の`ENTRIES`に`privacy.html`を追加。ネイティブ
  アプリ内の設定からもこのページへ遷移できるようにするため
- 連絡先メールアドレスは、開発者本人の判断で記載してもらう欄として
  プレースホルダのままにした（勝手に埋めない）

## 確かめたこと

Playwrightで、昼・夜どちらの配色でも崩れないこと（`localStorage`に
`theme:'night'`を仕込んで`data-theme`が正しく反映されること）、
320px幅でも崩れないこと、設定画面の「プライバシーポリシー」の行から
実際に`privacy.html`へ遷移できること、コンソールにJSの例外が出ない
ことをスクリーンショット付きで確認した。

# 追記53：Codemagicでのビルド・提出を自動化する（2026-09-19）

利用者の手元のMac（2014年・macOS Big Sur）にはClaude Codeが入らず、
そもそも最新のXcodeも動かせないため、Mac本体に頼らずクラウドのCIで
ビルド・署名・TestFlight配信まで自動化する方針にした（利用者の選択）。

## codemagic.yamlをリポジトリのルートに置く

このリポジトリはモノレポ（`tomoshibi/`はその中の1アプリ）。Codemagicの
仕様上、設定ファイルはリポジトリのルートに置く必要がある（`tomoshibi/`
の中には置けない）ため、`/codemagic.yaml`とし、`working_directory:
tomoshibi`で対象を絞った。他のアプリ（cashbook・dividend等）には
影響しない。中身は公式のCapacitorサンプル
（`codemagic-ci-cd/codemagic-sample-projects`のionic/ionic-capacitor
-demo-project）を土台にしたが、このプロジェクトはCapacitor 8で
CocoaPodsではなくSwift Package Manager（SPM）を使う（追記51）ため、
`.xcworkspace`が存在しない。そのため公式サンプルの
`--workspace "App.xcworkspace"`ではなく`--project "ios/App/App.
xcodeproj"`に置き換えた（WebFetchで`codemagic-cli-tools`のドキュメント
を確認し、`--project`がworkspace無しのプロジェクト向けに用意されている
ことを確かめた上での変更）。

## ビルド前に見つけて直した、実機を持つ人がいないと気づけない2つの欠落

`npx cap add ios`で生成されたプロジェクトには、誰もXcodeで一度も
開いたことがなかったため、次の2つが欠けていた。ローカルでXcodeを
開けば自動生成されるが、開かれるまでは存在せず、CIのような無人環境
では通らない：

1. **共有スキーム（`.xcscheme`）が無かった。** `xcodebuild -scheme App`
   はスキームが「共有」としてgit管理されていないと動かない。Xcodeを
   開いて手動で「共有」にチェックを入れる操作の代わりに、標準的な
   スキームXMLを`ios/App/App.xcodeproj/xcshareddata/xcschemes/
   App.xcscheme`として直接書いた（`project.pbxproj`内のターゲットの
   BlueprintIdentifierを実際の値に合わせて参照）。
2. **`VERSIONING_SYSTEM`が設定されていなかった。** codemagic.yamlの
   ビルド番号自動採番（`agvtool new-version`）は、ビルド設定に
   `VERSIONING_SYSTEM = "apple-generic"`が無いと動かない。Debug・
   Release両方のビルド設定に追加した。

## 安全側に倒したところ

- `submit_to_app_store: false`を既定にした。審査への提出は明確な
  意思決定なので、ここを`true`に変えるまで自動化しない（TestFlightへの
  配信＝`submit_to_testflight: true`までは自動でよいという判断）。
- `APP_STORE_APP_ID`はダミー値のプレースホルダのまま。App Store
  Connectでアプリのレコードを作ったあとでないと決まらない値のため。
- Codemagic側の設定（App Store ConnectのAPIキー登録など）は、
  リポジトリの外（Codemagicの管理画面）で利用者本人が行う必要がある
  手順として、yamlの冒頭コメントに残した。

## 確かめたこと

`codemagic.yaml`をPythonの`yaml.safe_load`で構文確認。追加した
`App.xcscheme`をPythonの`xml.dom.minidom`で構文確認。`project.pbxproj`
は波括弧の対応数が変更前後で保たれていること（48対48）と、差分が
意図した2行の追加だけであることを確認した。実際のCodemagicでの
ビルド実行そのものは、Codemagicアカウントでの連携設定が要るため、この
セッションでは確認できていない（利用者側での初回ビルドの確認が必要）。

# 追記54：審査待ちの間の小さな手直し3点（2026-09-19）

Apple Developer Programの審査待ちの間に、利用者から3つの依頼。

## 1. オープニング画面の灯りに「灯る」演出を足す

いままでは、灯りのアイコンがふわふわと光るだけ（`opening-breathe`）で、
アイコン自体は最初から出来上がった状態で表示されていた。「優しく火が
灯る演出」という依頼を受け、開いた瞬間に火が小さく縮こまった状態
（`opacity:0、scaleY(.2)`）から、ひと呼吸でふわっと立ちあがり
（`opening-flame-light`、0.9秒、少しだけオーバーシュートして着地する
イージング）、そのあとは生きた炎のようにごくわずかに揺れ続ける
（`opening-flame-flicker`、2.8秒周期）という2段階のCSSアニメーション
にした。周りの光暈（グロー）も、同じタイミングで小さく縮んだ状態から
ふわっと現れるようにして、火と光が一緒に灯る印象にした。
`prefers-reduced-motion`では両方とも静止した最終状態のまま出す
（既存の対応を維持）。

あわせて、この画面に「次回からこの画面を出さない」チェックボックスを
追加した。すでに設定画面の「起動画面」（`#seg-opening`）と同じ
`st.openingOff`を読み書きするだけなので、二重管理にはならない
——チェックを入れればその場で次回から省かれ、設定画面を開けば
そちらにもちゃんと反映されている。

## 2. 「じぶん」の波グラフの説明を短くする

「ここで見るのは、良くなったかどうかではありません。波のかたちです。」
は、「見て欲しいのは波のかたち」という主旨を回りくどく言っていた
との指摘を受け、「ここで見るのは、波のかたちです。」に短縮した。
すぐ下に続く「おもい日のあとに、すこし戻る日が来る。またさがる。
その行き来そのものが、前へ進んでいる形です。」が、「良くなったか
どうかではない」という趣旨をすでに十分説明しているため、意味は
変わらない。

## 3. おまいりの庭：花が増えすぎたときの見せ方

これまでは、通算回数ぶんの花を1つずつ並べ、600個で打ち切っていた
（`Math.min(n, 600)`）。長く使うほど際限なく縦に伸び、かつ600を
超えた分は描かれないという、このアプリの「花は枯れない・減らない」
という一貫した約束と食い違う挙動だった。

利用者の提案（「1ヶ月分をまとめて少し大きな花に変える」）どおり、
今月ぶんだけは今まで通り1日1つの花（そなえた実感を、その日のうちに
返す）、**先月より前は月ごとに1つの、すこし大きな花にまとめる**形にした。
大きさはその月に何回おまいりしたか（日数に対する割合）で3段階
（1.35倍／1.65倍／2.0倍）に分け、毎日そなえた月ほど大きく咲く。
どの日の分も消えたり隠れたりはしない——1つの月にまとまることで
「数えられ方」が変わるだけで、実際の記録（`st.visits`）は一切減らして
いない。これにより、600個の打ち切りも不要になった（何年分あっても、
花の数＝「月の数＋今月の日数」で自然に収まる）。月ごとの花には
SVGの`<title>`で「2026年3月・14回」のようなホバー用の補足も添えた
（タップ操作では使われないが、じゃまにもならない）。

## 確かめたこと

Playwrightで：オープニング画面のチェックボックスが`st.openingOff`を
正しく読み書きすること（チェック→true、解除→false）、昼夜どちらの
配色でも灯りの演出が崩れないこと、JSの例外が出ないことを確認。
「じぶん」画面の説明文が短縮された文言で出ることを確認。おまいりの
庭は、0件（初回メッセージ）・当月のみ（今まで通りの小さい花）・
900日分412件（29の月まとめ花＋当月16日ぶんの小さい花、計45個）の
3パターンでスクリーンショットを撮り、サイズの違う花が重ならずに
並ぶこと、「咲いた花」の数字（410件超）はまとめによらず正確な
通算のままであることを確認した。

# 追記55：庭の花を年でもまとめる／お供え完了で灯籠も光る（2026-09-19）

追記54を見た利用者から2つの追加依頼。「3（月まとめ）は最大どうなる？
幅が広くなりすぎるなら、1年経ったら1年の花にまとめて」「おまいりで
4つお供えしたら、両脇の灯籠も光るようにして」。

## 庭の花：年→月→日の3段階に

追記54の「今月は日ごと・それより前は月ごと」のままだと、何年も
使い続けると月の花だけが際限なく増えてしまう（10年なら120個）。
指摘の通りなので、もう1段追加した：

- **今月**：これまで通り、1日ごとの小さな花
- **今年の、今月より前**：月ごとに1つの中くらいの花（追記54のまま）
- **去年より前**：**年ごとに1つの、さらに大きな花**

年が変わった瞬間、それまで「今年の月の花」として並んでいたものは、
自動的に「去年の年の花」1つへとまとまる（実装上は、visitの年が
今年かどうかで振り分け先が変わるだけで、特別な繰り上げ処理はない）。
大きさは月と同じ考え方（その期間にどれだけおまいりしたかの割合）で
3段階、ただし月より一回り大きい倍率にして「年の花＞月の花＞日の花」
の大小関係が一目でわかるようにした。レイアウトも段ごとに専用の
列数・間隔を持たせ（年は5列・間隔64、月は8列・間隔40、日は11列・
間隔30）、いちばん大きい年の花同士が重ならないようにしている。

これにより、たとえば8年・1810回おまいりしても、花の数は
「年8＋月8＋日16」で32個に収まる（以前の月だけまとめでも、10年で
100個を超えていた）。何年使っても際限なく縦に伸びることはない。

## おまいり：4つそろったら、両脇の灯籠にも火が入る

これまで、4つのお供え（灯り・お水・ごはん・お花）がそろうと台座の上の
印が増えるだけだった。指摘を受け、両脇の灯籠の火（`.r-flame`）にも
光る演出を追加した。灯籠の輪郭ではなく火だけをふわっと光らせる
（`drop-shadow`フィルタを2.6秒周期で明滅）。左右で0.5秒ずらし、
同じ生きた炎でも動きが揃いすぎないようにした。`renderRitual()`で
`rstep>=4`のときだけ`.reien`に`lit`クラスを付け外しするだけなので、
おまいりをやり直せば（`startRitual()`でrstepが0に戻れば）元に戻る。
`prefers-reduced-motion`では静止した光り方にする（オープニング画面の
灯りの演出と同じ配慮）。

## 確かめたこと

Playwrightで、8年分・1810件の記録を仕込んで庭を描画し、花の総数が
32個（年8＋月8＋日16）に収まること、SVGの`<title>`（年・月それぞれの
内訳）が意図通り出ることを確認。おまいりで4つそなえた瞬間に
`.reien`へ`lit`クラスが付くこと（そなえる前はfalse、そなえた後は
true）を確認。JSの例外が出ないことも確認した。

# 追記56：おまいり画面を上に詰める、9月のお供えを廃止する（2026-09-19）

利用者から2つの依頼。「灯籠が光るのは、4つ選択した瞬間に」（追記55の
実装ですでにそうなっていたので、変更なし・動作を再確認しただけ）。
もう1つ、「おまいりを終えるボタンがスクロールなしで見えるように
したい。9月のお供えは消してそのあたりに終了ボタンを置きたい。でも
好きだったものは残したい」。

## 季節のおそなえ機能を廃止する

「〇月のおそなえ（例：おはぎ）」という、月がわりで置ける任意のカードを
画面から完全に削除した。UIだけでなく、支える側の仕組み一式も削除した
（追記49で「じぶん」の相談先案内を消したときと同じやり方）：

- `index.html`：`.seasonal`ボタンの塊を削除
- `js/app.js`：`renderRitual()`内の`#seasonal-*`更新処理、`wire()`内の
  `#seasonal`クリックハンドラを削除
- `js/store.js`：`SEASONAL`配列、`seasonalFor`・`seasonalDone`・
  `putSeasonal`関数、`state.seasonal`フィールド、どこからも呼ばれなく
  なった`ym()`ヘルパーを削除。古い保存に`seasonal`が残っていれば
  読み込み時に消して書き戻す（`photoHidden`のときと同じ移行処理）
- `css/style.css`：`.seasonal`関連のスタイル、`:active`の一覧からの
  参照を削除
- `README.md`：画面構成の説明文から「季節のおそなえ」の記載を除く

## おまいりを終えるボタンを上に

「また、あとで／おまいりを終える」ボタン（`#btn-omairi-close`）を、
好きだったもの一式より前、4つのお供えボタンのすぐ下に移動した。
空いた「季節のおそなえ」カードの場所がちょうどこのボタンの定位置に
なった。iPhone想定の画面高さ（390×844）で、スクロールせずにこの
ボタンまで見えることをPlaywrightで確認した。「好きだったもの」は
削除せず、このボタンの下にそのまま残してある——おまいり自体は
ボタン1つで完結しつつ、好きだったものをそなえたい日はそのまま
下にスクロールして続けられる。

## 確かめたこと

Playwrightで、旧`seasonal`フィールドを仕込んだ保存データが読み込み時に
消えること、`#seasonal`要素がDOMに存在しないこと、`#btn-omairi-close`
が390×844の初期表示範囲内（スクロール不要）に収まること、4つそなえる
と灯籠が光り・ボタンが「おまいりを終える」に変わること、好きだった
ものの一覧がボタンの下に変わらず表示・操作できることを確認した。
昼・夜どちらの配色でも崩れないこともスクリーンショットで確認した。

# 追記57：好きだったものをおまいりを終えるの上に、上限を2つに（2026-09-19）

追記56で「好きだったもの」を「おまいりを終える」ボタンより下に
動かしたが、利用者から「やはり好きだったものを、終えるボタンの
上にしたい」との依頼。並び順を、4つのお供え→好きだったもの→
おまいりを終える、に戻した。

並べ替えただけだと、好きだったものを3つ登録している状態で
`#btn-omairi-close`が390×844の初期表示範囲からわずかにはみ出す
（850px、6pxオーバー）ことが分かったため、直前の見出し
（`#faves-h`）とボタン自身の上余白を`--s6`(24px)から`--s4`(16px)に
詰めて、834pxに収まるよう調整した（利用者の「バランス調整して」に
対応）。

続けて、「好きだったものを最大2つにしよう」との依頼。3つ→2つに
変更した。変わったのは上限の数字だけで、仕組みはそのまま
（`addFave()`の上限チェック、設定/おむかえの追加欄の無効化と
プレースホルダ「2つまでです」、おまいり画面の「足す」ボタンの
表示条件）。すでに3つ登録している既存ユーザーのデータを消したり
2つに切り詰めたりはしない——新しく4つめ・3つめを足せなくなるだけで、
既存の登録はそのまま残る。README.mdの例示（さつまいも・ボール・
まくらの角の3つ）も、上限に合わせてさつまいも・ボールの2つに書き
直した。

## 確かめたこと

Playwrightで、並び順の変更後・余白調整後に`#btn-omairi-close`が
390×844の範囲内（834px）に収まることを再確認。「好きだったものを
足す」で2つ登録すると「足す」ボタンが消え、2つのチップだけが
残ることを確認。JSの例外が出ないことも確認した。

# 追記58：3つ登録済みだった好きだったものを、読み込み時に2つへ切り詰める（2026-09-19）

追記57で上限を2つにした後、利用者から「モコの好きなものが選択
できなくなっている」との報告。実機で、上限変更より前に3つ登録して
いたデータが残っていた状態だった。

Playwrightで2つ・3つ双方の登録数で「おまいり」画面のチップを実際に
タップして確認したが、選択自体（`aria-pressed`の切り替え、
「そなえる」ボタンの表示）はどちらの件数でも問題なく動作した
——チップの選択ロジック自体にバグは見つからなかった。ただ、
利用者から「まだ誰もユーザーいないので、3つ目は強制的に消して
いいよ」と明確な許可があったため、原因の特定より実利を優先し、
`js/store.js`の`load()`に移行処理を追加した：`pet.faves`が2件を
超えていたら、読み込み時に先頭2件だけ残す（3件目以降を切り詰める）。
`photoHidden`・`seasonal`と同じ「読み込み時に古い状態を消して
書き戻す」パターン。バックアップにも古い3件目は残らなくなる。

## 確かめたこと

Playwrightで、3件仕込んだ保存データを読み込ませると、読み込み直後に
`pet.faves`が先頭2件に切り詰められて保存し直されること、おまいり
画面に2件だけ表示されること、その2件がちゃんと選択できることを
確認した。JSの例外が出ないことも確認した。

# 追記59：好きだったものを「選ぶ→そなえる」の2段階から、1タップに（2026-09-19）

追記58のあとも、利用者から「サツマイモを選べないよ」との報告が続いた。
実際の仕組みを見直すと、これまでの好きだったものは「タップで選ぶ
（`faveSel`に印）→別の『そなえる』ボタンで確定」という2段階になって
いた。4つのお供え（灯り・お水・ごはん・お花）は1タップでその場で
確定するのに対し、ここだけ手順が1段多く、選んだつもりでも
「そなえる」ボタンを押し忘れると何も起きていないように見えた
はず——これが「選べない」と感じられていた実体だったと判断した。

利用者からも「ここは選択したら、お供えするボタンは不要。上4つ選択し、
好きなもの選択して、おまいりを終えるでいいよ」と、4動作と同じ
1タップ確定の仕組みに揃える方針が明確に示されたため、その通りに
直した：

- `js/app.js`：タップした瞬間に`S.putFave()`を呼んで確定するように
  変更。「選ぶだけ」の状態（`faveSel`）と、それをまとめて確定する
  `#btn-fave-offer`（「そなえる（N）」ボタン）を丸ごと削除
- 確定した好きだったものは、4動作の「done」ボタンと同じ見た目
  （緑・disabled）にする。もう一度タップしても何も起きないことが
  見た目からも分かるようにした
- ヒント文言を「気になるものを選んで、まとめてそなえられます」から
  「気になるものをタップして、そなえます」に変更（2段階だった説明を
  1段階に合わせた）
- `index.html`から`#btn-fave-offer`のボタンを削除、`css/style.css`
  から`.fave[data-sel="1"]`・`.btn.fave-offer-btn`の使われなく
  なったスタイルを削除

## 確かめたこと

Playwrightで、好きだったものを1回タップした直後に（別のボタンを
押さなくても）`faveDone`へ即座に保存されること、そのチップが緑・
disabled表示になること、`#btn-fave-offer`がDOMに存在しないことを
確認。4つのお供え＋好きだったもの2つをすべてそなえたあと、灯籠が
光り「おまいりを終える」に到達できることを昼・夜どちらの配色でも
スクリーンショットで確認した。JSの例外が出ないことも確認した。

# 追記60：「画面真っ白で何もできない」への対処（2026-09-19）

利用者から「アプリ立ち上げると画面真っ白で何もできない」との報告。

## 調べたこと

`.view`はJSが`.on`クラスを付けるまでCSSで隠れているため、起動処理の
どこかで例外が起きて`show()`に一度も辿り着けないと、何も表示されない
「真っ白」になる。心当たりのある箇所（追記54〜59で触った、
`faveSel`・`seasonal`・`#btn-fave-offer`まわり）をapp.js・index.html
全体で再度grepし、消し残しがないことを確認。app.js内の`$('#id')`が
参照するID一つひとつをindex.htmlの実IDと突き合わせても、存在しない
IDへの参照は無かった（動的に生成される要素への参照のみ、静的grepでは
見つからないが実際には問題なし）。Playwrightで、初回起動・通常の
起動（お気に入り0〜2件）を白紙の状態から確認しても再現しなかった。

コードそのものに直接の欠陥は見つからなかったが、今日1日で何度も
連続してmainへpushしたため、GitHub PagesのCDN反映のタイミングずれ
（ファイルごとに反映のタイミングが数十秒ずれることがある）や、
サービスワーカーの古いキャッシュが一時的に残っていた可能性が
考えられる。

## 対処したこと

1. **起動処理を try/catch で包み、失敗したら必ず何か表示する**
   （`js/app.js`）。今までは起動のどこかで例外が起きると本当に何も
   表示されなかったが、これからは「読み込みでうまくいきませんでした。
   もう一度開いてみてください。」という文言と再読み込みボタンを
   必ず出す。CSPが`script-src 'self'`のみでインラインスクリプトを
   許さないため、ボタンは`onclick`属性ではなく`addEventListener`で
   配線している。原因の特定にはならないが、「真っ白で何もできない」
   状態そのものは二度と起きなくなる。
2. **サービスワーカーのキャッシュを世代交代させる**（`sw.js`）。
   `CACHE`を`tomoshibi-v6`→`tomoshibi-v7`に上げ、事前キャッシュする
   ファイル一覧（`ASSETS`）に、追記51〜53で増えていた
   `privacy.html`・`js/capacitor-bridge.js`・`js/theme-apply.js`を
   追加した（今まで漏れていた）。バージョン文字列を変えることで、
   次にアプリを開いたときサービスワーカー自身が更新され、古い
   キャッシュ世代がすべて消えて作り直される。

## 確かめたこと

`node --check`でapp.js・sw.jsの構文確認。起動処理を独立した最小HTML
で再現し、例外を起こしたときにアプリ本体が隠れて案内文とボタンが
表示され、ボタンのクリックが正しく発火することを確認した。通常の
起動（初回・お気に入り0〜2件）ではフォールバック画面が出ず、今まで
通り正しい画面が出ることも確認した。

# 追記61：「まだ選べない」の本当の原因——network-firstのつもりが古いHTTPキャッシュを返していた（2026-09-19）

追記59・60を経てもなお、利用者から「開いたけど、やはりまだサツマイモ
など好きなものを選択できない」との報告。ここまで機能自体は
Playwrightで繰り返し確認できていたため、コードの欠陥ではなく
**「直したはずの新しいコードが、そもそも利用者の端末に届いていない」**
可能性を本気で疑って`sw.js`を見直した。

`sw.js`の`fetch`ハンドラは、コメント上は「network-first」（通信できる
ときは必ず最新を取る）としていたが、実際の実装はただの`fetch(req)`
だった。**素の`fetch()`は、Service Worker経由でもブラウザの通常の
HTTPキャッシュ（サーバーが返すCache-Controlヘッダ）にそのまま従う。**
GitHub PagesのCDN（Fastly）は静的ファイルに数分〜十数分程度の
Cache-Controlを付けて配信することがあり、その間はいくらmainに
push・マージしても、利用者の端末はCDN経由の"まだ古いHTTPキャッシュ
の応答"を「最新」として受け取ってしまう。今日はPR #101から #108まで
立て続けにマージしていたため、この時間差の影響をもろに受けていた
可能性が高い——**「直したのに、直っていないように見える」を
何度も引き起こしていた本当の原因はこれだったと考えられる。**

## 直したこと

- `sw.js`：`fetch(req)`を`fetch(req, { cache: 'no-store' })`に変更。
  これでService Worker内のfetchが、ブラウザのHTTPキャッシュを
  一切経由せず、確実にサーバーへ問い合わせにいくようになる
  （オフライン時のキャッシュ利用フォールバックはそのまま）。
  `CACHE`を`v7`→`v8`に。
- `js/register-sw.js`：今までは`register()`するだけで、更新確認は
  ブラウザの既定タイミング（数時間〜1日おき、開かなければ確認すら
  されない）に任せていた。開くたびに`registration.update()`で明示的に
  最新のsw.jsを確かめ、新しいService Workerに切り替わった瞬間
  （`controllerchange`）に1回だけ自動でページを読み込み直すようにした。
  これで、以後は手元のキャッシュだけ取り残される事故が起きにくくなる
  （register-sw.js自身は毎回サービスワーカーのfetchハンドラを通って
  取得されるため、この修正はキャッシュに阻まれず次回アクセス時から
  効く）。

## 確かめたこと

`node --check`でsw.js・register-sw.jsの構文確認。Playwrightで
サービスワーカーが正しく登録・有効化されること（`registration.active`
がtrueになること）、その状態でおまいり画面が問題なく開けることを
確認した。`{cache:'no-store'}`によるHTTPキャッシュバイパスの効果
そのものは、このサンドボックス環境の簡易HTTPサーバーではCDNの
挙動を再現できないため、実際のGitHub Pages上での確認はできて
いない——利用者に、この変更のマージ後に改めて確認してもらう必要が
ある。

# 追記62：好きだったものをそなえても「押しても何も変わらない」に見える（2026-09-19）

追記61のマージ後も「やはりまだサツマイモなどは押せない。推しても
何も変わらない」との報告。コードを見直すと、タップ時の見た目の
変化は枠線・背景色が薄い緑（`--grass-ink`／`--grass-soft`）に変わる
だけで、対する4つの基本のお供え（とうみょう・みず・ごはん・はな）
には丸いチェックバッジが付く（`.offer .chk`）。この差により、
たとえ機能自体は正しく動いていても、色の変化だけでは「選んだ」
という手応えが弱く、気づけていなかった可能性が高い。利用者からの
直接の指示「選択したことがわかるようにして」を受けて、見た目の
手応えを強化した。

## 直したこと

- `js/app.js`の`faveChip()`：4つの基本のお供えと同じ、丸いチェック
  バッジ（`#ic-check`）をタップできる好きだったものチップにも追加。
- `css/style.css`：`.fave`に`.chk`バッジのスタイルを追加
  （`.offer .chk`と同じ考え方。円形・`--grass-ink`背景・チェックの
  白いアイコン、そなえた瞬間だけ`display:grid`で現れる）。
- `js/app.js`の`#omairi-faves`クリックハンドラ：そなえた直後に
  `toast()`で「◯◯、そなえました」を一瞬表示。写真・動画を追加した
  ときと同じ、既存のトースト機構を流用した。

## 確かめたこと

`node --check`でapp.jsの構文確認。Playwrightで実際にタップし、
チェックバッジが表示されること・ボタンが押せなくなること・トースト
に「さつまいも、そなえました」と出ることをスクリーンショットで確認
した。

# 追記63：好きだったものが、開いた時点で最初からチェック済みに見える（2026-09-19）

追記62のマージ後、利用者から「最初から選択されているけど、自分で
選択したらチェックマークつくようにして」との指摘。

原因は、チェック表示の根拠を`S.faveDoneOn(today, name)`——保存に
残る「今日すでにそなえたか」——にしていたこと。好きだったものは
1日のうちに何度おまいり画面を開いても記録は同じ日付キーを指すため、
その日すでに一度そなえていれば、次に画面を開いた瞬間から（自分で
何もタップしていなくても）チェック済み・押せない状態で表示されて
いた。これに対して4動作（とうみょう・みず・ごはん・はな）は
`rstep`という画面を開くたび0に戻る変数で管理しており、毎回まっさら
から始まる。好きだったものだけこの扱いが違っていたのが、今回の
「最初から選択されている」の実体だった。

## 直したこと

- `js/app.js`：チェック表示の根拠を、保存された`S.faveDoneOn`から、
  4動作の`rstep`と同じ「このおまいりを開いてから自分がタップした
  もの」（`sessionFaves`、`startRitual()`で毎回`{}`にリセット）に
  変更。そなえたこと自体（`S.putFave`）は今まで通り保存するが、
  見た目のチェックはそれを読み返さない。
- `js/store.js`：チェック表示側で使わなくなった`faveDoneOn()`を削除
  （死んだコードを残さない）。記録用の`putFave()`・`state.faveDone`
  自体は変更なし（将来の振り返り用に記録は残す）。

## 確かめたこと

`node --check`でapp.js・store.jsの構文確認。Playwrightで「今日すでに
さつまいもをそなえた」という保存状態を用意したうえでおまいり画面を
開き、開いた直後はチェックが付いていないこと、自分でタップした
瞬間にはじめてチェックが付くことをHTML差分で確認した。

# 追記64：灯籠の灯り——4つそろう前は消えている、昼間でも分かる色で（2026-09-19）

追記55で「4つお供えすると両脇の灯籠が光る」演出を入れたが、実装は
`.r-flame`（火の部分）の色自体は最初から常に橙で、`.lit`のときだけ
`drop-shadow`によるふわっとした光の演出を足すだけだった。利用者から
「4つ選択する前は消えていて、お供え完了すると灯るようにして欲しい。
昼間は良く見えないので、わかるように表現して」との指摘。`drop-shadow`
の淡い光暈は、背景が明るい昼間の配色では地の色との差がほとんど
出ず、「灯った」ことが伝わっていなかった。

## 直したこと

- `css/style.css`：`--reien-flame-off`（灯籠の石と同系統の落ち着いた
  グレー）を新設。`.r-flame`の既定の色をこれに変更＝4つそろう前は
  「灯が入っていない灯籠」に見える。
- `.reien.lit .r-flame`で、そのとき初めて`fill`を橙（`--reien-flame`）
  に変える。光の演出（`drop-shadow`のパルス）は今まで通り残すが、
  「灯った」ことを伝える主な合図は色そのものの変化にした——これなら
  昼夜を問わず一目で分かる。色の切り替えに`transition:fill .6s ease`
  を添え、パッと変わるのではなく灯がともる自然な速さにした。
- 夜テーマは元々`.r-flame`を常に別の橙（`#E0A24A`）で上書きしていた
  ため、そのままでは常時点灯に戻ってしまう。夜テーマ用にも
  `--reien-flame-off`／`--reien-flame`をそれぞれ定義し直し、消灯・
  点灯の仕組みを昼夜で共通にした。

## 確かめたこと

`node --check`でapp.jsの構文確認（この修正はCSSのみでJS変更なし。
`.reien.lit`の切り替え自体は追記55の`renderRitual()`のロジックを
そのまま使う）。Playwrightで実際の`fill`計算値を確認：4つそろう前は
`rgb(201,194,180)`（グレー）、そろった後は`rgb(240,178,92)`（橙）に
変わることを昼テーマ・夜テーマ双方でスクリーンショットとともに確認
した。

# 追記65：おうちの背景を自動で今の季節に、「じぶん」タブを目立たせる、アプリアイコンを見直す（2026-09-19）

利用者から3件の依頼。

1. 「おうちの背景の季節は、本当の時期に合わせて自動で変わるように
   して。春456月・夏789月・秋101112月・冬123月←これ合っているか
   確認して。」
2. 「下部のアイコンは自分のアイコン（じぶんタブ）だけ添付（PayPayの
   『支払う』ボタンのような、丸く浮き上がったアイコン）のような
   感じにして。」
3. 「スマホ用アプリアイコンも、選ばれやすいデザインにして。」

## 1. おうちの背景の自動切り替え

これまで`おうちの背景`（春・夏・秋・冬の配色）はオンボーディングと
設定で手動で選ぶだけで、実際の季節とは無関係だった（既定は固定で
「夏」）。

月の区切りについて、利用者の提案（春4-6月・夏7-9月・秋10-12月・
冬1-3月）を確認したところ、**気象庁の定める気象学的な四季
（春3-5月・夏6-8月・秋9-11月・冬12-2月）と1か月ずれている**。
一般的な日本のアプリ・天気サービスで広く使われているのは気象庁の
区分のため、今回はそちらを採用した（`js/app.js`の`seasonNow()`）。

- `js/app.js`：`SCENES`の先頭に「自動（今の季節）」を追加。
  `sceneOf()`は保存値が空または`'auto'`のとき`seasonNow()`（実際の
  月から算出）を返し、特定の季節が明示的に選ばれていればそれを
  最優先する。ピッカーの選択状態表示だけは生の保存値を見る
  `sceneRaw()`を新設（`sceneOf()`をそのまま使うと、自動モードでも
  「今の季節」のボタンが押されて見えてしまうため）。
- `js/store.js`：`blank()`の既定値を`'summer'`固定から`'auto'`に変更。
  既定のまま一度も触っていない保存（`scene==='summer'`）も`'auto'`
  に引き上げる移行を追加（設定画面からいつでも選び直せるので、
  仮に明示的に夏を選んでいた場合の実害は小さいと判断）。
- `css/style.css`：「自動」の見本（`.sw`）は単一の季節色を持たない
  ため、4つの季節の空色を組み合わせた風車状の`conic-gradient`に
  した。「自動」ボタンをピッカーの先頭・2列ぶんの1行にし、いちばん
  選んでほしい既定を目立たせた。

## 2. 「じぶん」タブを丸く浮き上がらせる

利用者が例に挙げたPayPayのタブバーは、「支払う」だけが赤い丸として
バーの上に浮き上がり、他の4つは普通のアイコンのまま——という構え。
どのタブを指すか確認したところ、5つのうち「じぶん」（気分の記録）
という回答だった。

- `js/app.js`の`buildTabs()`：アイコンを`<span class="tab-ic">`で
  包み、`jibun`のボタンにだけ`tab-hero`クラスを付けるようにした。
- `css/style.css`：`.tab-hero .tab-ic`をタブの箱から絶対配置で
  外に出し、バーの上端に橙(`--amber`)の丸として乗せる。白い縁取り
  （`border:3px solid var(--panel)`）でバーの色と分離し、影を添えて
  浮いて見えるようにした。アイコンは白抜き。ラベルは他のタブと
  同じ場所に残る。

## 3. アプリアイコンの見直し

これまでのアイコンは、オンボーディングの例として出しているだけの
「モコ」（見本の犬）を空と芝生の上に置いたもので、次の2点で
アプリアイコンとしては弱かった。

- **アプリの本体（「灯」＝小さな灯り）を表していない。** 見本の
  ペットは利用者自身の子とは無関係で、汎用のマスコットのように
  見えてしまう。
- **細部が多く、小さいサイズで潰れる。** 空・芝生・キャラクターの
  3層の絵は、ホーム画面の40px前後の表示では輪郭が判別しにくい。

成功しているアイコンの共通点（Calm・Headspace・多くの瞑想／祈り系
アプリ）は、単一の力強いシルエット・少ない色数・小さいサイズでも
崩れない太さ、という3点に集約される。最初の案は、アプリ名そのものと
劇中の「とうみょう」「灯籠」で繰り返し使っている**灯りの炎**だけを
主役にしたものだった。

これに対して利用者から「もう少しペットのお墓と分かるような感じが
良い。世の中調査して再考して」との追加依頼。世の中のペット供養
関連のデザイン（ペット霊園・追悼キャンドル・墓石アイコン素材等）を
調べたところ、**肉球（ペットそのものの証）＋キャンドルや灯り
（追悼）＋お墓・花**が定番の組み合わせだと分かった。ただしこの
アプリ自身、`.reien`（おまいりの絵）の実装コメントに「お墓そのもの
（四角い墓石の列）は重く見えるため避け」とすでに明記してある通り、
literal な墓石は使わない方針を最初から持っている。そこで、定番の
組み合わせのうち**肉球＋灯り**を採用し、墓石は使わずに「ペットの
memorial」だと伝わるようにした。

- 暖色のクリーム地（`#FDF6E8`→`#F7E3BE`の縦グラデーション）を背景に、
  下に肉球（茶系グラデーションの丸い指球4つ＋大きな掌球1つ、左右
  対称の標準的な肉球シルエット）、その真上に小さな灯りの炎
  （最初の案と同じモチーフ）を重ねて据えた。肉球だけでは「ペット」
  としか伝わらず、炎だけでは「灯り」としか伝わらないため、両方を
  1つの塊にまとめることで「ペットの，灯りをともす場所」＝おまいり
  の場を指すようにした。
- 最初は炎を肉球の指球の1つに重ねる配置を試したが、40px相当の
  縮小で肉球と炎の境界が溶けて見えたため、肉球を左右対称の4本指
  そのままに保ち、炎はその真上・わずかに指の付け根へ重なる程度の
  位置に独立させた。これにより肉球のシルエットが常にくっきり残る。

この肉球案を見た利用者から「最初の犬のイラストの左側に灯火マークが
ある感じがいい」との指示。肉球という抽象的なモチーフより、もともと
アプリが使っていた犬のキャラクター（`#art-dog`。特定の見本ペットの
絵ではなく、犬を選んだ利用者全員に使われるこのアプリの標準の犬の
絵）の方が「このアプリらしさ」として馴染みがある、という判断だと
理解し、そちらを採用した。

- 空とみどりの2色（もとのアイコンと同じ構図）を背景に、`#art-dog`の
  パス（`index.html`のsymbol定義）をそのまま複製して縮小・配置。
  絵そのものは変えず、左どなりに小さな灯りの炎（肉球案と同じ炎の
  モチーフ）を光暈つきで添えた。犬は場所を空けるため中心よりやや
  右へ寄せた。
- `icons/icon-512.png`・`icons/icon-192.png`・
  `icons/apple-touch-icon.png`・
  `ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png`
  をすべて差し替え。いずれも透過なし（iOSの1024pxマスター画像は
  透過があると弾かれるため、最初から不透明で作った）。
  Androidのアダプティブアイコン用のプロジェクトはまだ無いため、
  今回はWeb版・iOS版のみ対応。

## 確かめたこと

- `node --check`でapp.jsの構文確認。
- Playwrightで今日（9/19）に開くと`autumn`（気象庁区分で正しい）に
  解決されること、設定の背景えらびで「自動（今の季節）」が選択
  状態で表示されることを確認。
- タブバーのスクリーンショットで、「じぶん」だけが丸く浮き上がって
  見えることを確認。
- 新アイコンをPlaywrightで1024px・192px・60px・40pxそれぞれの
  解像度で実際にラスタライズし、40px（ホーム画面相当の最小表示）
  でも犬の輪郭と灯りの両方が判別できることを確認。

# 追記66：「あれから◯日」を大きく、おまいりの骨壺を遺影に近づける（2026-09-19）

利用者から2件。

1. 「自分ページのあれから何日はもう少し大きく表示。」
2. 「おまいりのページで骨壺はもう少し写真に被らない程度で近くにして。
   バランスよくみて。」

## 直したこと

- `css/style.css`の`.since-pill`：じぶん画面の見出し「じぶん」の
  隣にひとつだけ置く数字なので、`--fs-2`（14px・補助の説明の大きさ）
  から`--fs-4`（17px・本文の基準）に。paddingも合わせて微調整。
- `css/style.css`の`.reien .ashes-frame`（おまいり画面、納骨後に
  台座の左どなりへ出る骨壺）：`left:21.8%`という固定%指定だと、
  遺影（`.face-md`、常に124px固定）とのあいだの隙間が画面幅に
  よって伸び縮みしてしまう（骨壺側は%・遺影側はpx固定のため）。
  遺影の中心（50%）から半径62px・隙間3px・自分の幅ぶんを引いた
  `calc(41.3% - 65px)`に変更し、画面幅によらず遺影との隙間が常に
  約3pxになるようにした。

## 確かめたこと

Playwrightで`Store.putMedia`によりお骨の写真を仮登録し、骨壺が
実際に表示される状態を作って確認。390px幅・320px幅（狭い端末を
想定）のどちらでも、遺影の左端と骨壺の右端の隙間が常に約3.0pxで
あり、重ならないことを確認した。「あれから◯日」はスクリーン
ショットで、見出しに対して十分読める大きさになったことを確認した。

# 追記67：バージョン表示と、配信前の総点検（2026-09-19）

利用者から「設定画面の中にバージョンを入れて。まずは Ver 1.0」
「それができたら、最後のバグ調査、修正、セキュリティ面チェック、
アプリ最適化をやって。スマホネイティブアプリとして配信できるように」
との依頼。

## 1. バージョン表示

付番のきまりは利用者が決めたもので、**こちらの判断では上げない**:

| 変更の大きさ | 上げかた |
|---|---|
| 軽微な修正（不具合直し・文言・見た目の微調整） | 据え置き |
| 小さな機能追加 | +0.1 |
| 大幅な機能変更 | +1 |

`js/app.js`の先頭に`APP_VERSION`を置き、設定画面の「プライバシー
ポリシー」と「この端末のデータを消す」のあいだに、押せない行
（`.setrow-info`）として「バージョン　Ver 1.0」を出す。上げるときは
`package.json`の`version`と、iOSの`MARKETING_VERSION`（2か所）も
同じ数字にそろえる旨をコード内コメントに残した。

## 2. バグ調査

Playwrightで全画面・主要導線（オンボーディング／おまいり4動作＋
好きだったもの／にわ／じぶん／アルバム／うごく／てがみ／設定／
夜テーマ／まっさらな起動）を通したところ、**JSエラーは0件**だった。

そのうえで「壊れた保存データ」を6通り作って流し込んだところ、
2件の落ちどころが見つかった。

- `visits.forEach is not a function` — 配列であるべき所に文字列が
  入っていた場合
- `Cannot read properties of null (reading 'slice')` — 日付として
  読めない記録が混じっていた場合

保存が壊れる道は現実にある（書き込みの途中で端末の空きが尽きた、
古い版の保存が残っている、他の端末のバックアップを読み込んだ）。
形が違うまま描くとその画面がまるごと出なくなるため、`js/store.js`の
`load()`に型をそろえる`coerce()`を足し、`visits`は日付として読める
ものだけ、`letters`は本文を持つものだけを残すようにした。直したあと、
6通りすべてでホーム画面まで到達することを確認した。

## 3. セキュリティ

| 見たところ | 結果 |
|---|---|
| XSS（名前・手紙・戒名・動画の見出しにHTMLを仕込む） | 発火せず。`esc()`が全経路に効いている |
| プロトタイプ汚染（`__proto__`入りのJSON） | 汚染されず。`restoreAll`は既定値のキーだけを写す作り |
| CSP | `script-src 'self'`（`unsafe-inline`/`eval`なし）、`object-src 'none'`、`connect-src 'self'`。インラインの`onclick`等は0件 |
| 外部通信 | Google Fonts以外への経路なし（CSPで遮断）。解析・広告・クラッシュ収集のSDKは無し |
| iOSの権限文言 | カメラ・写真・写真追加の3つとも日本語で用途を明示済み |
| ATS | 既定のまま（例外を開けていない） |

## 4. 最適化

- **メモリ**：`createObjectURL`の7か所すべてに対応する解放があり、
  取りこぼしは無かった。
- **重さ**：8年ぶん毎日おまいり（2,920件）＋気分の記録2,920件＋
  手紙500通を入れて計測。起動375ms・にわ474ms・じぶん470ms・
  てがみ一覧500ms・JSヒープ5MB・保存192KB。追記55の3段まとめが
  効いていて、花の数が増えても描画は増えない。
- **オフライン**：Google Fontsを遮断した状態で開いたところ、
  iOS標準の丸ゴシック（Hiragino Maru Gothic ProN）に落ちて
  見た目はほぼ変わらなかった。日本語書体の同梱は10〜20MB増える
  わりに得るものが無いため、**見送る**と判断した。
- **配信物**：432KB（うちapp.js 108KB・style.css 68KB）。開発用の
  ファイルは混じっていない。`ios/App/App/public/`はgit管理外で、
  Codemagicが毎回`npm run build && npx cap sync ios`で作り直すため、
  privacy.htmlを含む最新のwww/が確実に入る。

## 5. 書きかけの手紙を守る（この点検で見つかった、いちばん大事な直し）

追記61で入れた「新しい版に切り替わったら自動で読み込み直す」が、
**いつ起きるか選べない**ことに気づいた。あの子への手紙を打っている
最中にこれが起きれば、書いた言葉はそのまま消える。このアプリが
いちばんしてはいけないことなので、二重に手を打った。

- `js/register-sw.js`：読み込み直すのを「おうちに居て、書きかけも
  問いかけも無いとき」だけに限った。手紙を打っている最中はもちろん、
  送ったあとの「とどきました」の最中に割りこむのも防ぐため、
  条件を"書きかけの有無"ではなく"いま落ち着いた場面か"にしてある。
  見送っても、新しいサービスワーカー自体はもう有効なので、次に
  アプリを開いたときには確実に新しい版が出る。
- `js/store.js`＋`js/app.js`：打つそばから控えを取り、次に開いた
  ときに戻すようにした（`tomoshibi.draft.v1`）。これなら自動更新
  だけでなく、iOSが裏のアプリを落とした場合やうっかり別のアプリへ
  移った場合も消えない。送ったとき・自分で閉じたときには消える。
  控えは本文とは別のキーに置き、バックアップには混ぜない。

## 確かめたこと

Playwrightで、打っている途中に控えが残ること・開き直すと戻ること・
「おくる」が押せる状態で戻ること・送ると控えが消えること・自分で
閉じても消えることの5点を確認。そのうえで全画面の通し確認をやり直し、
エラーが増えていないことを確認した。`sw.js`の`CACHE`はv8→v9。

# 追記68：おうち画面の日付横のマークを、太陽から灯りに（2026-09-19）

日付の横にあった太陽マーク（`#ic-sun`）は、実際の天気とは無関係の
ただの飾りだった。利用者から「実際の天気に連動できるか」との相談を
受けたが、それには位置情報の許可と外部の天気APIへの通信が要る。
このアプリは「通信は一切しない（Google Fontsのみ例外）」「圏外でも
おまいりできる」を軸に据えており、プライバシーポリシーにもそう
明記してある。日付横の小さな飾りのためにそこを崩す理由が無いと
判断し、やめることを勧めた。

代わりに何のマークがよいかを相談し、**灯り**（アプリ名そのもの、
アプリアイコン・オープニング画面・おまいり画面の灯籠で繰り返し
使っている炎のモチーフ）にした。天気のような「毎回変わるが実は
何も表さない」飾りより、「ともしび」という名前を開くたびに小さく
思い出せるほうが、このアプリらしい。

## 直したこと

- `index.html`：`#home-date`の隣のアイコンを`#ic-sun`から、既存の
  `#of-light`（とうみょうの供え物と同じ炎の絵）に差し替え。新しい
  絵は起こさず、既にある symbol をそのまま再利用した。
- 使われなくなった`#ic-sun`のsymbol定義を削除（死んだコードを
  残さない）。

## 確かめたこと

Playwrightで`#ic-sun`への参照が残っていないこと、おうち画面の日付
横に灯りのアイコンが表示されることをスクリーンショットで確認した。

# 追記69：日付横のマークを、灯りから「この子」に合わせたものに（2026-09-19）

追記68で灯りに差し替えたところ、利用者から「ちょっと暗くなりすぎる」
との指摘。灯りは劇中（オープニング・おまいりの灯籠）では効くが、
おうち画面を開くたび毎回目にする場所に置くと、重さが勝ってしまう
ということだと理解した。

代わりに、登録した子の種類（いぬ・ねこ・そのほか、オンボーディング
の`#kindpick`で選ぶ既存の3択）に合わせて変えることにした。

- **いぬ** → 肉球
- **ねこ** → またたび
- **そのほか** → ハート（特定の動物に決め打ちしない、どの子にも合う印）

## 直したこと

- `index.html`：新しい小さな絵を3つ追加（`#mark-dog`＝肉球、
  `#mark-cat`＝またたびの葉、`#mark-other`＝ハート）。色づかいは
  既存の同系統の絵に合わせた——肉球は`#art-dog`の耳と同じ
  茶（`#C9A87E`/`#5A4A3A`）、またたびは草木の緑、ハートは
  `#of-flower`の花びらと同じ淡い桃（`#F0B6C4`）。
- `js/app.js`：`artRef()`（遺影の絵を選ぶ既存の関数）と対になる
  `markRef()`を新設。同じ`st.pet.kind`の3分岐で、日付横に出す
  マークを選ぶ。
- `renderHome()`で`#home-mark`（`<use>`のid）の`href`を毎回
  `markRef()`の結果に張り替える。

## 確かめたこと

Playwrightで`kind`を`dog`／`cat`／`other`それぞれに変えて開き、
`#home-mark`の`href`が`#mark-dog`／`#mark-cat`／`#mark-other`に
正しく切り替わることを確認。実際の見た目も、6倍に拡大した状態で
3つとも判別できる絵になっていることをスクリーンショットで確認した。

# 追記70：そのほかの絵をハートで揃える、節目の一言を増やす（2026-09-19）

利用者から3件。

## 1. 「そのほか」の絵をハートで統一

設定（プロフィール編集）の`#kindpick`（いぬ・ねこ・そのほかを選ぶ
ところ）で、「そのほか」だけ`#art-paw`（肉球の形をした胴体の絵）を
使っていた。追記69で日付横のマークの「そのほか」をハートに決めた
のに合わせ、こちらもハートに揃えた。

- `index.html`：`#kindpick`の「そのほか」ボタンの絵を`#art-paw`から
  `#mark-other`（ハート）に変更。`#art-paw`自体は、遺影の代わりの
  絵（`artRef()`が`kind==='other'`のとき返す絵）としてはそのまま
  残っている——ボタンの選択肢としての役目と、遺影の代わりとしての
  役目は別なので、後者は変えていない。

## 2. 節目ごとの一言を増やす

おまいりを終えた直後の「ありがとう。またね。」は、四十九日・百か日・
一周忌・三回忌・お誕生日のときだけ特別な一言に変わる仕組みが
すでにあった（追記46）。利用者から「それぞれの日にあった、飼い主
への心情に刺さる言葉を。月命日は複数パターンをランダムに、なるべく
多く」との依頼。

気づいたのは、**月命日はこの仕組みの対象に入っていなかった**こと
（`BIG_KEYS`に`monthly`が無く、`milestoneToday()`が拾わない）。
月命日は年に何度も来るため、`js/store.js`の`pastMilestones`／
「手紙のこだま」と混同しないよう、`BIG_KEYS`自体は変えずに
`showAfter()`側だけで別途「今日が月命日か」を確かめるようにした
（手紙のこだまは、月命日では出過ぎるので対象外のまま）。

- `js/app.js`：`MILESTONE_LINE`を、キーごとに配列を持つ形に変更。
  四十九日・百か日・一周忌・三回忌は一生に一度しか来ないので1本の
  まま（既存の文面を踏襲）。お誕生日は毎年来るので4本に増やした。
  **月命日は16本**用意し、`pickMilestoneLine()`で毎回くじ引きに
  した。「大きな節目です」で終わらない、通いなれた場所に毎月また
  来た、というトーンでそろえ、単調にならないようにした。
- `showAfter()`：`S.milestoneToday()`（四十九日〜お誕生日）を
  優先し、無ければ`S.milestones(today)`から`monthly`かつ`days===0`
  のものを別途探す。

## 3. 好きだったものの「2つまで」表記

確認したところ、オンボーディング／プロフィール編集の好きだったもの
の欄（`<label for="in-fave">`）には、すでに「ひとつずつ（2つまで・
あとから足せます）」と入っていた（追記58で2つに減らした際に
書き換え済み）。この点は追加の変更をしていない——利用者が見ている
画面がまだ古い版のままだった可能性がある。

## 確かめたこと

Playwrightで、`#kindpick`の「そのほか」が`#mark-other`になっている
ことを確認。節目の一言は、通常の日（「ありがとう。またね。」）・
四十九日ちょうど・月命日ちょうど（10回引いて重複が少なく散らばる
こと）をそれぞれ確認した。全画面の通し確認でエラーが増えていない
ことも確認した。

# 追記71：節目の一言、向きが逆だった（2026-09-19）

利用者から「お誕生日の『生まれてきてくれて、ありがとう』は、飼い主
からこの子への言葉になっている。逆向き」との指摘。確かめると、
追記70で書いた一言の多くが、あの子に語りかける言葉ではなく、
「あなたは強くなりましたね」のように**飼い主を励ます第三者の
ナレーション**になっていた。「あの子からあなたへ」で統一されて
いなければならなかった。

## 直したこと

`js/app.js`の`MILESTONE_LINE`を全件書き直した。全て「あの子の声で、
あなたに語りかける」形にそろえている。

- **四十九日**：「ここまで、そばにいてくれてありがとう。」
- **百か日**：「もう、やすらかな場所にいるよ。」
- **一周忌**：「1年間、忘れずにいてくれてありがとう。」
- **三回忌**：「ずっと、そばにいるよ。」
- **お誕生日**（4本）：「うまれてきて、よかったよ。」「今日は、
  うまれた日だね。」「何歳になっても、そばにいるよ。」「今日も
  いっしょに、お祝いしよう。」
- **月命日**（16本）：同じ考えで全て書き直した（一覧は
  `js/app.js`の`MILESTONE_LINE.monthly`を参照）。「あの子はきっと、
  今日も見ていますよ」のような三人称のナレーションは「今日も、
  ちゃんと見ているよ」のように一人称へ、「あなたは強くなりました
  ね」のような敬体のナレーションは「あなたは強くなったね」のような
  くだけた言い方へ、それぞれ直した。

「あなた」という二人称は、じぶん画面など既存の文面（「あなたらしく
生きていく」等）にすでにあるものをそのまま踏襲し、新しい言い回し
は持ちこんでいない。

## 確かめたこと

`node --check`でapp.jsの構文確認。Playwrightで四十九日・月命日
（10回引き直し）の一言が新しい文面に変わっていること、全画面の
通し確認でエラーが増えていないことを確認した。

## 追記：月命日の2本を差し替え（同日）

利用者から2件。

- 「少しずつでいいよ。今日はここまでで、じゅうぶん。」は味気ない
  との指摘。「ずっと応援しているよ」のようなトーンへ、と依頼された
  ため`ずっと応援しているよ。`に差し替えた。
- 「今月も、ちゃんと生きていたね。」は「飼い主の生には触れない」
  との指摘。文字通り、飼い主が「生きている」かどうかに触れる言葉に
  なってしまっていたため、`今月も、あなたを見守っていたよ。`に
  差し替えた。

# 追記72：節目の一言、1日のうちは固定にする（2026-09-19）

利用者から「何パターンかあるやつは、その日は固定にしてね。1日に
何度もお参りしても、その日分は固定で他の言葉は出ないように」との
依頼。それまでは`pickMilestoneLine()`が呼ばれるたび`Math.random()`
でくじを引いていたため、同じ日に2回おまいりすると別の一言に変わる
ことがあった——これでは選んでいるように見えてしまう。

## 直したこと

`js/app.js`の`pickMilestoneLine(key)`を`pickMilestoneLine(key, dateKey)`
に変更。くじ引きをやめ、「節目の種類＋今日の日付」を種にした簡易
ハッシュで番号を決めるようにした（手紙のこだま・`pickEchoLetter()`
と同じ考え方）。同じ日なら必ず同じ番号を引くので、1日に何度
おまいりしても一言は変わらない。日付が変われば種も変わるので、
翌月の月命日では別の一言になる。

## 確かめたこと

Playwrightで、同じ日（死亡日を固定した月命日）に3回連続でおまいり
し、3回とも同じ一言になることを確認した。あわせてNode単体で
ハッシュ関数を検証し、12か月ぶんの異なる月命日の日付を通したところ
16パターン中11通りに散らばることを確認した（かたよりが無いこと）。
全画面の通し確認でエラーが増えていないことも確認した。

# 追記73：日付横のマークを、「どちらですか」の絵とまったく同じに（2026-09-19）

追記69で「いぬ・ねこ・そのほか」に応じて肉球／またたび／ハートの
専用の小さな絵を用意したが、利用者から「設定の『どちらですか』
（#kindpick）で選ぶ絵と同じにして。犬なら犬のイラストのやつ」との
依頼。日付横だけの専用モチーフより、プロフィールで選んだ絵
そのものと一致させたいという判断だと理解した。

## 直したこと

- `js/app.js`の`markRef()`：`#mark-cat`は`#art-cat`、`#mark-dog`は
  `#art-dog`に変更。「そのほか」はもともと`#kindpick`と`#mark-other`
  （ハート）を共有していたため変更なし。これで日付横のマークは
  `#kindpick`で選ぶ絵と完全に同じになった。
- `index.html`：使われなくなった`#mark-dog`・`#mark-cat`のsymbol
  定義を削除（死んだコードを残さない）。`#mark-other`（ハート）は
  `#kindpick`「そのほか」と日付横のマークの両方で使う共有の絵の
  ため残した。

犬猫の絵（`#art-dog`／`#art-cat`）はもともと顔のイラスト用に
描いたもので、22px程度の小さな表示では輪郭がやや潰れて見える。
それでも「プロフィールで選んだ絵と一致させたい」という利用者の
明確な依頼のため、そのまま採用した。

## 確かめたこと

Playwrightで`kind`を`dog`／`cat`／`other`に変えたときの
`#home-mark`の`href`が、それぞれ`#art-dog`／`#art-cat`／
`#mark-other`になることを確認。全画面の通し確認でエラーが
増えていないことも確認した。

# 追記74：iPhoneの機種ごとの表示調整（2026-09-19）

利用者から「あとは、iPhoneなど各シリーズ毎に最適に表示されるように
調整して。iOSなども加味して」との依頼。現行〜少し前のiPhoneを一通り
（SE・mini・標準・Pro・Plus・Pro Max、幅375〜430px）Playwrightで
機械的に確認し、見つかった問題を直した。

## 見つけて直したこと

**おまいり画面が、いちばん縦の短いiPhone SEで「おまいりを終える」
ボタンまでスクロール無しで届かなかった。** 追記56・57で「スクロール
無しで終えるボタンまで見える」ことを目標にしていたが、その調整は
標準的な高さの画面を基準にしており、SE（667px）では本文の高さが
796pxあり約130px足りていなかった。

`css/style.css`に`@media (max-height:700px)`を追加し、高さ700px
以下の画面だけ、おまいりの絵・見出し・4動作のカード・好きだった
ものまわりの余白とカードの最小高さをさらに一段詰めた（横幅は
一切変えていないので、通常の高さの機種の見た目はそのまま）。
直した後、SE（375×667）で本文の高さ（796→667px）がちょうど
画面の高さに収まり、ボタンまでスクロール無しで届くことを確認した。
iOS 15以降が対象（`IPHONEOS_DEPLOYMENT_TARGET=15.0`）で、iOS 15が
動く最も古い機種（iPhone 6s、375×667）もこのSEケースでカバーされる。

**iOSプロジェクトの設定が、実際には作っていない横向き・iPadに
対応したままだった。** `npx cap add ios`の既定のまま、
`UISupportedInterfaceOrientations`に横向きが、`TARGETED_DEVICE_FAMILY`
に`"1,2"`（iPhone+iPad）が残っていた。この画面はどれも縦専用・
固定高さの絵・下部タブ固定で作られており、横向きやiPadの大きな
画面に対応する作りにはなっていない。中途半端に「対応している」と
名乗るより、作ってある通り「iPhoneの縦のみ」に絞るのが実態に
合っていると判断した。

- `ios/App/App/Info.plist`：`UISupportedInterfaceOrientations`を
  縦（Portrait）のみに。iPad用の別キー（横向き・上下逆さ込み）は
  削除。
- `ios/App/App.xcodeproj/project.pbxproj`：
  `TARGETED_DEVICE_FAMILY`を`"1,2"`から`1`（iPhoneのみ）に。

## 確認して、問題が無かったこと

- 幅375〜430px（SE〜Pro Max）のどの機種でも、おうち・おまいり・
  設定・にわ・じぶんの5画面で横方向のはみ出しは無かった。
- 名前・好きだったもの・一言などの文字入力欄はすべて16px以上の
  文字サイズで、iOSがフォーカス時に勝手に拡大するのを避けられて
  いる（既存のまま、追加の対応は不要だった）。
- キーボードが出たときの回避は、対象画面（てがみを書く等）が
  すべて`overflow-y:auto`のスクロール領域に入っており、固定の
  `100vh`指定も使っていないため、WKWebViewのビジュアルビューポート
  変化にそのまま追従できる作りになっている。
- ノッチ・Dynamic Island・ホームインジケータぶんの余白
  （`env(safe-area-inset-*)`）は、ヘッダー・タブバー・シート・
  トースト・オンボーディングのフッターなど、画面端に触れる場所
  すべてにすでに入っていた（このセッションで新たに追加したものは
  無い）。

## 確かめたこと

Playwrightで7つの画面幅・高さの組み合わせ（SE・mini・標準・Pro・
Plus・Pro Max・下限確認用の極小機種）× 5画面（おうち・おまいり・
設定・にわ・じぶん）＝35通りを機械的に確認し、横はみ出し・JSエラー
ともに0件だった。おまいり画面の「終えるボタンがスクロール無しで
見えるか」は、直す前はSEと極小機種の2件がNGだったが、直した後は
実在するiPhone全機種（SE〜Pro Max）でOKになった（iOS 15が動作しない
超小型の旧機種1件のみ、テストの下限確認として意図的に対象外）。

# 追記75：おまいりの庭の注記から「他の人との比較」の一文を削除（2026-09-19）

利用者から、おまいりの庭にある「連続記録はありません。何日あいても、
花は枯れません。ほかの人の数も出しません。ここにあるのは、あなたと
この子の数だけ。」のうち、後半の「ほかの人の数も出しません〜」の
一文を消してほしいとの依頼。

`index.html`の`#view-niwa`内`.notebox`と、対応するデザイン下書き
`design/Niwa.dc.html`（サンプルデータでは「ももこの数だけ」と表示）
の両方から、該当の一文を削除した。前半の「連続記録はありません。
何日あいても、花は枯れません。」はそのまま残している。

## 確かめたこと

`npm run build`でwww/に反映後、Playwrightでにわ画面を開き、
`.notebox`の表示文言が「連続記録はありません。何日あいても、花は
枯れません。」の1文だけになっていること、および他の要素（花の数・
いっしょにいた日・あの日までのカード・タブバー）に崩れが無いことを
スクリーンショットで確認した。

# 追記76：設定画面の右上にも「閉じる」を追加（2026-09-19）

利用者から、設定画面は開いてすぐ閉じるパターンと、スクロールして
いちばん下で閉じるパターンの両方があるはずなので、右上にも
「閉じる」を付けてほしいとの依頼。いちばん下の「とじる」ボタン
（`#btn-settings-close`）はそのまま残す。

`index.html`の設定画面`.head`を、見出しと右上の小さな「閉じる」
テキストボタンが横並びになるようflexにした（`.head`クラス自体は
変えず、この画面のインラインstyleだけで対応。ほかの画面の見出しに
影響しない）。新しいボタンは「ぜんぶ見る」（うごくあの子カード）と
同じ、`.btn.btn-ghost`に`width:auto;min-height:auto`を重ねる既存の
やりかたを踏襲。文字は小さいが、見た目より広いタップ領域を持たせる
ため、`padding`と負の`margin`で当たり判定だけ上下左右に広げた
（実測で幅54px・高さ38px）。

`js/app.js`側は、いちばん下のボタンと同じ`show('home')`を新しい
ボタンにも紐づけただけで、閉じたときの挙動そのものは変えていない。

## 確かめたこと

Playwrightで、上のボタン・下のボタンそれぞれをクリックして
`view-home`に戻ることを確認。375×667（iPhone SEクラス）でも
横はみ出しが出ないこと、見出し「設定」とボタンの位置関係が崩れて
いないことをスクリーンショットで確認した。

# 追記77：おうち画面の「設定」を右上に移動（2026-09-19）

利用者から、実機で試すとおうち画面の「設定」ボタンがいちばん下に
あり、動画をたくさん入れるほど下に行ってしまうので、右上に移動して
ほしいとの依頼。

`#btn-settings`を、うごく一覧（`#home-vids`）の下から、いちばん上の
日付の行（`.daybar`）の右側へ移動した。`.daybar`は元から
`justify-content:space-between`のflexなので、右側に要素を1つ足す
だけで自然に右寄せになる。文字ラベル「設定」だと日付との横幅の
釣り合いが悪いため、アイコンのみのボタンに変更（`aria-label="設定"`
は残す）。

アイコンは最初、丸＋放射状の線（太陽のような形）で作ったが、
スクリーンショットで見ると実際の太陽（テーマの絵の中の☀）や、
にわタブのアイコン（`#t-niwa`＝丸＋放射状の線の花）と紛らわしい
ことに気づき、スライダー（横線3本＋つまみ）の形に描き直した。
既存のタブアイコンと同じ、細い線・角丸のstrokeスタイルに合わせて
ある。タップ領域は40×40pxを確保（見た目のアイコンは22px）。

ボタンの`id="btn-settings"`はそのまま引き継いだので、`js/app.js`側
の配線（`$('#btn-settings').onclick = function () { show('settings'); }`）
は変更していない。

## 確かめたこと

Playwrightで、新しい位置のボタンをクリックして設定画面に遷移する
こと、タップ領域が40×40pxあること、375×667（iPhone SEクラス）・
夜間テーマの両方で横はみ出しや見た目の崩れが無いことをスクリーン
ショットで確認した。

# 追記78：おうち画面の設定ボタンを大きく・歯車アイコンに（2026-09-19）

利用者から、追記77で右上に移したおうち画面の「設定」ボタンについて、
もう少し大きくしてほしいのと、アイコンをもっとわかりやすいものに
してほしいとの依頼。

design-best-practicesスキルを読み、アクセシビリティの原則
（タップ領域は44×44px以上）に沿って、`.daybar-set`のタップ領域を
40×40pxから44×44pxに広げた（見た目のアイコンも22pxから25pxへ）。

アイコンは、スライダー（横線3本＋つまみ）から、世界的にいちばん
「設定」として通じる記号である歯車（ギア）に描きかえた。前回
（追記77）は丸＋放射状の細い線で作って「にわタブの花」や太陽と
紛らわしかったため、その反省をふまえ、円のふちに沿った太めの
四角い歯を8つ配置する構成にして、線を放射状に伸ばすだけの
デザインにはしなかった（細い放射線は花・太陽と同じ見えかたになる
ため）。中心には穴（小さい円）も入れ、歯車と一目でわかる形にした。

## 確かめたこと

Playwrightでボタンの実測サイズが44×44pxになっていること、
クリックで設定画面に遷移することを確認。アイコン単体・daybar全体
それぞれのスクリーンショットで、歯車として認識できる見た目に
なっていることを確認した。

# 追記79：おうち画面の設定ボタンをさらに大きく（2026-09-19）

追記78で44×44pxにしたところ、利用者から「もう少し大きく」との
追加依頼。タップ領域を52×52px、見た目のアイコンを25pxから30pxに
拡大した（歯車の線の太さは30px用に1.5→1.4へわずかに調整し、
太すぎて歯がつぶれて見えないようにした）。

`.daybar`の右側にはほかに要素が無く、拡大しても日付側のテキストや
右側の余白と衝突しないため、比率だけ引き上げれば済んだ。

## 確かめたこと

Playwrightでボタンの実測サイズが52×52pxになっていること、
クリックで設定画面に遷移すること、横はみ出しが無いことを確認。
daybar・おうち画面全体のスクリーンショットで、歯車がはっきり
大きく見えつつ、ほかの要素とのバランスが崩れていないことを
確認した。

# 追記80：うごく動画タイルの押した手ごたえ・キャプション・並び順を直す（2026-09-19）

利用者から実機のスクリーンショットとともに、「うごく」タブで動画を
押してもなかなか反応しない、文字が被っているのでは、との指摘。
続けて「うごくすがた」という既定の題名も不要、日付だけでよい、
おうち画面のうごくプレビューは最新アップしたものから出してほしい、
との追加依頼が続いた。

## 見つけたこと・直したこと

**タップしても反応が無いように見えた本当の原因。** `css/style.css`に
「タップできるもの全部に共通の手ごたえをつける」という趣旨の
`:active{opacity:.6}`のまとめ指定があるが、動画・写真タイル本体
（`.tile`）だけがそこに入っていなかった。押しても見た目が何も
変わらないため、実際の処理（IndexedDBから動画を読み出してから
再生画面を開く、という非同期の一手間）にかかるわずかな時間が、
体感としては「反応していない」ように見えていた。`.tile`をこの
共通の`:active`指定に加え、あわせて`touch-action:manipulation`も
付けて、ブラウザ側の余計なタップ判定待ちが入らないようにした。

**タイルのキャプションを日付だけに。** これまでは動画の題名
（読み込めた元のファイル名、または既定の「うごくすがた」）と日付を
2行で出していたが、`js/app.js`の`tileHTML()`から題名の行を削除し、
日付だけを1行で出すようにした。`css/style.css`の`.tile .cap`も
1行ぶんの余白に詰め、使われなくなった`.tile .cap b`のスタイルは
削除した。動画の名前づけ機能（「···」メニューの「名前をつける」）や
再生画面上部のタイトル表示はそのまま残してあり、一覧の見た目だけを
整理した形。

**おうち画面のうごくプレビューを、最新アップしたものから表示。**
`renderHomeVideos()`の`vs.slice(-2)`（日付が古い順に並ぶ配列から
末尾2件＝最新2件を取り出す）はそのままに、`.reverse()`を足して
表示順を新しい方から先に出るようにした。「うごく」タブ本体の一覧は
これまでどおり古い順のまま（利用者からの依頼はおうち画面のプレ
ビューに限定されていたため）。

## 確かめたこと

Playwrightで、IndexedDBに日付の異なる動画を複数件直接仕込んで
確認した。タイルのキャプションが日付のみになっていること、おうち
画面のプレビューが新しい日付から先に並ぶこと（「うごく」タブ本体は
従来どおり古い順のまま）、タイルを指で押している間だけ
opacityが1→0.6→1と変化して手ごたえが出ることを確認した。

# 追記81：おうちのうごくプレビューを「登録した順」に直す（2026-09-20）

追記80で「おうち画面のうごくプレビューを最新順に」直したが、実際は
動画自体の日付（`v.at`＝撮影日や、ファイルの更新日時）を基準に
並べていた。利用者から、「最新」とは動画の日付ではなく、この端末に
登録した順のことだと指摘があった。例：今日あらためて動画を登録
すれば、その動画の日付が過去のものであっても、おうち画面ではいちばん
左（先頭）に出てほしい、という意味。

`v.at`（撮影日・表示している日付そのもの）と、「この端末に登録した
時刻」は別の概念なので、後者を独立した`addedAt`として新たに持つよう
にした。

- `js/store.js`：`putMedia()`が受け取った`rec.addedAt`を、索引
  （`tomoshibi.index.v1`）のエントリにも保存するようにした
  （`indexPut`・`loadEntry`の両方）。索引が失われたときの復旧経路
  （`migrate()`）はこれまでどおり`at`しか持たないため、その場合は
  `addedAt`が無いまま＝日付順にフォールバックする（後述）。
- `js/app.js`の`addVideos()`：動画を保存するとき`addedAt: Date.now() + i`
  （`i`はその回でのアップロード内での順番）を一緒に渡すようにした。
- `js/app.js`の`renderHomeVideos()`：`vs.slice(-2).reverse()`（日付
  基準）をやめ、`addedAt`（無ければ`at`で代用）の大きい順に並べ替えて
  から先頭2件を取る方式に変更。これで、動画自体の日付が同じ・または
  過去の日付でも、今まさに登録したものが確実に先頭（画面上でいちばん
  左）に出る。
- 「うごく」タブ本体の一覧（`renderVideos()`）は変更していない。
  こちらは引き続き、動画の日付（`v.at`）順のまま。

この仕組みより前に登録済みの動画は`addedAt`を持たないため、
`addedAt || at`で日付順にフォールバックする。挙動が急に変わって
困ることはなく、新しく登録するものから正しく「登録順」になる。

## 確かめたこと

Playwrightで、IndexedDBに「表示上の日付（`at`）はすべて同じだが、
`addedAt`だけ異なる」動画を3件仕込んで確認した。日付だけでは区別
できない状況でも、`addedAt`が一番大きい（＝最後に登録した）ものが
おうち画面プレビューの先頭に出ること、次に大きいものが2番目に出る
ことを確認。あわせて、日付も`addedAt`もばらばらな動画でおうち画面
（登録順）と「うごく」タブ本体（日付順）を見比べ、それぞれ意図した
順序のまま独立して動いていることと、JSエラーが出ないことを確認した。

# 追記82：再生画面左下の表示をファイル名から動画の日付に（2026-09-20）

利用者から、動画再生画面の左下に出ているファイル名を、動画自体の
日付表示に変えてほしいとの依頼。追記80でタイル一覧の題名（ファイル
名・「うごくすがた」）は消したが、再生画面の左下だけ`st.videoTitles`
（ファイル名または既定名）を出したままだった。

`js/app.js`の`playVideo()`で、`$('#player-title').textContent`の
中身を`st.videoTitles[v.id] || 'うごくすがた'`から
`v.at ? S.formatShort(new Date(v.at)) : ''`に差し替えた。タイルの
キャプションと同じ`S.formatShort`（例：`2026.5.17`）を使っている
ので、一覧で見ていた日付とそのまま同じ表記で続く。

## 確かめたこと

Playwrightで、`videoTitles`にわざと「IMG_0970」という名前を仕込んだ
うえで動画を再生し、再生画面左下に名前ではなく日付（動画自体の
`at`から計算した表記）が出ることを確認した。

# 追記83：「動画に名前をつける」機能を削除（2026-09-20）

追記80・82で、動画一覧のタイルと再生画面のどちらからも題名
（ファイル名・「うごくすがた」）の表示を無くした結果、動画の
「名前をつける」機能（「···」メニュー）でつけた名前を表示する場所が
どこにも無くなっていた。利用者に確認のうえ、この機能自体を削除。

- `js/app.js`：動画の「···」メニューから「名前をつける」の項目を
  削除（`消す`・`とじる`の2つだけに）。名前入力ダイアログだった
  `renameVideo()`、ファイル名から題名を作る`videoName()`を削除。
  動画追加時に`st.videoTitles[id] = videoName(f)`していた行と、
  それに伴っていた`S.save()`呼び出しも削除（この時点でstateを
  書き換える理由が無くなったため）。動画を消すときの
  `delete st.videoTitles[id]`も削除。
- `js/store.js`：既定状態（`blank()`）から`videoTitles: {}`を削除。
  古い保存に残っていた読めないファイル名（"_users_..."等）を
  既定名に戻す一度きりの移行処理も、対象のフィールドごと無くなった
  ため削除。

古い保存データに`videoTitles`がまだ残っていても、読み込み時の
`coerce()`は既知のキーだけを見るので害はなく、そのまま無視される。

## 確かめたこと

Playwrightで、動画の「···」メニューを開いて「名前をつける」が
出ないこと（「消す」「とじる」のみ）、動画の削除が変わらず動くこと、
JSエラーが出ないことを確認した。

# 追記84：Bundle IDを com.tomoshibi.app → com.tomoshibi.petmemorial に変更（2026-09-20）

利用者がApple Developer PortalでBundle IDを登録しようとしたところ、
`com.tomoshibi.app`は既に他の開発者に使われていて登録できなかった
（Bundle IDは全世界の開発者間で重複できない仕様）。
`com.tomoshibi.petmemorial`で登録し直したところ通ったため、コード側の
表記もすべて合わせた。

- `tomoshibi/capacitor.config.json`：`appId`
- `tomoshibi/ios/App/App.xcodeproj/project.pbxproj`：
  `PRODUCT_BUNDLE_IDENTIFIER`（Debug・Release両方）
- `codemagic.yaml`（リポジトリルート）：`bundle_identifier`と、
  コメント中の説明文

`docs/AppStore提出情報.md`には具体的なBundle IDの記載が無かった
ため、変更不要だった。

## 確かめたこと

`npm run build`がエラー無く通ることを確認した。実際のApple
Developer Portal・App Store Connect・Codemagic側の設定は、
このセッションのネットワーク制限で直接は確認できないため、
利用者自身の画面操作で進めてもらっている。

# 追記85：App Store Connect側の申請作業を進める（2026-09-20）

利用者と画面を見ながら並走し、以下を完了した。

- Apple Developer PortalでApp IDを登録（追記84の`com.tomoshibi.petmemorial`）
- App Store Connectで新規Appのレコードを作成。アプリ名「ともしび」も
  Bundle IDと同様すでに他者に使われていたため、
  「**ともしび - ペットのメモリアル**」に変更して登録した
  （App Store Connect上の管理名であり、アプリ内の表示名・アイコン名
  とは別物）
- 作成したAppの「Apple ID」（数字のみのApp識別番号：`6814046106`）を
  控え、リポジトリルートの`codemagic.yaml`にあった
  `APP_STORE_APP_ID`のプレースホルダー（`1234567890`）を実際の値に
  差し替えた

残りはApp Store Connect API キーの発行（Codemagic連携用）と、
Codemagicへのリポジトリ接続・Integration登録。

## 確かめたこと

`codemagic.yaml`の該当行が実値になっていることを確認した。

# 追記86：CodemagicでのiOSビルド初回セットアップと、つまずいた点（2026-09-20）

利用者と画面を見ながら並走し、Codemagic側の初回ビルドまでを進めた。
つまずいた点と直したことを記録する。

## つまずいた点1：署名の自動生成が効かない

`codemagic.yaml`の`ios_signing`＋`integrations.app_store_connect`
だけでは、まだ一度も証明書・プロビジョニングプロファイルが存在しない
Bundle IDの場合、Codemagicが自動で新規発行してはくれなかった
（`No matching profiles found for bundle identifier
"com.tomoshibi.petmemorial" and distribution type "app_store"`で
ビルド開始前に失敗）。

- 最初、App Store Connect APIキーの権限が「App Manager」だったが、
  証明書の新規発行には「**Admin**」権限のキーが要ることが分かり、
  Admin権限のキーを作り直して`tomoshibi_asc`を差し替えた
- それでも解決せず、Codemagicの「**Settings → Code signing
  identities**」画面から、手動で一度だけ証明書とプロビジョニング
  プロファイルを用意する必要があると判明：
  - iOS certificates タブ →「Generate certificate」で、Distribution
    証明書をCodemagic上で新規発行（ローカルのMacでCSRを作る必要が
    無く、クラウド上で完結する）
  - Apple Developer Portal側で、その証明書を使ってApp Store用の
    プロビジョニングプロファイルを作成・ダウンロード
  - Codemagicの iOS provisioning profiles タブから、その
    `.mobileprovision`ファイルをアップロードして登録
- つまり、`ios_signing`ブロックは「既にある署名情報を**使う**」
  設定であって、「**初めての証明書を自動で作る**」ところまでは
  やってくれない。最初の1回だけは、この手動セットアップが要る。

## つまずいた点2：Node.jsのバージョン不足

署名が解決した後、`npx cap sync ios`のステップで
`[fatal] The Capacitor CLI requires NodeJS >=22.0.0`のエラー。
`codemagic.yaml`の`environment.node`が`20`だったため、`22`に
引き上げた。

## 確かめたこと

Codemagic側のビルドログで、それぞれのエラーメッセージの全文を
確認したうえで対応した。この追記の時点では、Node.jsバージョンを
直した後の再ビルド結果はまだ確認できていない（次回の確認事項）。

### 追記：Node.js引き上げ後の再ビルドで成功（2026-09-20）

`node: 22`に直したあと、あらためて`main`ブランチでビルドを実行。
全ステップ（署名・`npm ci`・ビルド・`cap sync ios`・署名反映・
ビルド番号採番・ipa書き出し・Publishing）が緑（成功）になり、これで
CodemagicのCI/CDパイプラインが実際に動くことを確認できた。

**訂正：** この時点では「TestFlightへのアップロードまで自動で完了
した」と判断したが、誤りだった。「Publishing」ステップが緑だった
のは、ログの後始末（`desktop-tutorial_4_artifacts.zip`の公開）
だけが成功していたためで、実際にはApp Store Connectへの登録は
`Skip publishing to App Store Connect: no IPAs or PKGs found`で
スキップされていた（詳細は次の追記87）。App Store ConnectのTestFlight
タブにビルドが一件も出てこないことで発覚した。

# 追記87：ipaがPublishingステップに見つからず、アップロードがスキップされていた（2026-09-20）

`docs/UI設計.md`追記86で「TestFlightまで自動完了した」としたのは
誤りだった。利用者がApp Store ConnectのTestFlightタブを見ても
ビルドが1件も表示されず、Codemagicのビルド詳細を確認したところ、
「ipaを書き出す」ステップ自体は成功していたが、直後の「Publishing」
ステップで

```
Skip publishing to App Store Connect: no IPAs or PKGs found
```

と出ており、ipaファイルが見つからずアップロード自体がスキップ
されていたことが分かった。

## 原因（推定）

`codemagic.yaml`の`artifacts:`に`tomoshibi/build/ios/ipa/*.ipa`
（`working_directory: tomoshibi`からの相対だろうという推測で
`tomoshibi/`を付けていた）と指定していたが、`xcode-project
build-ipa`が実際に書き出す場所がその推測と一致していなかった
可能性が高い。`artifacts:`の照合パスは常にリポジトリのルートから
の相対だが、`xcode-project build-ipa`の既定の書き出し先が
`working_directory`基準なのかビルドディレクトリ基準なのか、
ドキュメントだけでは断定できなかった。

## 直したこと

`artifacts:`のipa指定を、固定パスの推測ではなく`"**/*.ipa"`という
再帰マッチに変更した。これでipaがどちらの場所に書き出されても
確実に拾える。

## 確かめたこと

この修正はコードの変更のみで、実際にPublishingがTestFlightへの
アップロードまで進むかどうかは、次回のビルドで確認する必要がある
（次回の確認事項）。
