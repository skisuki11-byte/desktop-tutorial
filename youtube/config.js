/* config.js — この場所に置いたアプリ全体で使うクライアントID。
 *
 *  ここに入れておくと、端末を変えても、ブラウザの保存領域が消えても、
 *  毎回貼り直さずに済む。スマホのアプリ内ブラウザ（LINEやXの中で開いたとき）や
 *  プライベートモードは保存領域を残さないことがあり、
 *  端末に覚えさせるやり方だけだと毎回入力になってしまう。
 *
 *  ▼ これを公開リポジトリに置いてよい理由
 *  ブラウザで動くGoogleログインでは、クライアントIDは元々公開される値である。
 *  このアプリを開いた人は誰でも、通信を見ればこの値を読める。
 *  Google もそれを前提にしていて、守りは「値を隠すこと」ではなく
 *  「承認済みの JavaScript 生成元」と「テストユーザー」で掛けている。
 *    ・生成元 … このIDは登録したドメインのページからしか使えない
 *    ・テストユーザー … 同意画面が「テスト」の間は、登録した人しか通らない
 *  したがって、このIDを他人が見ても、その人のブラウザで
 *  こちらのチャンネルの数字が見えるようにはならない。
 *
 *  ▼ 秘密にすべきなのはこちら（ここには絶対に置かない）
 *  クライアントシークレット。このアプリはそれを使わない方式なので、
 *  そもそも持つ必要がない。
 */
window.YTLAB_CONFIG = {
  // Google Cloud で作った「ウェブ アプリケーション」のクライアントID。
  // 例: '000000000000-xxxxxxxxxxxx.apps.googleusercontent.com'
  clientId: '128161798962-pdhlsrgs80vjuhmvbncivpgqdtt53umk.apps.googleusercontent.com'
};
